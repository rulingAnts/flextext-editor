# crowd.flextext.app — the Crowd Recorder.
#
# NOT FOR CRAWLING. Each recorder is reached by its own link, which is the researcher's to hand out, not a search engine's to find. The page worth finding is the landing page at
# https://flextext.app/, which lists this app among the rest.
#
# ⚠ ROBOTS.TXT IS A REQUEST, NOT A FENCE. Every line here is voluntary, and the crawlers most
# worth refusing are the least likely to honor it. No part of this app's security rests on it:
# the enforcing half is AI Crawl Control and the WAF on the zone. This is the polite half, and
# stating it costs nothing.
#
# ⚠ AND "Disallow" IS NOT "noindex": a URL linked from somewhere else can still appear in a
# search index without ever being crawled. If a host must be absent from search entirely, that
# is an X-Robots-Tag: noindex response header, not this file.

User-agent: *
Disallow: /

# ── States that police what their people may read ────────────────────────────────────────────
# These are the search engines and social platforms those states lean on. Somebody inside can
# still open the app directly — nothing here hides it from them. It only declines to be listed.
#
# (China: Baidu, Sogou, 360, Shenma/Yisou, Huawei Petal, Tencent, ByteDance)
User-agent: Baiduspider
User-agent: Sogou web spider
User-agent: Sogou inst spider
User-agent: Sogou Orion spider
User-agent: Sogou spider2
User-agent: 360Spider
User-agent: HaosouSpider
User-agent: Yisouspider
User-agent: PetalBot
User-agent: Sosospider
User-agent: Bytespider
Disallow: /

# (Russia: Yandex, Mail.ru, VK. ⚠ BOTH Yandex lines are needed, and this is documented by
#  Yandex: "Yandex" is read by their whole fleet EXCEPT the main indexer, which reads only
#  "YandexBot".)
User-agent: Yandex
User-agent: YandexBot
User-agent: Mail.RU_Bot
User-agent: vkShare
Disallow: /

# ── AI crawlers ──────────────────────────────────────────────────────────────────────────────
# On flextext.app and its subdomains, Cloudflare's AI Crawl Control already prepends its own
# managed block to this file and keeps it current. These lines are what covers the hosts that are
# NOT on that zone: the staging copies on workers.dev, and the GitHub Pages mirror.
#
# ⚠ Google-Extended and Applebot-Extended are the TRAINING agents only. Googlebot and Applebot
# are deliberately absent from every list in this file: blocking them would take the site out of
# ordinary search, which is the opposite of the point.
#
# ⚠ AND THE ANSWER ENGINES COST SOMETHING TO REFUSE. OAI-SearchBot, Claude-SearchBot,
# Claude-User, ChatGPT-User, Perplexity-User and DuckAssistBot are not trainers: they fetch a page
# so an assistant can answer with it and cite it. Refusing them means an assistant asked "is there
# an offline interlinear tool that round-trips with FieldWorks?" cannot surface this one. That is
# a real cost to being found, accepted here on purpose — deleting those six lines is the whole
# change if it is ever judged the wrong trade.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: anthropic-ai
User-agent: CCBot
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Meta-ExternalAgent
User-agent: Meta-ExternalFetcher
User-agent: FacebookBot
User-agent: Amazonbot
User-agent: Diffbot
User-agent: Omgilibot
User-agent: ImagesiftBot
User-agent: YouBot
User-agent: AI2Bot
User-agent: cohere-ai
User-agent: DuckAssistBot
User-agent: PanguBot
Disallow: /
