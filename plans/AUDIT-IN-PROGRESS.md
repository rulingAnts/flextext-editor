# Phase C authorization audit — LIVE SNAPSHOT (auto-written, do not edit)

⚠ This file exists because the previous fan-out audit's output was lost to a container reset
(project-split.md:721). It is rewritten every ~90s from the run's journal so findings survive
a reset. It is DELETED once the audit is written up properly — if you are reading it in a
finished repo, the run did not complete.

Agent results captured so far: **66**

## a0331010e172c246c

```json
{
  "findings": [
    {
      "title": "Text-scoped routes resolve the text by a caller-supplied docId through an account-wide Drive search, so a member can steal a text out of another project",
      "severity": "critical",
      "file": "worker/src/v1.js",
      "line": 3868,
      "claim": "POST /v1/instances/<id>/texts/<docId>/adopt (and .../move) authorize on the instanceId in the path but then locate the text folder by a caller-supplied docId with a Drive-wide tag search that is never constrained to the project authMember resolved \u2014 so a member of project A can re-parent a text folder belonging to project B into a device folder inside project A.",
      "attack": "Researcher M is a project_member of project A with caps {\"assignTexts\":true}; the owner also owns project B, which M is not a member of (or was removed from, and still holds the docIds). M knows a docId of a text in project B. M sends POST /v1/instances/<A-instance-id>/texts/<B-docId>/adopt with body {}. authMember({instance: A-instance}, 'assignTexts') resolves project A and returns ok. The route then runs the tag query for flextextDoc='<B-docId>' over spaces=drive (the owner's WHOLE Drive), finds project B's text folder, and calls driveReparent(access, f.id, toFolder, f.parents) where toFolder is a device folder in project A. Project B's text \u2014 every uploaded recording, flextext and consent file in it \u2014 is now physically inside project A, where M has drive:read/assignTexts, and its flextextUnassigned tag is cleared so nothing sweeps it back. Same sequence works via POST .../texts/<B-docId>/move {to:<another A instance>}.",
      "evidence": "worker/src/v1.js:3855-3875 \u2014\n  if (m === 'POST' && sub === 'texts' && seg.length === 6 && seg[5] === 'adopt') {\n    const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');\n    ...\n    const docId = String(seg[4] || '').replace(/[^\\w-]/g, '').slice(0, 64);\n    ...\n    const fq = encodeURIComponent(`appProperties has { key='flextextDoc' and value='${docId}' } and mimeType='application/vnd.google-apps.folder' and trashed=false`);\n    const found = await driveJson(access, 'GET', 'https://www.googleapis.com/drive/v3/files?spaces=drive&orderBy=createdTime&fields=files(id,parents)&q=' + fq);\n    const f = (found.files || [])[0];\n    ...\n      if (!(f.parents || []).includes(toFolder)) await driveReparent(access, f.id, toFolder, f.parents);\nThe identical unscoped search is repeated in /move at v1.js:3909-3910. Nothing between the authMember call and the reparent compares the found folder's parents to the resolved project's drive_folder_id."
    },
    {
      "title": "A member with assignTexts can mint a bearer streaming URL for ANY file in the owner's Drive (promptFileId is unscoped and unvalidated)",
      "severity": "critical",
      "file": "worker/src/v1.js",
      "line": 3828,
      "claim": "The mint call sites pass caller-supplied Drive file ids straight into mintTextfileUrl with r = ctx.owner, and /v1/textfile's member-revocation check validates only that the minter still belongs to the SCOPED instance's project \u2014 never that the file is inside that project \u2014 so assignTexts in one project yields read access to every app-created file in the owner's whole Drive, including other projects and the account master folder.",
      "attack": "Researcher M is a project_member of project A with {\"assignTexts\":true}. M obtains any Drive file id from the owner's account outside project A (e.g. from the enumeration in the /texts/<docId>/files finding, or cached from a project they were removed from). M sends POST /v1/instances/<A-instance-id>/texts/anything/assignment/finish with {\"promptFileId\":\"<foreign-file-id>\",\"ttlDays\":400}. authMember passes on project A. mintTextfileUrl is called with researcherId = ctx.owner.researcher_id, fileId = the foreign id, scope = null, minterId = M, so the token is v1-shaped with tk.m = M and a 400-day expiry. The response hands M the URL. GET /v1/textfile/<token> takes the `tk.m && !tk.i` branch, which asks only 'is M still a member of ANY project this owner owns' \u2014 true, because of project A \u2014 and then streams the file with the owner's Drive token to an unauthenticated fetch. The same works with audioFileId/flextextFileId on /move and /adopt, where the scope is a project-A instance and the membership check passes for the same reason.",
      "evidence": "worker/src/v1.js:3826-3828 \u2014\n  const audioUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.audioFileId, '', ttlMs, scope, ctx.caller.researcher_id);\n  const flextextUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.flextextFileId, '', ttlMs, scope, ctx.caller.researcher_id);\n  const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);\nmintTextfileUrl (v1.js:1209-1210) does no validation at all: `if (!fileId) return null; const tk = { r: researcherId, f: fileId, ... }`.\nRedemption, v1.js:1885-1896:\n  ok2 = tk.i ? ...'FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?'\n             : ...'FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?'\nNeither query mentions tk.f. clampTtlDays (v1.js:1186-1190) allows up to 400 days."
    },
    {
      "title": "GET /v1/instances/<id>/texts/<docId>/files lists any text in the owner's entire Drive, not just the resolved project's",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 3676,
      "claim": "The route authorizes drive:read against the instance in the path, then runs the flextextDoc tag search over spaces=drive with a docId the caller fully controls, returning folder ids, file ids, names and sizes for a text that may belong to a different project of the same owner.",
      "attack": "Researcher M is a project_member of project A with {\"drive\":\"read\"}. M sends GET /v1/instances/<A-instance-id>/texts/<docId-belonging-to-project-B>/files. authMember resolves project A and passes; the `owned` re-check only confirms the A instance still belongs to the owner. The Drive query is then account-wide, so the response body carries project B's text folderId, its originalsFolderId, and every child file's id, name, size, mime and role. Those file ids feed directly into the mint finding above to read the bytes.",
      "evidence": "worker/src/v1.js:3663-3680 \u2014\n  const ctx = await authMember(request, env, { instance: instanceId }, 'drive:read');\n  ...\n  const docId = String(seg[4] || '').replace(/[^\\w-]/g, '').slice(0, 64);\n  ...\n  const fq = encodeURIComponent(`appProperties has { key='flextextDoc' and value='${docId}' } and mimeType='application/vnd.google-apps.folder' and trashed=false`);\n  const found = await driveJson(access, 'GET', 'https://www.googleapis.com/drive/v3/files?spaces=drive&orderBy=createdTime&fields=files(id)&q=' + fq);\n  ... return j({ folderId, originalsFolderId: ..., files }, 200, origin, env);\nThe comment on driveEnsureTextFolder states the same property explicitly (v1.js:1134-1136): \"The tag search is scoped to trashed=false but NOT to the parent\"."
    },
    {
      "title": "assignment/upload/start writes to an entirely unvalidated caller-supplied Drive parent folder",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 3765,
      "claim": "`parent` comes straight from body.originalsFolderId with no files.get check, no parentage check and no relation to the instance, text or project that authMember resolved, so a member with assignTexts can create files anywhere in the owner's app-created Drive tree \u2014 another project's folders, another project's text folders, or the FlexText Uploads master folder.",
      "attack": "Researcher M is a project_member of project A with {\"assignTexts\":true}. M first calls GET /v1/instances/<A-instance-id>/texts/<B-docId>/files (previous finding) to learn project B's text folder id F. M then calls POST /v1/instances/<A-instance-id>/texts/<any-docId>/assignment/upload/start with {\"name\":\"consent.wav\",\"mime\":\"audio/wav\",\"size\":1024,\"kind\":\"audio\",\"originalsFolderId\":\"F\"}. The route sets parent = \"F\" verbatim and opens a Drive resumable session with parents:[F] under the OWNER's token; M then PUTs the bytes through .../assignment/upload/chunk. The file lands inside project B, planted with a legitimate flextextRole tag, and project B's devices/panel will treat it as source material. The same call with F = the master folder id writes outside every project.",
      "evidence": "worker/src/v1.js:3763-3777 \u2014\n  const access = await driveAccessToken(env, r);\n  let parent = String(body.originalsFolderId || '');\n  if (body.kind === 'consent-prompt') { parent = await driveEnsureDeviceFolder(...); }\n  if (!parent) return j({ error: 'bad_folder' }, 400, origin, env);\n  const init = await fetch('https://www.googleapis.com/upload/drive/v3/files?uploadType=resumable&fields=id', {\n    method: 'POST', headers: { Authorization: 'Bearer ' + access, ... },\n    body: JSON.stringify({ name, mimeType: mime, parents: [parent], appProperties: { flextextRole: role } }),\n  });\nThe sibling begin route has the same hole one level up (v1.js:3737): `driveEnsureTextFolder(access, deviceFolder, docId, body.title, body.folderId)`, whose knownId path returns any caller-named folder that merely exists and is untrashed (v1.js:1147-1153)."
    },
    {
      "title": "/texts/<docId>/move accepts a destination instance in a different project of the same owner",
      "severity": "medium",
      "file": "worker/src/v1.js",
      "line": 3900,
      "claim": "The destination instance named in body.to is validated only against the owner's researcher_id, never against ctx.project_id, so a member of one project can push a text into a device belonging to a project they have no grant on.",
      "attack": "Researcher M is a project_member of project A with {\"assignTexts\":true} and knows an instance id in the owner's project B (for example from a membership in B that has since been deleted). M sends POST /v1/instances/<A-instance-id>/texts/<A-docId>/move {\"to\":\"<B-instance-id>\"}. authMember resolves project A from the path instance and passes. The `to` lookup binds researcher_id = ctx.owner.researcher_id, which is the same denormalized owner for every one of that owner's instances in every project, so the B instance resolves; the text folder is then re-parented under B's device folder and delivery tokens are minted for it. A member acting outside their project boundary silently re-homes project A's data into project B.",
      "evidence": "worker/src/v1.js:3890-3903 \u2014\n  const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');\n  ...\n  const r = ctx.owner;   // the PROJECT OWNER's row\n  const toId = String(body.to || '');\n  ...\n  const to = await env.DB.prepare('SELECT instance_id, nickname, oauth_folder_id FROM instance WHERE instance_id=? AND researcher_id=? AND revoked=0')\n    .bind(toId, r.researcher_id).first();\n  if (!from || !to) return j({ error: 'not_found' }, 404, origin, env);\nNo second authMember({ instance: toId }) call and no comparison of the destination's project_id with ctx.project_id appears anywhere in the route."
    },
    {
      "title": "The assign and uploadDelete commands are gated on manageDevices, bypassing the separately-defined assignTexts capability",
      "severity": "low",
      "file": "worker/src/v1.js",
      "line": 3554,
      "claim": "POST /v1/instances/<id>/command requires only manageDevices, yet the command types it accepts include 'assign', 'delete' and 'uploadDelete' \u2014 the text-assignment and text-removal actions that validateCaps models as the distinct assignTexts capability everywhere else.",
      "attack": "The owner adds researcher M to project A with caps {\"manageDevices\":true} and deliberately withholds assignTexts, intending M to enroll and rename devices but not to hand out or remove texts. M sends POST /v1/instances/<A-instance-id>/command with {\"command\":{\"type\":\"assign\",\"id\":\"<docId>\",\"url\":\"<any url>\"}} \u2014 accepted, queued with by = M, and delivered to the field device on its next poll. The same call with {\"type\":\"uploadDelete\",\"id\":\"<docId>\"} makes a device upload-and-delete a text. Neither requires the capability the owner withheld.",
      "evidence": "worker/src/v1.js:3554-3564 \u2014\n  const ctx = await authMember(request, env, { instance: instanceId }, 'manageDevices');\n  ...\n  if (cmd.type === 'assign' && !cmd.id) return j({ error: 'assign_needs_id' }, 400, origin, env);     // \u00a7F.5\n  if (!['assign', 'delete', 'changeSettings', 'triggerUpload', 'uploadDelete', 'setDone'].includes(cmd.type)) return j({ error: 'unknown_command' }, 400, origin, env);\nvalidateCaps (v1.js:497) lists 'assignTexts' as its own capability, and the neighbouring assignment routes gate on it (v1.js:3724, 3748, 3791, 3806, 3855, 3890); the command lane never consults it for the assign/delete/uploadDelete types."
    }
  ]
}
```

## ab0f470f9d88fe8bc

```json
{
  "findings": [
    {
      "title": "Member removal does not revoke key grants \u2014 the sweep keys on member_key.project_id, which is '' or stale",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 2501,
      "claim": "DELETE /v1/projects/<id>/members deletes member_key rows by project_id, but member_key.project_id is a write-time snapshot that is the empty string whenever the instance had no project_id at grant time \u2014 so the sweep matches nothing, the removed member's wrapped Ki survives, and GET /v1/researcher/keys hands it back forever.",
      "attack": "1) Owner O has migrated project P (project.drive_folder_id set). 2) O creates a device: POST /v1/instances {nickname} \u2014 the INSERT at v1.js:3471 lists no project_id column, so instance X has project_id NULL. 3) O adds member M: POST /v1/projects/P/members {researcher_id:M, caps:{manageDevices:true}} -> 200. 4) O grants M the device key: POST /v1/researcher/keys {instance_id:X, grants:[{researcher_id:O,wrapped_ki:...},{researcher_id:M,wrapped_ki:...}]}. Because X.project_id is NULL, both rows are written with project_id='' (v1.js:2300). 5) O removes M: DELETE /v1/projects/P/members {researcher_id:M} -> {ok:true, removed:1, grants_removed:0}; the approval log records '0 grant(s)'. 6) M, now a non-member, calls GET /v1/researcher/keys with no instance filter -> still returns {instance_id:X, wrapped_ki:...}, because that query (v1.js:2314) filters only on researcher_id and has no membership check at all. M keeps decrypting device X's E2EE material indefinitely. The same failure occurs without the '' case: grant M a key while X is in project P, then move X to project Q (POST /v1/researcher/projects/assign rewrites instance.project_id but nothing rewrites member_key.project_id \u2014 no UPDATE member_key exists anywhere in the file); removing M from Q now deletes nothing.",
      "evidence": "v1.js:2300 \u2014 `).bind(String(proj.project_id || ''), instanceId, String(g.researcher_id), version, String(g.wrapped_ki), r.researcher_id, now));` with its own comment at 2292-2299: \"'' means \\\"no project yet\\\" ... \u26a0 Phase C must treat '' as unassigned rather than as a project id.\"\nv1.js:2501 \u2014 `env.DB.prepare('DELETE FROM member_key WHERE project_id=? AND researcher_id=?').bind(projectId, who),` under the comment \"\u26a0 ONE BATCH, so a member can never be left listed-but-keyless or keyless-but-listed.\"\nv1.js:2314 \u2014 `: env.DB.prepare('SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=? ORDER BY instance_id, key_version DESC')` \u2014 no project or membership predicate.\nv1.js:3471 \u2014 `'INSERT INTO instance (instance_id, researcher_id, type, nickname, desired_blob, desired_rev, revoked, created_at, estate) VALUES (?,?,?,?,?,0,0,?,?)'` \u2014 project_id is never written on creation."
    },
    {
      "title": "docId-keyed Drive routes search the owner's entire estate \u2014 a capability in one project acts on every project",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 3868,
      "claim": "authMember binds only the instanceId in the URL; the docId path segment is then used in an unparented, estate-wide Drive query, so a member holding assignTexts/drive:read in one project can list, re-parent and stream texts belonging to a different project of the same owner.",
      "attack": "Member M holds drive:read in project Q (so M legitimately learns Q's docIds \u2014 the device reports M can decrypt with their member_key Ki enumerate them) and assignTexts on device D_P in project P. M calls POST /v1/instances/D_P/texts/<docId_from_Q>/adopt. authMember resolves D_P -> project P -> M has assignTexts -> ok:true. The route then searches the OWNER's whole Drive for the flextextDoc tag (no parent constraint, v1.js:3868), finds Q's text folder, and re-parents it under D_P's device folder \u2014 i.e. moves another project's text into M's project \u2014 then mints streaming URLs for its files. Read-only variant needing no assignTexts: GET /v1/instances/<any instance M can reach>/texts/<docId_from_Q>/files (v1.js:3678) returns Q's file ids, names and sizes. POST .../texts/<docId>/move (v1.js:3909) is the third instance of the same query.",
      "evidence": "v1.js:3868 \u2014 `const fq = encodeURIComponent(\\`appProperties has { key='flextextDoc' and value='${docId}' } and mimeType='application/vnd.google-apps.folder' and trashed=false\\`);` followed at 3869 by a `spaces=drive` search and at 3874 by `if (!(f.parents || []).includes(toFolder)) await driveReparent(access, f.id, toFolder, f.parents);`\nThe helper's own comment at v1.js:1134 already flags the shape: \"\u26a0 The tag search is scoped to trashed=false but NOT to the parent\".\nThe only binding done before it is `authMember(request, env, { instance: instanceId }, 'assignTexts')` (v1.js:3855) plus `SELECT ... FROM instance WHERE instance_id=? AND researcher_id=?` \u2014 nothing constrains docId to ctx.project_id, which is never read anywhere in the file."
    },
    {
      "title": "assignment/finish mints an owner-authority bearer URL for any caller-supplied Drive file id",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 3826,
      "claim": "POST /v1/instances/<id>/texts/<docId>/assignment/finish passes body.audioFileId / flextextFileId / promptFileId straight into mintTextfileUrl with the PROJECT OWNER's researcher_id and no check that the file lives in the text folder, the device folder, or the project subtree \u2014 so a member with assignTexts turns any file id in the owner's app-created estate into a credential-free 90-day download URL.",
      "attack": "Member M has assignTexts on device D_P in project P. M obtains a file id from another project Q \u2014 e.g. from the GET .../texts/<docId_Q>/files listing described in the previous finding, or from a period when M was a member of Q. M calls POST /v1/instances/D_P/texts/<any docId>/assignment/finish {flextextFileId: '<fileId from Q>', ttlDays: 400}. authMember passes (D_P is in P and M has assignTexts). The route mints a token carrying r=owner.researcher_id and f=<fileId from Q>; TTL is clamped only to 7..400 days by clampTtlDays. M then GETs the returned /v1/textfile/<token> with no headers at all: the worker mints an access token from the OWNER's stored refresh token (driveAccessToken(env, owner), v1.js:1854/1899) and streams the bytes. The v2 scope check only verifies that instance D_P is still live; it never relates the file to the instance or the project.",
      "evidence": "v1.js:3826-3828 \u2014 `const audioUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.audioFileId, '', ttlMs, scope, ctx.caller.researcher_id);` / `const flextextUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.flextextFileId, ...)` / `const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);`\nv1.js:1210 \u2014 `if (!fileId) return null;` is the ONLY validation mintTextfileUrl performs on fileId.\nThe route's preceding checks (v1.js:3810-3815) validate only the instance and that at least one id is present: `if (!body.audioFileId && !body.flextextFileId && !body.promptFileId) return j({ error: 'nothing_to_mint' }, 400, origin, env);`\nThe adjacent removed-route comment at v1.js:3934 states the intended rule this violates: \"Do not reintroduce a route that fetches a URL the caller chose.\""
    },
    {
      "title": "assignment/upload/start writes into any caller-supplied parent folder in the owner's Drive",
      "severity": "medium",
      "file": "worker/src/v1.js",
      "line": 3765,
      "claim": "body.originalsFolderId is used verbatim as the Drive `parents` for a resumable upload created with the project owner's token, with no check that the folder is inside the text, the device, or the project \u2014 so a member with assignTexts can plant files anywhere in the owner's estate, including another project's folders and the account master folder.",
      "attack": "Member M has assignTexts on device D_P in project P. M learns a folder id outside P \u2014 e.g. the account master 'FlexText Uploads' id or another project's folder id, both returned by GET .../texts/<docId>/files as `folderId`/`originalsFolderId`, or by adopt's `folderId` response. M calls POST /v1/instances/D_P/texts/<docId>/assignment/upload/start {kind:'flextext', name:'consent.flextext', size:N, originalsFolderId:'<foreign folder id>'} -> 200 with an uploadId, then PUTs the bytes to .../assignment/upload/chunk. The file lands in the foreign folder tagged flextextRole:'source-flextext', where the owner's panel and the other project's device will treat it as legitimate source material.",
      "evidence": "v1.js:3765-3771 \u2014 `let parent = String(body.originalsFolderId || '');` ... `if (!parent) return j({ error: 'bad_folder' }, 400, origin, env);` then `body: JSON.stringify({ name, mimeType: mime, parents: [parent], appProperties: { flextextRole: role } }),` with `Authorization: 'Bearer ' + access` where `access = await driveAccessToken(env, r)` and `const r = ctx.owner;`. The only prior binding is `authMember(request, env, { instance: instanceId }, 'assignTexts')` at v1.js:3748 plus an instance-ownership SELECT; `parent` is never compared to the device folder, the text folder, or the project subtree."
    },
    {
      "title": "POST /v1/researcher/keys decides authorization itself and answers 403, making instance ids enumerable",
      "severity": "low",
      "file": "worker/src/v1.js",
      "line": 2280,
      "claim": "This route is project data but bypasses authMember (a second authorization authority, against I1) and distinguishes 'exists but not yours' (403) from 'does not exist' (404), so any authenticated researcher can probe whether an arbitrary instance GUID exists.",
      "attack": "Researcher A (any approved account, member of nothing) POSTs /v1/researcher/keys {instance_id:'<guess>', grants:[{researcher_id:'x', wrapped_ki:'y'}]}. A non-existent id returns 404 not_found (v1.js:2273); an id belonging to another researcher returns 403 forbidden (v1.js:2280). The two responses are distinguishable, which is exactly the oracle the design forbids and which check-project-scoping.sh tests for \u2014 but that script only inspects lines matching `if (!ctx.ok`, so this route, having no authMember call, is invisible to it. The route also omits `AND revoked=0` on the instance lookup and skips the not_migrated gate that POST /v1/projects/<id>/members enforces.",
      "evidence": "v1.js:2271-2280 \u2014 `const inst = await env.DB.prepare('SELECT instance_id, project_id, researcher_id FROM instance WHERE instance_id=?').bind(instanceId).first();` / `if (!inst) return j({ error: 'not_found' }, 404, origin, env);` ... `if (r.researcher_id !== proj.owner_id) return j({ error: 'forbidden' }, 403, origin, env);`\nContrast v1.js:2371 in the sibling DELETE route, which was converted: `if (!ctx.ok || !ctx.isOwner) return j({ error: 'not_found' }, 404, origin, env);`\ncheck-project-scoping.sh: `if grep 'if (!ctx.ok' \"$W\" | grep -q '}, 403,'` \u2014 the guard only sees authMember call sites."
    }
  ]
}
```

## ab54e8b2130932bb0

```json
{
  "findings": [
    {
      "title": "Removing a member does not remove their key grants when member_key.project_id is '' (or stale)",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 2501,
      "claim": "DELETE /v1/projects/<id>/members deletes member_key rows scoped by project_id, but member_key.project_id is a snapshot written at grant time \u2014 it is '' for any instance whose project_id was still NULL (every freshly created device) and stale for any instance that later moved projects \u2014 so those grants survive removal and GET /v1/researcher/keys keeps serving the wrapped Ki to the removed member, while the response reports grants_removed:0.",
      "attack": "1. Owner O owns migrated project P (project.drive_folder_id set). O creates a device: POST /v1/instances {nickname:\"Phone\"} -> instance D. The INSERT at v1.js:3471 has no project_id column, so instance.project_id is NULL until a later panel estate load stamps it (reconcileProjects only stamps devices whose Drive folder already sits under a project folder, so a device with no uploads stays NULL indefinitely).\n2. O adds member M: POST /v1/projects/P/members {researcher_id:M, caps:{\"manageDevices\":true}} -> 200.\n3. O shares D's key: POST /v1/researcher/keys {instance_id:D, grants:[{researcher_id:O,wrapped_ki:...},{researcher_id:M,wrapped_ki:...}]}. Because inst.project_id is NULL, the dual-read fallback at v1.js:2278 yields proj.project_id=null and the row is stored with project_id='' (v1.js:2300).\n4. O removes M: DELETE /v1/projects/P/members {researcher_id:M}. The batch deletes member_key WHERE project_id='P' -> matches nothing. Response: {ok:true, removed:1, grants_removed:0}.\n5. M calls GET /v1/researcher/keys (v1.js:2310-2318 \u2014 no membership check at all, keyed only on researcher_id) and still receives {instance_id:D, wrapped_ki} and can unwrap it with their researcher private key.\nSame outcome without the NULL window: grant M a key for device D while D is in project A (row carries project_id=A), then move D to project B (POST /v1/projects/assign, v1.js:3238, or reconcileProjects' correction at v1.js:1814), then remove M from B \u2014 the delete scoped by B never touches the row stamped A.",
      "evidence": "v1.js:2499-2506 \u2014 `const res = await env.DB.batch([\\n  env.DB.prepare('DELETE FROM project_member WHERE project_id=? AND researcher_id=?').bind(projectId, who),\\n  env.DB.prepare('DELETE FROM member_key WHERE project_id=? AND researcher_id=?').bind(projectId, who),\\n]);` \u2026 `return j({ ok: true, removed: gone, grants_removed: keys }, \u2026)`\nv1.js:2300 \u2014 `).bind(String(proj.project_id || ''), instanceId, String(g.researcher_id), version, String(g.wrapped_ki), r.researcher_id, now));` with the comment above it: \"`|| ''` IS LOAD-BEARING \u2026 '' means \\\"no project yet\\\"\".\nv1.js:2312-2315 \u2014 `SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=? \u2026` (no project or membership predicate).\nworker/schema-current.sql:107-116 \u2014 `PRIMARY KEY (instance_id, researcher_id, key_version)`; project_id is not part of the key, i.e. it is a label, not identity.\nThe route's own comment (v1.js:2424-2427) claims the opposite: \"REMOVING A MEMBER ALSO DELETES THEIR KEY GRANTS, in the same batch. Without that, 'removed' would mean 'no longer listed' while they still hold every wrapped Ki\"."
    },
    {
      "title": "DELETE /v1/researcher/keys cannot revoke a grant once the instance is revoked",
      "severity": "medium",
      "file": "worker/src/v1.js",
      "line": 2369,
      "claim": "The new per-grant revoke route resolves authorization through authMember({instance}), whose instance lookup filters `revoked=0`, so after the owner revokes a device the route answers not_found and the owner can no longer withdraw any grant on it \u2014 while GET /v1/researcher/keys keeps handing the wrapped Ki to the grantee, and instance revocation itself deletes no member_key rows.",
      "attack": "1. Owner O grants member M a key on device D: POST /v1/researcher/keys {instance_id:D, grants:[{O,\u2026},{M,\u2026}]} -> stored.\n2. The device is lost, so O revokes it: POST /v1/instances/D/revoke -> instance.revoked=1 (v1.js:3966-3968; the batch touches instance and install only \u2014 no member_key delete).\n3. O tries to withdraw M's key: DELETE /v1/researcher/keys {instance_id:D, researcher_id:M}. authMember's instance branch runs `SELECT project_id, researcher_id FROM instance WHERE instance_id=? AND revoked=0` (v1.js:555), finds no row, so addressedRow stays false, project_id stays '' and the helper returns {ok:false}; the route answers 404 not_found (v1.js:2371). There is no other per-grant delete path.\n4. M calls GET /v1/researcher/keys?instance=D and is still served the wrapped Ki. (The members DELETE is not a substitute \u2014 it only removes rows whose stored project_id equals the project being managed; see the other finding.)",
      "evidence": "v1.js:2369-2371 \u2014 `const ctx = await authMember(request, env, { instance: instanceId }, null);\\n    if (!ctx) return j({ error: 'unauthorized' }, 401, origin, env);\\n    if (!ctx.ok || !ctx.isOwner) return j({ error: 'not_found' }, 404, origin, env);`\nv1.js:555 \u2014 `const row = await env.DB.prepare('SELECT project_id, researcher_id FROM instance WHERE instance_id=? AND revoked=0')` \u2014 a revoked instance never resolves, and v1.js:594-598 then denies (`if (!project_id) { \u2026 return deny; }`).\nv1.js:3965-3969 \u2014 revoke writes only `UPDATE instance SET revoked=1 \u2026` and `UPDATE install SET revoked=1 \u2026`; no member_key cleanup.\nv1.js:2312-2315 \u2014 GET /v1/researcher/keys serves rows by researcher_id with no instance-state or membership check."
    },
    {
      "title": "A member with assignTexts can mint a /v1/textfile URL for any Drive file id in the owner's account",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 3921,
      "claim": "In the converted texts/move and texts/adopt routes the file ids that get sealed into a /v1/textfile bearer token come straight from the request body and are never checked against the doc, the device or ctx.project_id \u2014 the token is minted with the OWNER's researcher_id, so any member holding assignTexts in one project can obtain the bytes of any app-created file in the owner's whole Drive, including other projects and the account master folder.",
      "attack": "1. M is a member of project A with caps {\"assignTexts\":true}; A contains devices D1 and D2 (ids a member legitimately knows).\n2. M knows a Drive file id F that belongs to project B of the same owner O (e.g. from an earlier membership, a forwarded link, or a docId listed through GET /v1/instances/<D1>/texts/<docId>/files, whose Drive query at v1.js:3677 is account-wide with no parent constraint).\n3. M calls POST /v1/instances/D1/texts/<anyDocId>/move with body {to:\"D2\", flextextFileId:\"F\"}. authMember resolves D1 -> project A and passes assignTexts; `from`/`to` are validated only by `researcher_id=?` bound to ctx.owner (v1.js:3899-3903), i.e. the owner's entire estate, and nothing validates F at all.\n4. The response contains flextextUrl = <origin>/v1/textfile/<token> where the token is {r:O, f:F, m:M, i:D2}.\n5. M fetches that URL. The Phase C grant recheck at v1.js:1886-1889 only asks whether M is still a member of the project D2 belongs to \u2014 which is project A \u2014 so it passes, and the worker streams F using the owner's Drive access token (v1.js:1904-1907). Same primitive via audioFileId and via the /adopt route at v1.js:3881.",
      "evidence": "v1.js:3921 \u2014 `const mint = (fileId, extract) => mintTextfileUrl(env, url.origin, r.researcher_id, fileId, extract, 0, { instanceId: to.instance_id, docId }, ctx.caller.researcher_id);`\nv1.js:3922-3923 \u2014 `const flextextUrl = await mint(body.flextextFileId) || await mint(body.extractFromZipId, 'flextext');\\n        const audioUrl = await mint(body.audioFileId);` \u2014 caller-supplied ids, unvalidated.\nv1.js:3899-3903 \u2014 `const from = await env.DB.prepare('SELECT instance_id, nickname, oauth_folder_id FROM instance WHERE instance_id=? AND researcher_id=? AND revoked=0')\u2026` / same for `to` \u2014 scoped to the OWNER's account, never to ctx.project_id.\nv1.js:1886-1889 \u2014 the redemption check is `SELECT 1 AS ok FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?`, i.e. it validates the DESTINATION device's project, not the file's."
    }
  ]
}
```

## a114ed70f6e797497

```json
{
  "findings": [
    {
      "title": "Removing a member does not revoke Ki grants minted while the instance was in the dual-read window (member_key.project_id = '')",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 2300,
      "claim": "POST /v1/researcher/keys stamps member_key.project_id with the sentinel '' whenever instance.project_id is NULL (the dual-read/legacy state), but DELETE /v1/projects/<id>/members deletes grants by project_id \u2014 so those rows survive member removal, and GET /v1/researcher/keys, which is scoped only by researcher_id, keeps handing the removed member the wrapped Ki.",
      "attack": "1. Owner O owns migrated project P (project.drive_folder_id set). O adds member M: POST /v1/projects/P/members {\"researcher_id\":\"M\",\"caps\":{\"drive\":\"read\"}} -> ok.\n2. O creates a device: POST /v1/instances {\"nickname\":\"Village phone\"} -> instance I. The INSERT at v1.js:3471 does not include project_id, so instance.project_id is NULL.\n3. O grants M the device key: POST /v1/researcher/keys {\"instance_id\":\"I\",\"grants\":[{\"researcher_id\":\"O\",\"wrapped_ki\":\"...\"},{\"researcher_id\":\"M\",\"wrapped_ki\":\"...\"}]}. Because inst.project_id is NULL, line 2279 yields proj.project_id = null and line 2300 binds String(null || '') = '' -> both member_key rows are written with project_id ''.\n4. Trust breaks; O removes M: DELETE /v1/projects/P/members {\"researcher_id\":\"M\"}. Line 2501 runs DELETE FROM member_key WHERE project_id='P' AND researcher_id='M' -> 0 rows. The response is {ok:true, removed:1, grants_removed:0} \u2014 i.e. it reports success.\n5. M, now a non-member, calls GET /v1/researcher/keys with their own credentials. Line 2314 selects member_key by researcher_id only, so M is still handed wrapped_ki for instance I, unwraps it with their researcher private key, and can decrypt that device's E2EE material for as long as the row exists. Because of the companion finding (instances are created with project_id NULL and are usually never stamped), this is the DEFAULT outcome for devices created after Phase C ships, not an edge case.",
      "evidence": "worker/src/v1.js:2277-2279 \u2014 `const proj = inst.project_id ? await env.DB.prepare('SELECT project_id, owner_id FROM project WHERE project_id=?')... : { project_id: null, owner_id: inst.researcher_id };`\nworker/src/v1.js:2300 \u2014 `).bind(String(proj.project_id || ''), instanceId, String(g.researcher_id), version, String(g.wrapped_ki), r.researcher_id, now));`  (comment at 2292-2299: \"'' means \\\"no project yet\\\"\u2026 \u26a0 Phase C must treat '' as unassigned rather than as a project id\")\nworker/src/v1.js:2501 \u2014 `env.DB.prepare('DELETE FROM member_key WHERE project_id=? AND researcher_id=?').bind(projectId, who),` under the comment \"REMOVING A MEMBER ALSO DELETES THEIR KEY GRANTS, in the same batch. Without that, \\\"removed\\\" would mean \\\"no longer listed\\\" while they still hold every wrapped Ki\"\nworker/src/v1.js:2313-2315 \u2014 `'SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=? ...'` \u2014 no membership check on the read path, so deletion is the only control.\nworker/migrate-projects.sql:53 \u2014 `PRIMARY KEY (instance_id, researcher_id, key_version)` \u2014 project_id is not part of the row's identity, so nothing else repairs the sentinel."
    },
    {
      "title": "The dual-read/legacy branch never closes: POST /v1/instances creates every instance with project_id NULL and nothing reliably stamps it",
      "severity": "medium",
      "file": "worker/src/v1.js",
      "line": 3471,
      "claim": "authMember's legacy branch is justified by the claim that it is a shrinking migration window (\"It closes on its own: every lazy mint removes rows from it, and it is unreachable once none are left\"), but instance creation never writes project_id, the lazy backfill only fires for researchers who have NO project row at all, and the lazy device-folder path parents new folders under the account master \u2014 so a device created after Phase C can stay in the legacy branch permanently, which both breaks project-scoped member access for it and stamps every grant it receives with the '' sentinel.",
      "attack": "1. Owner O already has a project row (true for every researcher after the Phase B backfill), so the lazy mint at v1.js:2628-2630 (`if (!mine) await backfillProjectsFor(...)`) never runs again for O.\n2. O creates a device from a flat estate or the stray tab, so the panel sends no projectFolderId: POST /v1/instances {\"nickname\":\"phone\"} -> the INSERT at 3471 omits project_id; instance.project_id is NULL.\n3. The device's folder is created lazily on its first upload: driveEnsureDeviceFolder is called with no projectFolderId and no existing id (v1.js:4130 / 4231), so the parent chain at 1028-1029 resolves to `await driveMasterFolder(access)` \u2014 the folder is NOT inside any project folder. (driveProjectFolderFor is not even consulted on the device path, and the appProperty it searches, `flextextProject`, is never written anywhere in the file.)\n4. buildDriveEstate therefore reports projectId '' for that device, and reconcileProjects skips it at 1804 (`if (!d.projectId || !d.folderId) continue;`). Repeat GET /v1/researcher/drive-estate as often as you like: instance.project_id stays NULL forever.\n5. Consequences, both testable: (a) member M of O's project with manageDevices gets 404 not_found from POST /v1/instances/I/rename, because authMember falls to the legacy branch which consults project_member not at all \u2014 access is project-scoped in name only for new devices; (b) every POST /v1/researcher/keys for I writes member_key.project_id = '' (see the companion finding), so grants for it are unrevokable by member removal. The branch that the audit note calls transient and self-closing is the live authorization path for every device created from now on.",
      "evidence": "worker/src/v1.js:3470-3472 \u2014 `'INSERT INTO instance (instance_id, researcher_id, type, nickname, desired_blob, desired_rev, revoked, created_at, estate) VALUES (?,?,?,?,?,0,0,?,?)'` \u2014 no project_id column, and the route's own comment at 3480-3484 still asserts \"Eager creation also makes Drive PARENTAGE the record from birth\u2026 no `project_id` written anywhere, nothing to drift.\"\nworker/src/v1.js:588-591 \u2014 `if (!project_id) { if (addressedRow && legacyOwner && legacyOwner === caller.researcher_id) { return { ok: true, caller, owner: caller, project_id: '', caps: {}, isOwner: true, legacy: true }; }` with the comment at 585-587: \"It closes on its own: every lazy mint removes rows from it, and it is unreachable once none are left.\"\nworker/src/v1.js:2628-2630 \u2014 `const mine = await env.DB.prepare('SELECT project_id FROM project WHERE owner_id=? LIMIT 1')\u2026 if (!mine) await backfillProjectsFor(env, r, now);` \u2014 the only lazy adoption, gated on the researcher having no project at all.\nworker/src/v1.js:1028-1029 \u2014 `const parent = projectFolderId || (await drivePriorProjectParent(access, existingId)) || await driveMasterFolder(access);`\nworker/src/v1.js:1803-1804 \u2014 `for (const d of (estate.devices || [])) { if (!d.projectId || !d.folderId) continue;`\nworker/src/v1.js:4491-4493 (same defect for crowd, stated outright by the code) \u2014 \"driveEnsureCrowdFolder resolves its parent from `rec.project_id`, which is always NULL\"."
    }
  ]
}
```

## a2c3cdb4fd49b1cd1

```json
{
  "findings": [
    {
      "title": "Textfile tokens are minted for a caller-chosen Drive file id that is never checked against the caller's project (cross-project read)",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 3826,
      "claim": "POST /v1/instances/<id>/texts/<docId>/assignment/finish mints a /v1/textfile token whose `f` is taken verbatim from the request body, with no check that the file lies inside the caller's project \u2014 and redemption re-checks only the owner, the instance and the minter's membership, never the file \u2014 so a member of one project can have the owner's Drive token stream any app-created file in the owner's account, including files in another project and under the account master folder.",
      "attack": "Owner O has projects A and B. M is a member of A with assignTexts (and drive:read). 1) M calls GET /v1/instances/<instance in A>/texts/<docId belonging to a text in project B>/files \u2014 that route resolves the folder with a whole-Drive tag search (`spaces=drive`, line 3678: `appProperties has { key='flextextDoc' and value='${docId}' }`) with no parent/project constraint, and returns the ids of every file in project B's text folder. (Any file id M knew from a project they were removed from works equally well; nothing else is needed.) 2) M calls POST /v1/instances/<instance in A>/texts/<any docId>/assignment/finish with {\"promptFileId\":\"<project-B file id>\",\"ttlDays\":400}. authMember passes: the target instance really is in project A and M really has assignTexts there. 3) The response contains promptUrl. GET that URL: tk.r is O, so driveAccessToken(env, owner) fetches https://www.googleapis.com/drive/v3/files/<project-B file id>?alt=media and streams project B's audio/flextext back to M. Using promptFileId rather than audioFileId is the sharp variant: line 3828 passes `null` for scope, so the token carries no `i`, is not killed by revoking any device, and lives the full clamped TTL (up to 400 days via clampTtlDays).",
      "evidence": "worker/src/v1.js:3826-3828 \u2014 `const audioUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.audioFileId, '', ttlMs, scope, ctx.caller.researcher_id);` / `const flextextUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.flextextFileId, '', ttlMs, scope, ctx.caller.researcher_id);` / `const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);` \u2014 the only prior validation is worker/src/v1.js:3815 `if (!body.audioFileId && !body.flextextFileId && !body.promptFileId) return j({ error: 'nothing_to_mint' }, 400, origin, env);`. mintTextfileUrl (worker/src/v1.js:1209-1215) copies it straight in: `if (!fileId) return null;` \u2026 `const tk = { r: researcherId, f: fileId, x: extract || '', \u2026}`. At redemption the route resolves the owner, the scoped instance and the minter's membership but never the file: worker/src/v1.js:1904 `const g = await fetch('https://www.googleapis.com/drive/v3/files/' + encodeURIComponent(tk.f) + '?alt=media', { headers: { Authorization: 'Bearer ' + access, \u2026 } });`. The id-discovery step is member-reachable and equally unscoped: worker/src/v1.js:3663 `const ctx = await authMember(request, env, { instance: instanceId }, 'drive:read');` then worker/src/v1.js:3678-3679 `const fq = encodeURIComponent(\\`appProperties has { key='flextextDoc' and value='${docId}' } \u2026\\`); const found = await driveJson(access, 'GET', 'https://www.googleapis.com/drive/v3/files?spaces=drive&orderBy=createdTime&fields=files(id)&q=' + fq);`"
    },
    {
      "title": "The fallback minter check asks \"a member of ANY project of this owner\", so removing a member from the project they minted in does not close their tokens",
      "severity": "medium",
      "file": "worker/src/v1.js",
      "line": 1894,
      "claim": "For a member-minted token with no scoped instance (`m` present, `i` absent \u2014 which is exactly what /assignment/finish mints for promptFileId, line 3828), redemption asks only whether the minter is still a member of some project owned by tk.r. A member of two of the same owner's projects therefore keeps a live streaming URL after being removed from the project the URL was minted in, contradicting the comment's claim that \"the coarse one still closes on removal\".",
      "attack": "Owner O has projects A and B; M is a member of both. M calls POST /v1/instances/<instance in A>/texts/<docId>/assignment/finish with a promptFileId, receiving an unscoped token carrying m=M, r=O, e=now+400d. O then removes M from project A entirely (DELETE /v1/projects/A/members), believing that withdrew M's access. M re-fetches the saved promptUrl: tk.i is absent so the fallback query runs, finds M's surviving project_member row for project B (p.owner_id is still O), returns ok, and the file keeps streaming for the remainder of the TTL. The same holds if the owner deletes project A outright while M remains in B.",
      "evidence": "worker/src/v1.js:1882-1897 \u2014 `if (tk.m && tk.m !== tk.r) { \u2026 ok2 = tk.i ? \u2026  : await env.DB.prepare('SELECT 1 AS ok FROM project_member pm JOIN project p ON p.project_id=pm.project_id ' + 'WHERE pm.researcher_id=? AND p.owner_id=?').bind(tk.m, tk.r).first(); \u2026 if (!ok2) return j({ error: 'gone' }, 410, origin, env); }` \u2014 the bind pair is (minter, OWNER), never (minter, the project the token was minted in); the token carries no project id to bind against. The unscoped member-minted token that lands here is worker/src/v1.js:3828 `const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);`"
    },
    {
      "title": "The minter check never re-checks the capability the mint required, so a capability downgrade leaves the member's tokens serving",
      "severity": "low",
      "file": "worker/src/v1.js",
      "line": 1887,
      "claim": "Minting requires the assignTexts capability (authMember(..., 'assignTexts')), but redemption re-checks bare membership only \u2014 no caps are read \u2014 so an owner who strips a member's capabilities without removing the membership row leaves every URL that member minted live for the full TTL.",
      "attack": "Owner O adds M to project A with caps {assignTexts:true, drive:'read'}. M mints assignment URLs (up to 400 days). O decides M should no longer assign or read and re-POSTs /v1/projects/A/members with {researcher_id:M, caps:{}} \u2014 the handler is `INSERT OR REPLACE INTO project_member \u2026` (worker/src/v1.js:2488), so the row survives with empty caps and every subsequent authMember('assignTexts'/'drive:read') call denies M. M re-fetches the saved audioUrl/flextextUrl: the scoped query below matches on the surviving membership row alone and the file streams.",
      "evidence": "worker/src/v1.js:1886-1889 \u2014 `ok2 = tk.i ? await env.DB.prepare('SELECT 1 AS ok FROM project_member pm JOIN instance i ON i.project_id=pm.project_id ' + 'WHERE pm.researcher_id=? AND i.instance_id=?').bind(tk.m, tk.i).first()` \u2014 it selects a constant, not `pm.caps`, and nothing downstream parses caps. Compare the mint site's requirement, worker/src/v1.js:3806: `const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');`"
    }
  ]
}
```

## a3098e0f044f36baa

```json
{
  "findings": [
    {
      "title": "Converted member routes resolve a text by caller-supplied docId with an account-wide Drive search, so a member of one project can read and physically extract another project's texts",
      "severity": "critical",
      "file": "worker/src/v1.js",
      "line": 3679,
      "claim": "The five Drive-touching routes that were converted to authMember (texts/<docId>/files, assignment/begin, assignment/finish, adopt, move) bind authorization to the instanceId in the PATH but then resolve the actual Drive object from a caller-supplied docId via a `spaces=drive` appProperties tag search across the OWNER's entire Drive. Nothing between authMember and the Drive call compares the found folder's parents to the project authMember resolved, and `project.drive_folder_id` \u2014 the field the members-POST migration gate exists to guarantee \u2014 is read by no route a member can reach.",
      "attack": "Owner O owns project A and project B. M is a project_member of A with {\"assignTexts\":true,\"drive\":\"read\"} and is NOT in B (or was removed from B and still has its docIds, which are plaintext in the panel's move/pending maps and survive removal). 1) M sends GET /v1/instances/<A-instance-id>/texts/<B-docId>/files. authMember({instance:A-instance},'drive:read') resolves project A \u2192 ok; the `owned` re-check only asks whether the A instance still belongs to the owner. The tag query then runs over the whole account and returns project B's text folderId, originalsFolderId, and every child file's id/name/size/mime/role. 2) M then sends POST /v1/instances/<A-instance-id>/texts/<B-docId>/adopt with body {}. The same account-wide query finds B's text folder and `driveReparent(access, f.id, toFolder, f.parents)` moves it under a device folder inside project A, clearing flextextUnassigned so no sweep returns it. B's recordings, flextexts and consent files are now physically inside A. POST .../texts/<B-docId>/move {to:<second A instance>} does the same. Note the same crossing happens with no attacker foresight at all whenever the owner moves a device between projects with POST /v1/researcher/projects/assign: docIds M legitimately learned while the device was in A keep resolving after it becomes B's.",
      "evidence": "worker/src/v1.js:3663-3680 (files) \u2014 `const ctx = await authMember(request, env, { instance: instanceId }, 'drive:read');` \u2026 `const docId = String(seg[4] || '').replace(/[^\\w-]/g, '').slice(0, 64);` \u2026 `const fq = encodeURIComponent(\\`appProperties has { key='flextextDoc' and value='${docId}' } and mimeType='application/vnd.google-apps.folder' and trashed=false\\`);` / `const found = await driveJson(access, 'GET', 'https://www.googleapis.com/drive/v3/files?spaces=drive&orderBy=createdTime&fields=files(id)&q=' + fq);` \u2014 with `const r = ctx.owner;` at 3666 and `const access = await driveAccessToken(env, r);` at 3677, i.e. the OWNER's token. Identical unscoped queries at 3868-3870 (adopt, followed by `if (!(f.parents || []).includes(toFolder)) await driveReparent(access, f.id, toFolder, f.parents);`) and 3909-3910 (move). driveEnsureTextFolder states the property outright at v1.js:1134-1136: \u201cThe tag search is scoped to trashed=false but NOT to the parent\u201d. validateCaps' own comment (v1.js:474-478) already knows this \u2014 \u201ca member restricted to one device could still open another's files through any docId-routed Drive route \u2026 The honest boundary is the one Drive can actually enforce, and that is the project\u201d \u2014 but no route enforces the project either: grep shows `drive_folder_id` read only at 1679/1700/1760/2400/2473/3234, never in the /v1/instances block."
    },
    {
      "title": "assignment/finish, move and adopt mint owner-authority streaming URLs for any caller-supplied Drive file id, and redemption never checks the file's project",
      "severity": "critical",
      "file": "worker/src/v1.js",
      "line": 3828,
      "claim": "mintTextfileUrl validates nothing about the file (`if (!fileId) return null;` then straight into the token), the call sites pass `r.researcher_id` where `r = ctx.owner`, and /v1/textfile's member-grant check asks only whether the minter still belongs to the SCOPED instance's project \u2014 never whether tk.f is inside that project. assignTexts in one project therefore yields read access to every app-created file in the owner's whole Drive, across all their projects.",
      "attack": "M is a project_member of project A with {\"assignTexts\":true}. M obtains a file id from outside project A \u2014 e.g. straight out of the cross-project listing above, or cached from a project they were removed from. M sends POST /v1/instances/<A-instance-id>/texts/<any-docId>/assignment/finish with {\"promptFileId\":\"<foreign-file-id>\",\"ttlDays\":400}. authMember passes on project A; promptUrl is minted with scope = null, so the token is v1-shaped with tk.m = M, tk.r = owner, and a 400-day life (clampTtlDays caps at 400, v1.js:1186-1190). The response hands M the URL. GET /v1/textfile/<token> takes the `tk.m && !tk.i` branch, whose query is 'is M a member of ANY project this owner owns' \u2014 true because of project A \u2014 and streams the file with the owner's Drive token to a header-less fetch. Because the prompt token is deliberately unscoped, it also survives revocation of every device in project A. audioFileId/flextextFileId on finish/move/adopt work the same way with a project-A instance in tk.i, which the membership query then happily confirms.",
      "evidence": "worker/src/v1.js:3826-3828 \u2014 `const audioUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.audioFileId, '', ttlMs, scope, ctx.caller.researcher_id);` / `\u2026body.flextextFileId\u2026` / `const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);` with `const r = ctx.owner;` at 3811. mintTextfileUrl, v1.js:1209-1213: `if (!fileId) return null; const tk = { r: researcherId, f: fileId, x: extract || '', e: \u2026 };` \u2014 no Drive lookup, no parentage check. Redemption, v1.js:1885-1896: `ok2 = tk.i ? \u2026'FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?' : \u2026'FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?'` \u2014 neither query mentions tk.f. The streaming call at 1904 is `fetch('\u2026/files/' + encodeURIComponent(tk.f) + '?alt=media', { headers: { Authorization: 'Bearer ' + access } })` where access came from `driveAccessToken(env, owner)`. Same unvalidated mint at 3881 (adopt) and 3921 (move)."
    },
    {
      "title": "assignment/upload/start writes into an unvalidated caller-supplied Drive parent, letting a member plant files in another project or at the account root",
      "severity": "high",
      "file": "worker/src/v1.js",
      "line": 3765,
      "claim": "`parent` is taken verbatim from body.originalsFolderId with no files.get, no parentage check and no relation to the instance, text or project authMember resolved, and the resumable session is opened with the OWNER's token \u2014 so a member with assignTexts writes anywhere in the owner's app-created estate. The sibling begin route has the same hole one level up: driveEnsureTextFolder trusts body.folderId as long as it merely exists and is untrashed.",
      "attack": "M is a project_member of project A with {\"assignTexts\":true}. 1) M learns a foreign folder id F \u2014 from GET /v1/instances/<A-instance>/texts/<B-docId>/files, which returns project B's `folderId` and `originalsFolderId`, or from adopt's `folderId`. 2) M sends POST /v1/instances/<A-instance-id>/texts/<any-docId>/assignment/upload/start {\"name\":\"consent.flextext\",\"mime\":\"application/xml\",\"size\":1024,\"kind\":\"flextext\",\"originalsFolderId\":\"F\"} \u2192 200 with an uploadId. 3) M PUTs the bytes to .../assignment/upload/chunk (the only check there is `sess.rr !== r.researcher_id`, and rr is the owner, so it passes). The file lands inside project B tagged flextextRole:'source-flextext', where B's panel and B's device treat it as legitimate source material. The same call with F = the master 'FlexText Uploads' folder id writes outside every project. Variant on begin: POST .../texts/<any-docId>/assignment/begin {\"folderId\":\"<B-text-folder-id>\"} returns that foreign folder and creates an `originals` child inside it.",
      "evidence": "worker/src/v1.js:3764-3777 \u2014 `const access = await driveAccessToken(env, r);` (r = ctx.owner, 3751) / `let parent = String(body.originalsFolderId || '');` / `if (body.kind === 'consent-prompt') { parent = await driveEnsureDeviceFolder(...); }` / `if (!parent) return j({ error: 'bad_folder' }, 400, origin, env);` / `body: JSON.stringify({ name, mimeType: mime, parents: [parent], appProperties: { flextextRole: role } }),`. The only prior binding is `authMember(request, env, { instance: instanceId }, 'assignTexts')` at 3748 plus an instance-ownership SELECT; `parent` is never compared to the device folder, the text folder, or project.drive_folder_id. begin, v1.js:3737: `const folder = await driveEnsureTextFolder(access, deviceFolder, docId, body.title, body.folderId);` and driveEnsureTextFolder's knownId path at 1147-1153 returns any id for which `files.get` succeeds and `!f.trashed`."
    },
    {
      "title": "A member action can create a project's device folder directly under the account master, putting project data outside every project subtree",
      "severity": "medium",
      "file": "worker/src/v1.js",
      "line": 1029,
      "claim": "Every member-reachable call to driveEnsureDeviceFolder omits the projectFolderId argument, so when the instance has no live oauth_folder_id the parent falls through to `driveMasterFolder(access)` \u2014 the account root of the FlexText estate. A member with assignTexts therefore creates a container, its text folder and its uploaded assignment files at the account master level, outside the project folder the members-POST gate insists must exist before sharing is allowed.",
      "attack": "Instance D belongs to project A (project_id stamped by the backfill) but has oauth_folder_id NULL \u2014 a device enrolled and never uploaded, which is exactly the device assign-by-upload targets. M, a member of A with {\"assignTexts\":true}, sends POST /v1/instances/<D>/texts/<docId>/assignment/begin {\"title\":\"Story 1\"}. driveEnsureDeviceFolder(env, access, D, nickname, null) runs with projectFolderId undefined; drivePriorProjectParent('') returns ''; so parent = driveMasterFolder(access) and the device folder is created directly under 'FlexText Uploads'. begin then creates the text folder and originals/ beneath it and M uploads the assignment there. The bytes now sit outside project A's Drive folder, so any listing rooted at project.drive_folder_id (the confinement the 409 not_migrated gate was added to guarantee) cannot see them, while reconcileProjects leaves the row alone because a container under master has projectId '' (v1.js:1801-1806: `if (!d.projectId || !d.folderId) continue;`). The same fallback fires on adopt (3867) and move (3905) whenever a device folder has been trashed and its old parent is gone.",
      "evidence": "worker/src/v1.js:1028-1029 \u2014 `const parent = projectFolderId || (await drivePriorProjectParent(access, existingId))` / `|| await driveMasterFolder(access);`. Member-reachable call sites all pass five arguments, never the sixth: 3736 `driveEnsureDeviceFolder(env, access, instanceId, inst.nickname, inst.oauth_folder_id)` (begin), 3767 (upload/start, consent-prompt), 3867 (adopt), 3905 (move). Contrast POST /v1/instances at 3492, which does pass a project: `driveEnsureDeviceFolder(env, access, instance_id, nickname, '', wantProject)`. The members gate at 2473-2482 states the rule this breaks: \u201ca member's file listing has to be ROOTED at that folder rather than walked from the account master\u201d."
    }
  ]
}
```

## a1da61f5b972ea650

```json
{
  "refuted": false,
  "reasoning": "Confirmed by direct trace. worker/src/v1.js:3855-3888 (adopt) and 3903-3920 (move) call authMember(request, env, { instance: instanceId }, 'assignTexts'), which resolves the project SOLELY from the instance row (v1.js:548-556) and never sees the docId. The routes then take the caller-supplied docId from the path, sanitize it, and run an unscoped Drive query with the OWNER's token \u2014 `files?spaces=drive&orderBy=createdTime&fields=files(id,parents)&q=appProperties has { key='flextextDoc' and value='<docId>' } ...` \u2014 and reparent whatever it finds: `if (!(f.parents || []).includes(toFolder)) await driveReparent(access, f.id, toFolder, f.parents);`, then clears the flextextUnassigned tag. No step between authMember and driveReparent compares the found folder's parents to the resolved project's drive_folder_id; `grep drive_folder_id` shows it is read only at 1679/1700/1706/1760-1794/2400/2473/3234/3271, never inside the /v1/instances block. driveReparent (v1.js:924) validates nothing. The destination instance lookup binds `researcher_id = r.researcher_id` where r = ctx.owner, i.e. the owner, not ctx.project_id \u2014 so any instance of the same owner is a legal destination, which is exactly what makes two projects of one owner mutually reachable. Routing is reachable as claimed (instanceId = seg[2], sub = seg[3], v1.js:3501-3502; no pre-guard on the block). The same unscoped query appears a third time at 3678 in GET .../texts/<docId>/files behind only 'drive:read', giving a read-only cross-project variant, and driveEnsureTextFolder's header (v1.js:1134-1136) states the property outright: \"The tag search is scoped to trashed=false but NOT to the parent\". Refutation attempts that failed: (a) no guard exists anywhere on the path; (b) the docId-knowledge precondition is realistic and does not require enumeration \u2014 a member of project A who legitimately learned a docId while the text was in A, whose owner then moved it into project B to take it out of that assistant's reach, can pull it straight back with the docId they already hold; (c) it is not deliberate-and-correct: plans/project-split.md:1134 states \"a member must not reach another project\", II.D7 is amended to say Seth meant other PROJECTS, and PART VII.1 explicitly records that the Drive-side project scoping this requires is NOT built yet. Known-and-deferred is still a live violation of the stated invariant in the code under audit.",
  "correction": "The finding holds as stated. Two refinements for whoever fixes it: (1) it is not confined to adopt/move \u2014 the identical unscoped docId tag search is also at v1.js:3678 in GET .../texts/<docId>/files, gated only by 'drive:read', which is the read/enumerate half of the same hole and the way an attacker confirms a foreign docId and harvests its file ids; (2) the repo's own plan (plans/project-split.md PART VII.1) argues that fixing this with a parents-vs-drive_folder_id check would be the WRONG mechanism, because text folders are deliberately mobile (tag-addressed, never parent-addressed: driveEnsureTextFolder's header, buildDriveEstate's \"never by where they sit\", and the Unassigned sweep all depend on that), so a parentage check would deny legitimately swept or hand-filed texts. The prescribed fix is a D1 drive_object(file_or_folder_id, doc_id, instance_id, project_id, ...) map authorized by one indexed lookup, with an operator-gated backfill."
}
```

## ad4ef24ba990db975

```json
{
  "refuted": false,
  "reasoning": "Confirmed by direct trace. worker/src/v1.js:3855-3877 (adopt) and 3890-3915 (move) call authMember(request, env, { instance: instanceId }, 'assignTexts'), take r = ctx.owner, verify only that the PATH instance belongs to the owner (`FROM instance WHERE instance_id=? AND researcher_id=?`), and then resolve the text from the caller-supplied path segment with an account-wide query: `appProperties has { key='flextextDoc' and value='${docId}' } ... &spaces=drive`, followed by `if (!(f.parents || []).includes(toFolder)) await driveReparent(access, f.id, toFolder, f.parents);` (adopt) or an addParents/removeParents PATCH (move). Nothing between authorization and the write compares the found folder's parents to the resolved project \u2014 `grep 'ctx.project_id'` returns zero hits in the whole file, so the project authMember resolved is never used for anything. The identical unscoped search sits in the drive:read route at 3663-3680, giving a read-only variant of the same crossing.\n\nReachability holds: one owner may hold many projects (POST /v1/researcher/projects/create, \"a SECOND (third, fourth\u2026) project\", each a real Drive folder under master with device folders inside), and because r = ctx.owner is the same denormalized owner for every project of that account, the search under the owner's Drive token spans all of them. The only precondition is knowing a foreign docId (a UUID, crypto.randomUUID). Realistic acquisition: a member of two of the owner's projects with asymmetric caps (assignTexts in A, see/drive:read in B) legitimately learns B's docIds; a member removed from B retains them (the DELETE members batch deletes member_key grants precisely so removal is a real act \u2014 this route hands the access back); or a device re-homed between projects by /researcher/projects/assign while the member keeps cached docIds. The repo's own standard at the revoke route rejects id-unguessability as a boundary: \"Today the only barrier is that instance ids are unguessable. Under the projects/researchers split every member legitimately SEES those ids\".\n\nNot deliberate: the members-POST migration gate comment (v1.js ~2461) states the intended confinement \u2014 \"a member's file listing has to be ROOTED at that folder rather than walked from the account master and filtered afterwards\" \u2014 and project.drive_folder_id, the anchor that gate insists must exist before sharing, is never consulted by any converted route. driveEnsureTextFolder's comment (\"The tag search is scoped to trashed=false but NOT to the parent\") documents the search shape but was written for a single-owner world; under the Phase C rule \"a member reaches everything in their project, nothing outside it\" it is now the hole. Only caveat, which does not refute: the attack needs a docId the member must previously have been given, so it is an insider/ex-member escalation rather than blind enumeration."
}
```

## a37673b3076878f11

```json
{
  "refuted": false,
  "reasoning": "Confirmed by reading worker/src/v1.js. Both routes authorize on the instance in the path and then resolve the text by a caller-supplied docId with an account-wide Drive tag search that is never constrained to the project authMember resolved.\n\nadopt, v1.js:3855-3878:\n  const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');\n  if (!ctx) 401; if (!ctx.ok) 404;\n  const r = ctx.owner;   // the PROJECT OWNER's row\n  const docId = String(seg[4] || '').replace(/[^\\w-]/g, '').slice(0, 64);\n  const to = await env.DB.prepare('SELECT instance_id, nickname, oauth_folder_id FROM instance WHERE instance_id=? AND researcher_id=? AND revoked=0').bind(instanceId, r.researcher_id).first();\n  const toFolder = await driveEnsureDeviceFolder(env, access, instanceId, to.nickname, to.oauth_folder_id);\n  const fq = ...`appProperties has { key='flextextDoc' and value='${docId}' } and mimeType='application/vnd.google-apps.folder' and trashed=false`;\n  const found = await driveJson(access, 'GET', '...files?spaces=drive&orderBy=createdTime&fields=files(id,parents)&q=' + fq);\n  if (f) { if (!(f.parents || []).includes(toFolder)) await driveReparent(access, f.id, toFolder, f.parents); ... appProperties: { flextextUnassigned: '' } }\nmove repeats the identical unscoped search at v1.js:3909-3910 and PATCHes addParents/removeParents inline. ctx.project_id is used for nothing after the guard; no comparison against project.drive_folder_id anywhere in either route.\n\nWhy it is exploitable rather than academic:\n1. One owner routinely owns SEVERAL projects. reconcileProjects (v1.js:1723-1800) exists precisely for that case (\"A researcher who owns TWO project folders (Seth, 2026-08-20 \u2014 'Fayu Text Corpus' and 'Dani Dictionary')\") and mints one D1 project per Drive project folder under the same owner_id. spaces=drive with the owner's token spans all of those trees.\n2. The design leans on that boundary. validateCaps (v1.js:465-490) removed per-device `see` lists explicitly because \"Google Drive is addressed by FILE ID \u2014 a member restricted to one device could still open another's files through any docId-routed Drive route\u2026 The honest boundary is the one Drive can actually enforce, and that is the project. \u2026 Finer separation is a separate project \u2014 which is how Seth's own estate is already arranged.\" An unscoped docId-routed Drive route falsifies exactly that guarantee.\n3. Mechanics all check out: no outer gate on the /v1/instances/<id> block (v1.js:3499-3502); assignTexts is a real grantable cap; members only exist in migrated projects (the not_migrated gate), so project folders do exist; driveEnsureDeviceFolder returns the existing to.oauth_folder_id (a device folder inside the caller's project); driveReparent (v1.js:924) runs under the owner's token and succeeds Drive-wide; check-project-scoping.sh checks only guard shape, no-403, no authResearcher in the block, and wipe/force-remove owner-only \u2014 it has no containment check for docId lookups.\n\nAttack sequence I can trace: M is a project_member of A with {\"assignTexts\":true}; the same owner also owns project B. M sends POST /v1/instances/<A-instance>/texts/<B-docId>/adopt {}. authMember resolves A and returns ok; the tag query finds B's text folder anywhere in the owner's Drive; driveReparent moves it under A's device folder and clears flextextUnassigned so no sweep returns it. Same via POST .../texts/<B-docId>/move {to:<another A instance>}.\n\nThe only out-of-band precondition is knowing a docId in B (UUID-ish, and I found no member-reachable enumeration \u2014 /v1/researcher/drive-estate at v1.js:2870 is scoped to the caller's own account). That is satisfied by an ex-member of B who kept docIds, or by a researcher who is a member of both projects with assignTexts in only one; in the latter case the reparent still physically removes B's text tree from B and places it where A's other members reach it. Concrete sequence, real boundary violation, so not refuted.\n\nSame unscoped lookup also appears in GET .../texts/<docId>/files (v1.js:3678, drive:read) \u2014 the read-side twin."
}
```

## ad232ced7f0b92bd7

```json
{
  "refuted": false,
  "reasoning": "Traced end to end and could not refute it. (1) Mint: worker/src/v1.js:3805-3828 \u2014 `const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts'); ... const r = ctx.owner; ... const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);`. The only prior validation is :3815 `if (!body.audioFileId && !body.flextextFileId && !body.promptFileId) return j({error:'nothing_to_mint'},400,...)` plus an instance lookup bound to the OWNER's researcher_id (not to ctx.project_id). `mintTextfileUrl` (:1209-1215) validates nothing: `if (!fileId) return null;` then `const tk = { r: researcherId, f: fileId, ... }`. `clampTtlDays` (:1186-1190) allows 400 days. (2) Redemption: :1859-1897 \u2014 with scope null the token has no `tk.i`, so the instance-liveness check is skipped, and the member check is `SELECT 1 AS ok FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?` bound to (minter, owner). Neither that query nor the scoped variant (`JOIN instance i ON i.project_id=pm.project_id WHERE i.instance_id=?`) mentions `tk.f`; :1902-1905 then streams `files/<tk.f>?alt=media` with `driveAccessToken(env, owner)`. So the file is never related to a project at mint or at serve. (3) No guard elsewhere: authMember (:543-648) resolves the project off the instance row and its own comment says the project IS the boundary; nothing maps a Drive file id to a project. (4) Multiple projects per owner are real (:1760, :2400), and drive.file scope (:1998) spans the whole app-created estate, so cross-project read is meaningful. (5) Not deliberate \u2014 plans/project-split.md:1708-1714 states the intended rule the code misses: \"The file id comes straight from the request body at all three mint sites... Put the scope INTO the token (owner, project, instance, doc, granting member, jti), re-check against live D1 at serve, and clamp member-minted TTLs well below the 400 days clampTtlDays allows.\" The shipped code implemented the minter-stamp/revocation half only. The attack sequence as written is performable. This violates the stated rule that a member \"must never reach... another project\".",
  "correction": "Two scope corrections, neither weakening the finding: (a) \"ANY file in the owner's Drive\" is really any APP-CREATED file (the OAuth scope is drive.file, v1.js:1998) whose id the member already knows \u2014 Drive ids are unguessable, so the attack needs a prior id (retained from a project they were removed from, or obtained via the account-wide `flextextDoc` tag search at :3678 in GET /texts/<docId>/files), not a brute-force; (b) the account master Drive FOLDER is not readable this way \u2014 `alt=media` on a folder id returns a Drive error, so the exposure is files, not folder listings."
}
```

## a07e575a7c3e1c65b

```json
{
  "refuted": false,
  "reasoning": "Verified against the code; every quoted line is accurate and the attack sequence traces end to end.\n\nMINT: worker/src/v1.js:3805-3828. `const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts'); ... const r = ctx.owner;` then `const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);`. Grep for promptFileId/audioFileId/flextextFileId/extractFromZipId returns six sites (3815, 3826-3828, 3882-3883, 3922-3923, 4806-4809) and NONE validates the id against the project, the docId's Drive folder, or anything else. mintTextfileUrl (1209-1210) is `if (!fileId) return null; const tk = { r: researcherId, f: fileId, ... }` \u2014 no validation whatsoever. authMember (502-645) resolves project_id only from instance.project_id / crowd_recorder.project_id / target.project; it never sees a file id. The response returns the minted URL to the caller (`return j({ ok: true, ttlDays, audioUrl, flextextUrl, promptUrl }, 200, ...)`).\n\nREDEMPTION: 1885-1896. Both branches of the `tk.m && tk.m !== tk.r` check query project_member joined to either `instance i ON i.project_id=pm.project_id ... AND i.instance_id=?` or `project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?`. Neither mentions tk.f. It is a revocation gate, not a scoping gate, and it passes trivially while M remains a member of project A. The file then streams via `driveAccessToken(env, owner)` + `files/<tk.f>?alt=media` to a header-less, unauthenticated fetch.\n\nWHY IT IS A PHASE C DEFECT, NOT PRE-EXISTING: before conversion these routes used authResearcher, so `r` was the caller and an arbitrary file id reached only the caller's own Drive \u2014 harmless. The contrast is still in the file: GET /v1/researcher/drive-file/<fileId> (3387) is still `const r = await authResearcher(...)` and therefore streams from the CALLER's Drive, no escalation. Substituting `const r = ctx.owner` under a member-reachable guard is exactly what converts an unvalidated caller-supplied id into a cross-project read of the owner's Drive, violating the stated rule \"a member reaches everything in their project, nothing outside it, and must never reach the account master Drive folder or another project.\"\n\nREACHABILITY (the lens I was asked to apply): the path is reachable, not theoretical. The only precondition beyond a legitimate assignTexts grant is knowing one foreign Drive file id, and concrete sources exist in this same file: GET .../texts/<docId>/files (3662) searches the owner's ENTIRE Drive for `appProperties has {key='flextextDoc' and value='<docId>'}` with no project filter, so a member with a foreign docId enumerates that text's file ids; and a researcher removed from project B retains every id they saw there while still a member of project A, letting them mint a FRESH long-lived token after removal \u2014 precisely the \"revocation that leaves a tail\" the tk.m mechanism was written to prevent, defeated by the coarse `p.owner_id=?` fallback. check-project-scoping.sh was read in full for the relevant sections: it enforces call-site shape (`!ctx.ok` guards), no-403 denial, no authResearcher regression under /v1/instances, and owner-only wipe/force-remove. Nothing in it constrains file ids.\n\nI could not find any guard elsewhere that prevents this, and no reading under which the behaviour is deliberate: the design comments around mintTextfileUrl and the redemption check discuss revocation of member-minted tokens, never the question of whether the file is in the member's project.",
  "correction": "Two precision corrections, neither of which weakens the finding. (1) The reachable set is bounded by the OAuth scope: the worker holds `https://www.googleapis.com/auth/drive.file` (v1.js:1998, 2054), so the exposure is every app-CREATED file in the owner's Drive \u2014 which still spans all of the owner's other projects and the account master folder, i.e. exactly the boundary the design rule names. It is not the owner's arbitrary personal Drive content. (2) The 400-day TTL applies only to POST .../assignment/finish, which passes `clampTtlDays(body.ttlDays)` (max 400, v1.js:1186-1190). /move (3881) and /adopt (3921) pass `ttlMs = 0`, and mintTextfileUrl falls back to `ttlMs || 90 * 86400000`, so tokens minted there live 90 days, not 400. Also worth noting for the fix: on /assignment/finish the audio and flextext tokens ARE scoped (tk.i = the project-A instance) and so are killable by revoking that device, while promptFileId is deliberately unscoped (the comment at 3818-3824 explains why) \u2014 making the prompt slot the strongest variant of the attack, as the finding states."
}
```

## a1ea98e30bdf7518d

```json
{
  "refuted": false,
  "reasoning": "The code says what the finding claims and the sequence reproduces. worker/src/v1.js:3826-3828 pass body.audioFileId/flextextFileId/promptFileId verbatim into mintTextfileUrl with r = ctx.owner; mintTextfileUrl (1209-1210) validates nothing but non-emptiness (\"if (!fileId) return null; const tk = { r: researcherId, f: fileId, ... }\"); the minted URLs are returned to the caller at 3831. Redemption (1885-1896) checks only minter membership \u2014 'FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?' for the unscoped token, or the instance's project for a v2 token \u2014 and neither query references tk.f (nor tk.d), then streams the file with the owner's Drive token to an unauthenticated GET. authMember (543-645) resolves the project off instance.project_id, so a project_member row in project A with assignTexts passes for project A's instances while ctx.owner's Drive token spans the owner's whole FlexText estate (plans/drive-scope-containment.md:95-120). There is no outer guard on the /v1/instances block (3499-3502), and DELETE /v1/projects/<id>/members (2494-2508) is deliberately per-project, so the coarse p.owner_id fallback does not close when a member is removed from project B but remains in project A \u2014 the exact revocation property the comment at 1866-1882 claims to provide. clampTtlDays (1186-1190) allows 400 days with no member-specific clamp. The repo's own plan (plans/project-split.md:1710-1715) lists this as work Phase C had to do \u2014 \"the file id comes straight from the request body at all three mint sites... re-check against live D1 at serve, and clamp member-minted TTLs\" \u2014 of which only the minter stamp and instance scope shipped, so this is not deliberate-and-correct. Concrete sequence: M is a member of the same owner's projects A and B, legitimately learns a project-B file id via GET .../texts/<docId>/files (3662-3690), is removed from B, then POSTs {\"promptFileId\":\"<B file id>\",\"ttlDays\":400} to a project-A instance's .../assignment/finish and fetches the returned URL. Two claims in the write-up are overstated and should not be carried into a fix: the attacker must already know the file id (this path supplies no enumeration \u2014 /v1/researcher/drive-estate at 2870-2871 uses authResearcher and lists the CALLER's own Drive, not the owner's), and a folder id such as the account master folder cannot be streamed via ?alt=media, so the exposure is files rather than folder traversal.",
  "correction": "The defect is real but is not specific to promptFileId's null scope: NO mint site validates that the supplied Drive file id lies within the caller's project, and the v2 scope on audioFileId/flextextFileId only re-checks that the named instance is live, never the file (tk.d is never consulted at serve). The exploit requires the member to already possess an out-of-project file id \u2014 no enumeration is available through this endpoint, and /v1/researcher/drive-estate is account-scoped to the caller's own Drive \u2014 so the realistic reproduction is a researcher who is (or was) a member of a second project of the same owner. Streaming a folder id would fail, so \"reaches the account master Drive folder\" is wrong; the exposure is app-created files elsewhere in the owner's estate. The crowd-side mint (4809), which passes no minterId at all, is NOT member-reachable \u2014 that block authenticates with authResearcher (4742)."
}
```

## a90f4a6edfdb3de50

```json
{
  "refuted": false,
  "reasoning": "The code says exactly what the finding claims, and I can trace a concrete request sequence.\n\nworker/src/v1.js:3662-3681 (GET /v1/instances/<id>/texts/<docId>/files):\n  const ctx = await authMember(request, env, { instance: instanceId }, 'drive:read');\n  ...\n  const r = ctx.owner;   // the PROJECT OWNER's row: Drive acts in their account (R2-5)\n  const owned = await env.DB.prepare('SELECT instance_id FROM instance WHERE instance_id=? AND researcher_id=?')\n    .bind(instanceId, r.researcher_id).first();\n  if (!owned) return j({ error: 'not_found' }, 404, origin, env);\n  const docId = String(seg[4] || '').replace(/[^\\w-]/g, '').slice(0, 64);\n  ...\n  const fq = encodeURIComponent(`appProperties has { key='flextextDoc' and value='${docId}' } ...`);\n  const found = await driveJson(access, 'GET', '...files?spaces=drive&orderBy=createdTime&fields=files(id)&q=' + fq);\n\nThree things I verified rather than assumed:\n\n1. authMember (v1.js:543-647) resolves the project from `instance.project_id` ONLY \u2014 the docId is never consulted, and there is no D1 table binding a docId to a project (`grep drive_object` \u2192 no hits; plans/project-split.md says such a table \"is still needed\"). So the only scoping this route applies is on the instance in the PATH.\n2. The `owned` re-check is `researcher_id=?` bound to ctx.owner, i.e. account scope, not project scope. It cannot constrain the docId.\n3. The Drive lookup is `spaces=drive` on the owner's token with no parentage filter, and the same tag-search property is stated deliberately at v1.js:1134-1136 (\"scoped to trashed=false but NOT to the parent\"). listChildren then returns id/name/size/mime/role for the text folder and its originals/ child, plus folderId and originalsFolderId.\n\nOne owner can hold several projects (POST /v1/researcher/projects/create, v1.js:3156-3184; buildDriveEstate models a project layer), so \"another project of the same owner\" is a real state, not hypothetical.\n\nConcrete sequence (no guessing of ids at any step): researcher M is a member of projects A and B of the same owner, both with {\"drive\":\"read\"}. M legitimately lists text T in project B and records T's docId (returned in the same response family the panel uses). The owner then removes M from project B \u2014 DELETE /v1/projects/<B>/members deletes the project_member row and the member_key grants (v1.js:2493+), so authMember denies for every B instance. M then sends GET /v1/instances/<A-instance-id>/texts/<T-docId>/files: authMember resolves project A, caps.drive==='read' passes, `owned` passes (the A instance does belong to the owner), and the account-wide tag search still finds T's folder inside project B's subtree. M gets T's folderId, originalsFolderId and every child file's id, name, size, mime and role \u2014 after their access to B was revoked. The same shape occurs without any revocation when the owner moves a device/text container into another project (POST /v1/researcher/projects/assign, v1.js:3186+): the instance's project_id follows, but a docId the member already saw does not.\n\nThat is a direct violation of the stated rule \"A member reaches everything in their project, nothing outside it\", and it is not an accepted trade-off in the design: plans/project-split.md records the boundary as \"a member's estate listing must be ROOTED at their project folder, never at master\", lists R2-1's folder-parentage scoping as \"unchanged and still required\", and the v1.js:2455-2472 comment on the members POST gate says the confinement \"has to be ROOTED at that folder rather than walked from the account master and filtered afterwards\". This route neither roots nor filters. The validateCaps comment (v1.js:473-478) makes the identical argument that killed the per-device `see` list \u2014 \"a member restricted to one device could still open another's files through any docId-routed Drive route\" \u2014 and the argument applies verbatim to projects here; the code asserts the project boundary is the one Drive can enforce, but this route does not enforce it.",
  "correction": "The finding is right about the behaviour and the impact, but one scope qualifier should ride with it so it is not over-sold: this is not an enumeration primitive. docIds are `crypto.randomUUID()` (docs/js/flextext.js:14, newGuid), and no member-reachable route hands a member docIds from outside their own project \u2014 GET /v1/researcher/drive-estate uses authResearcher, so a member calling it walks their OWN Drive, not the owner's. The leak therefore requires a docId the member learned while legitimately inside that scope (revoked membership, or a container/text that moved to another project), which the two sequences above supply. The fix is the same either way: constrain the resolved folder to the project's subtree (project.drive_folder_id) before listing, or resolve the docId\u2192project relation in D1, rather than trusting the unguessability of a UUID \u2014 which this repo already calls an insufficient barrier in the /revoke post-mortem comment at v1.js:3948-3960."
}
```

## a5987497cd9f8fc7d

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED \u2014 I traced it end to end and could not refute it.\n\n1) The code says what the claim says. worker/src/v1.js:3662-3680:\n   const ctx = await authMember(request, env, { instance: instanceId }, 'drive:read');\n   if (!ctx) 401; if (!ctx.ok) not_found;\n   const r = ctx.owner;\n   const owned = await env.DB.prepare('SELECT instance_id FROM instance WHERE instance_id=? AND researcher_id=?').bind(instanceId, r.researcher_id).first();\n   if (!owned) not_found;\n   const docId = String(seg[4] || '').replace(/[^\\w-]/g, '').slice(0, 64);\n   const fq = encodeURIComponent(`appProperties has { key='flextextDoc' and value='${docId}' } and mimeType='application/vnd.google-apps.folder' and trashed=false`);\n   const found = await driveJson(access, 'GET', '...files?spaces=drive&orderBy=createdTime&fields=files(id)&q=' + fq);\n   ... return j({ folderId, originalsFolderId, files }, 200, ...)\n   The docId is caller-supplied; the tag search is account-wide (spaces=drive, no parent filter). The `owned` query only re-asserts the instance belongs to the owner whose Drive token is used \u2014 it adds no scoping over the docId. The helper comment at v1.js:1134-1136 states the property explicitly: \"The tag search is scoped to trashed=false but NOT to the parent\".\n\n2) No guard exists elsewhere. authMember (v1.js:543-660) scopes only the TARGET (the instance row's project_id); it never constrains a resource named later in the path. I searched for any Drive-parentage containment on member reads (driveProjectFolderFor, project.drive_folder_id, any \"under the project folder\" test) \u2014 none is applied on this path, nor on the identical searches in /adopt (3868) and /move (3909). check-project-scoping.sh verifies only that authMember is called, that !ctx.ok is guarded, that no 403 is emitted, and that wipe/force-remove stay owner-only; it has no Drive-containment check.\n\n3) The design explicitly requires the opposite, so this is not deliberate. The members-POST migration gate comment (v1.js, in the /v1/projects/<id>/members POST branch) says: \"The boundary a member is confined to IS a Drive project folder \u2014 Seth's rule is that they must not reach *the root folder outside of projects shared with them*, so a member's file listing has to be ROOTED at that folder rather than walked from the account master and filtered afterwards.\" This route walks the account master. backfillProjectsFor's comment likewise says \"Phase C's Drive rules (R2-1) work by FOLDER PARENTAGE\".\n\n4) It is genuinely new: pre-Phase-C the caller was the owner (authResearcher), for whom an account-wide search was correct. Converting to authMember admits a member without adding the scoping the conversion assumes.\n\n5) Concrete sequence, with no guessing (docIds are GUIDs from newGuid(), so I checked the attacker can hold one legitimately):\n   a. Owner O has two migrated projects A and B \u2014 reconcileProjects (v1.js:1755+) exists precisely because one owner has two project folders. M is added to A with caps {\"drive\":\"read\"}.\n   b. M legitimately lists a text on device D in A: GET /v1/instances/<D>/texts/<docId>/files \u2192 M now knows docId.\n   c. O moves container D into project B via POST /v1/researcher/projects/assign (v1.js:3211+), which sets instance.project_id=B. authMember({instance: D}) now denies M \u2014 access revoked as intended.\n   d. M re-issues the read through any instance still in A: GET /v1/instances/<A-instance>/texts/<docId>/files. authMember resolves A, caps pass, `owned` passes (same owner O), and the account-wide tag search finds the text folder now under project B. The 200 body returns folderId, originalsFolderId, and each child's id/name/size/mime/modified/role.\n   Equivalent variants: a member removed from B who retains docIds; or a member of B holding non-drive caps who learns B's docIds and then reads them via A's drive:read.\n\nSo a member's Drive read is scoped by the path instance, not by the resource, and revocation-by-move (or removal) fails to cut access. That violates \"a member reaches everything in their project, nothing outside it, and must never reach the account master Drive folder or another project\", and the rooted-listing rule the code itself states.",
  "correction": "The claim stands as written. One narrowing worth recording: exploitation requires the attacker to KNOW a docId (they are unguessable GUIDs), so the practical attack is a member who learned the docId while legitimately entitled and retains access after the entitlement is withdrawn (device re-homed to another project, or membership removed), or a member of two projects whose caps differ. The same unscoped docId tag search also appears in POST .../texts/<docId>/adopt (v1.js:3868) and POST .../texts/<docId>/move (v1.js:3909), both gated on 'assignTexts' \u2014 so the fix belongs in a shared \"resolve this text's folder within ctx.project_id's Drive folder\" helper rather than in the /files route alone."
}
```

## a550f2f983f41b546

```json
{
  "refuted": false,
  "reasoning": "The code says what the finding claims, and I found no guard anywhere that constrains the lookup to the resolved project.\n\nAt worker/src/v1.js:3663-3680 the route is:\n\n    if (m === 'GET' && sub === 'texts' && seg.length === 6 && seg[5] === 'files') {\n      const ctx = await authMember(request, env, { instance: instanceId }, 'drive:read');\n      if (!ctx) return j({ error: 'unauthorized' }, 401, origin, env);\n      if (!ctx.ok) return j({ error: 'not_found' }, 404, origin, env);\n      const r = ctx.owner;   // the PROJECT OWNER's row: Drive acts in their account (R2-5)\n      const owned = await env.DB.prepare('SELECT instance_id FROM instance WHERE instance_id=? AND researcher_id=?')\n        .bind(instanceId, r.researcher_id).first();\n      if (!owned) return j({ error: 'not_found' }, 404, origin, env);\n      const docId = String(seg[4] || '').replace(/[^\\w-]/g, '').slice(0, 64);\n      ...\n        const fq = encodeURIComponent(`appProperties has { key='flextextDoc' and value='${docId}' } and mimeType='application/vnd.google-apps.folder' and trashed=false`);\n        const found = await driveJson(access, 'GET', '...files?spaces=drive&orderBy=createdTime&fields=files(id)&q=' + fq);\n\nThe only checks are (a) authMember resolves the *path instance* to a project the caller is a member of with drive:read, and (b) `owned` re-confirms that instance belongs to the owner. The docId is caller-supplied and is used verbatim in an account-wide (`spaces=drive`, no parent/ancestor filter) tag search against the OWNER's Drive token. `ctx.project_id` is never read by this route \u2014 in fact grep shows `ctx.project_id` is not consumed anywhere in the file, and no helper performs a project-folder containment test (`driveProjectFolderFor` exists at v1.js:987 but is only used by the crowd-folder path at 1285). The response returns `folderId`, `originalsFolderId`, and every child's `id, name, size, mime, modified, role`.\n\nMultiple projects per owner in one Google account are real, not hypothetical: `reconcileProjects` (v1.js:1760-1796) creates one D1 project per Drive project folder under the single `driveMasterFolder`, and POST /v1/researcher/projects/create (3156) plus /projects/assign (3211) let an owner add projects and re-home a device container into another one, updating `instance.project_id`.\n\nConcrete, fully in-band sequence (no out-of-band secret needed):\n1. M is a member of project A with caps {\"drive\":\"read\"}. Device X and device Y are both in A. M legitimately learns docId D from device X's texts via the panel.\n2. The owner moves X's container into project B: POST /v1/researcher/projects/assign {folderId: X-folder, projectFolderId: B-folder} \u2192 `UPDATE instance SET project_id=<B>` (v1.js:3237). M loses membership-based access to X and to text D, which now lives under B's subtree.\n3. M sends GET /v1/instances/<Y-instance-id>/texts/<D>/files. authMember resolves Y \u2192 project A \u2192 member with drive:read \u2192 ok. `owned` passes (Y is the owner's). The tag search is account-wide, finds D's folder under project B, and the response returns B's folderId, originalsFolderId, and every file's id/name/size/mime/role.\n\nA second, equally concrete variant: a researcher removed from project B (DELETE /v1/projects/<B>/members) who remains a member of A keeps listing every project-B text whose docId they saw while they were a member.\n\nThis contradicts the design rule as stated in the code itself. The members POST gate comment (v1.js:2460-2470) spells out the intended confinement: *\"The boundary a member is confined to IS a Drive project folder \u2014 Seth's rule is that they must not reach 'the root folder outside of projects shared with them', so a member's file listing has to be ROOTED at that folder rather than walked from the account master and filtered afterwards.\"* This route does neither: it resolves from an account-wide search. The `driveEnsureTextFolder` comment (v1.js:1134-1136) confirms the search property: *\"The tag search is scoped to trashed=false but NOT to the parent.\"*\n\nPre-Phase-C this route was owner-only, where account-wide resolution is correct by definition; the conversion to authMember at 3663 is what turns it into a cross-project reach for members. I could not refute it: the code path is reachable, the guard is absent, and the sequence is performable.",
  "correction": "Two scoping notes on the finding as written, neither of which defeats it. (1) It is not an enumeration oracle: docIds are `crypto.randomUUID()` (docs/js/researcher-panel.js:3220) and no member-reachable endpoint lists another project's docIds \u2014 the account-wide `/v1/researcher/drive-estate` etc. run under `authResearcher`, i.e. the caller's own Drive. So the leak applies to docIds the member has legitimately seen (or seen previously), which is exactly the re-homed-device and removed-member cases above, not to arbitrary discovery. (2) The claimed chain into byte-level reads via a mint is a separate route's property and was not verified here; this route on its own leaks folder ids, file ids, names, sizes, mime types, modification times and role tags for texts outside the caller's project."
}
```

## a11a827d30c867c05

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED \u2014 traced end to end, could not refute.\n\n1) The code is verbatim as quoted. worker/src/v1.js:3747-3777 (POST /v1/instances/<id>/texts/<docId>/assignment/upload/start):\n   const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');\n   if (!ctx) 401; if (!ctx.ok) not_found;\n   const r = ctx.owner;   // the PROJECT OWNER's row: Drive acts in their account (R2-5)\n   const inst = await env.DB.prepare('SELECT instance_id, nickname, oauth_folder_id FROM instance WHERE instance_id=? AND researcher_id=? AND revoked=0').bind(instanceId, r.researcher_id).first();\n   const access = await driveAccessToken(env, r);\n   let parent = String(body.originalsFolderId || '');                      // 3765\n   if (body.kind === 'consent-prompt') { parent = await driveEnsureDeviceFolder(...); }\n   if (!parent) return j({ error: 'bad_folder' }, 400, origin, env);\n   ... body: JSON.stringify({ name, mimeType: mime, parents: [parent], appProperties: { flextextRole: role } }),   // 3776\n   `parent` is the raw body value: no files.get, no parents walk, no comparison to inst.oauth_folder_id, to the text folder, or to project.drive_folder_id. The token is the OWNER's (driveAccessToken(env, r), r = ctx.owner). drive.file scope does not save it \u2014 every folder in the estate is app-created and therefore writable.\n\n2) No guard downstream. The chunk route (3790-3803) checks only `sess.rr !== r.researcher_id`; `rr` was stamped as the owner's id at start and `r` is the same owner, so a member's session passes.\n\n3) No guard upstream. authMember (543-647) resolves the project only from instance.project_id / crowd_recorder.project_id / an explicit {project}; it never sees a Drive id from the body. validateCaps (465-500) makes assignTexts a plain delegable boolean. check-project-scoping.sh verifies only that authMember is called, that !ctx.ok is guarded, that no 403 is emitted, and that wipe/force-remove stay owner-only \u2014 it has no Drive-containment check.\n\n4) Newly reachable and contrary to the stated design. Pre-Phase-C the route was authResearcher (owner), for whom \"anywhere in my own Drive\" is correct; the conversion admits a non-owner without adding scoping. It violates \"a member reaches everything in their project, nothing outside it, and must never reach the account master Drive folder or another project\", and the code's own rule in the members-POST gate comment that a member's confinement \"has to be ROOTED at that folder rather than walked from the account master and filtered afterwards\".\n\n5) Concrete sequence with no id guessing. Owner O holds projects A and B (reconcileProjects, 1755+, exists because one owner has several project folders). M is a member of A with {\"assignTexts\":true,\"drive\":\"read\"} and holds a folder id F inside B \u2014 either legitimately from a prior membership of B (DELETE members removes the row, not M's knowledge of F) or from GET /v1/instances/<A-instance>/texts/<B-docId>/files (3662-3708), whose account-wide flextextDoc tag search returns folderId and originalsFolderId for a text in B; driveEnsureTextFolder's own comment (1134-1136) states \"The tag search is scoped to trashed=false but NOT to the parent\". M then POSTs .../texts/<any-docId>/assignment/upload/start {\"name\":\"consent.wav\",\"mime\":\"audio/wav\",\"size\":1024,\"kind\":\"audio\",\"originalsFolderId\":\"F\"} \u2192 200 + uploadId, and PUTs the bytes to .../assignment/upload/chunk. The file lands inside project B tagged flextextRole:'source-audio'. The same call with F = the master folder id writes outside every project.\n\nThe sibling begin route has the same hole one level up (3737): driveEnsureTextFolder(access, deviceFolder, docId, body.title, body.folderId), whose knownId path (1147-1153) returns any caller-named id for which files.get succeeds and !f.trashed \u2014 so begin both returns and mutates (creates an `originals` child in) a foreign folder.\n\nOnly caveat: harm outside the member's own project requires possessing a foreign folder id. That is a knowledge barrier, not an authorization control, and the docId-routed read supplies it; a removed member of B retains F regardless. The claim about the code \u2014 unvalidated caller-supplied parent, member-reachable, written with the owner's token \u2014 is exactly true."
}
```

## afbdf05ac2f136197

```json
{
  "refuted": false,
  "reasoning": "I tried to refute this and could not. The code says exactly what the finding claims, and I traced a concrete request sequence that needs no guessing and no dependency on the other finding.\n\nCODE (worker/src/v1.js:3748-3777). The route resolves authorization against the INSTANCE:\n  const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');\n  if (!ctx) return j({ error: 'unauthorized' }, 401, ...);\n  if (!ctx.ok) return j({ error: 'not_found' }, 404, ...);\n  const r = ctx.owner;   // the PROJECT OWNER's row: Drive acts in their account (R2-5)\n  const inst = await env.DB.prepare('SELECT instance_id, nickname, oauth_folder_id FROM instance WHERE instance_id=? AND researcher_id=? AND revoked=0').bind(instanceId, r.researcher_id).first();\n...then acts on a folder the CALLER named, with nothing tying the two together:\n  3765: let parent = String(body.originalsFolderId || '');\n  3766-3768: if (body.kind === 'consent-prompt') { parent = await driveEnsureDeviceFolder(...); }\n  3769: if (!parent) return j({ error: 'bad_folder' }, 400, ...);\n  3776: body: JSON.stringify({ name, mimeType: mime, parents: [parent], appProperties: { flextextRole: role } }),\nNo files.get, no parents check, no docId-tag check, no comparison with `inst.oauth_folder_id`, `ctx.project_id`, or `project.drive_folder_id`. I grepped for any containment helper (driveAssert*/inProject/underProject/confine) \u2014 none exists; the only hits are the prose comment at 2459-2471. check-project-scoping.sh checks only that authMember is called and guarded on !ctx.ok; it has no notion of which Drive folder a route then touches, so it passes this route as written.\n\nREACHABILITY \u2014 the part I attacked hardest. Drive ids are unguessable, so the escalation needs a folder id the member actually holds. That is obtainable through documented, legitimate endpoints, and the grant that yielded it is never re-checked:\n1. M is a member of project A and project B (any caps incl. drive:read).\n2. M calls GET /v1/instances/<B-instance-id>/texts/<docId>/files (3662-3709) \u2014 a normal, authorized call for a project M belongs to \u2014 and receives `{ folderId, originalsFolderId, files }`. M records originalsFolderId = F.\n3. The owner removes M from project B: DELETE /v1/projects/<B>/members (2494+). project_member and member_key rows go; the Drive ids M already saw do not.\n4. M \u2014 still a member of project A with assignTexts \u2014 calls POST /v1/instances/<A-instance-id>/texts/<any-docId>/assignment/upload/start with {\"name\":\"consent.wav\",\"mime\":\"audio/wav\",\"size\":1024,\"kind\":\"audio\",\"originalsFolderId\":\"F\"}. authMember resolves project A and passes; parent = F verbatim; the resumable session opens with parents:[F] under the OWNER's token. M then PUTs bytes via .../assignment/upload/chunk (3791-3803, which only checks sess.rr === r.researcher_id).\nResult: a file tagged flextextRole:'source-audio' planted inside project B by a researcher with no membership in B. Same shape without any removal, via the container move at 3210-3250: the owner moves a device folder from A to B (instance.project_id is updated, and the code comment says outright \"A container keeps its folder ID across the move, so nothing that resolves by id notices\"), and M's remembered originalsFolderId still writes into it.\n\nThis is precisely the rule \"A member reaches everything in their project, nothing outside it\" \u2014 and the design's own comment at 2459-2463 states the intent (\"a member's file listing has to be ROOTED at that folder rather than walked from the account master and filtered afterwards\"). The read side was reasoned about; the write side has no rooting at all.\n\nI also checked the sibling claim at 3737: driveEnsureTextFolder(access, deviceFolder, docId, body.title, body.folderId) with the knownId path at 1147-1153 \u2014 `if (f && f.id && !f.trashed) return f.id;` \u2014 returns any caller-named folder that merely exists and is untrashed, with no parentage and no flextextDoc-tag check, after which driveEnsureChildFolder creates an 'originals' child inside it. Same hole one level up, confirmed as written.\n\nMulti-project-per-owner is real (1760, 2400, 3178 all list/insert several projects per owner_id), so \"another project\" is not a hypothetical shape.",
  "correction": "Two overreaches in the finding should be trimmed before it is written up, because fixing what it literally says would aim at the wrong place.\n\n(a) \"anywhere in the owner's app-created Drive tree\" is stronger than what is reachable. A member can only write into folders whose ids they possess, and every member-reachable route returns ids inside the member's own project. I found no route a non-owner can call that returns the account master folder id: driveMasterFolder (733) is only called from owner-only paths, and /v1/researcher/drive-estate (2870) and /drive-snapshot (2839) use authResearcher and return the CALLER's own estate, not the owner's. So the \"writes outside every project, into FlexText Uploads\" half is not evidenced \u2014 drop it.\n\n(b) The attack as written depends on the separate unscoped-docId read finding, and that dependency is not needed and is itself weaker (docIds are client-minted GUIDs, newGuid() in docs/js/app.js, so a foreign docId is not guessable either). The load-bearing sequence is the one above: an id legitimately obtained while the member had access (or while the container was in their project) stays usable after the grant is withdrawn or the container moves, because `parent` is never re-resolved against ctx.project_id.\n\nStated accurately: possession of a Drive folder id, not project membership, is what authorizes the write \u2014 authMember decides the caller may act on an instance, and the route then acts on an unrelated folder. The fix belongs at 3765 (and at 3737/1147-1153): resolve the parent from the authorized instance/project \u2014 verify via files.get that the named folder carries the expected flextextRole/flextextDoc tag AND descends from ctx's project drive_folder_id \u2014 and fail closed otherwise."
}
```

## a6f259d90a62b1def

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED \u2014 I traced it and could not refute it.\\n\\n1) The code is as quoted. v1.js:3748-3777, POST /v1/instances/<id>/texts/<docId>/assignment/upload/start:\\n   const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');\\n   if (!ctx) 401; if (!ctx.ok) not_found;\\n   const r = ctx.owner;\\n   const inst = await env.DB.prepare('SELECT instance_id, nickname, oauth_folder_id FROM instance WHERE instance_id=? AND researcher_id=? AND revoked=0').bind(instanceId, r.researcher_id).first();\\n   ...\\n   const access = await driveAccessToken(env, r);\\n   let parent = String(body.originalsFolderId || '');          <-- v1.js:3765, verbatim\\n   if (body.kind === 'consent-prompt') { parent = await driveEnsureDeviceFolder(...); }\\n   if (!parent) return bad_folder;\\n   ... parents: [parent], appProperties: { flextextRole: role }\\n   docId is never even parsed on this route. The only scoping is authMember on the PATH instance; the `inst` re-check binds researcher_id to ctx.owner (account scope) and cannot constrain a body-supplied folder id.\\n\\n2) No guard elsewhere. authMember (543-648) resolves the project from instance.project_id only and never sees a resource named later in the request. project.drive_folder_id is consulted only by the members-POST migration gate (2473-2475) and reconcileProjects; no parentage/containment helper exists among the drive* helpers (662-1489). check-project-scoping.sh checks only that authMember is called, that !ctx.ok is guarded, that no 403 is emitted, and that wipe/force-remove stay owner-only.\\n\\n3) The chunk leg does not close it: 3791-3800 re-runs authMember(...,'assignTexts') and validates only sess.rr === r.researcher_id \u2014 r is the OWNER, so the member's own session passes.\\n\\n4) Preconditions are real: assignTexts is a grantable capability (validateCaps, 484-488); one owner can hold several projects (POST /v1/researcher/projects/create at 3156-3184, reconcileProjects at 1755+).\\n\\n5) Concrete sequence with no guessing: M is a member of project A with {\\\"assignTexts\\\":true} and was (or still is) a member of project B under the same owner, so M legitimately holds B's originalsFolderId (returned by GET .../texts/<docId>/files at 3648 and by assignment/begin at 3741). M POSTs .../instances/<A-instance>/texts/<any-docId>/assignment/upload/start with originalsFolderId = B's folder, then PUTs .../assignment/upload/chunk. The file is created inside project B under the OWNER's token, tagged flextextRole: source-audio. The unscoped docId search at 3678 supplies the same id for a project M was never in.\\n\\n6) The begin sibling has the same hole one level up: 3737 passes body.folderId to driveEnsureTextFolder, whose knownId branch (1148-1155) returns any untrashed folder id that files.get resolves, after which driveEnsureChildFolder plants an 'originals' child inside it.\\n\\n7) Not deliberate. The members-POST gate comment requires a member's Drive view to be ROOTED at the project folder; validateCaps 468-482 argues the project is \\\"the boundary Drive can actually enforce\\\". BACKLOG's \\\"stopping before R2-1 is safe\\\" argument rests on unconverted Drive routes running under authResearcher with the CALLER's token \u2014 it does not cover these routes, which are converted and act on ctx.owner's token. It is genuinely new: pre-Phase C only the owner could reach this, and an owner writing anywhere in their own Drive is not an escalation.",
  "correction": "The claim holds. One narrowing for whoever fixes it: the master-folder variant is not directly reachable today, because no member-facing response returns the master folder id (GET /v1/projects deliberately omits drive_folder_id for joined projects, 2409-2414). The reproducible target is another project's text/originals folder, whose id a member obtains either legitimately (prior membership, or assignment/begin at 3741) or through the unscoped docId tag search at 3678. Also note the write hole is not confined to upload/start: assignment/begin at 3737 accepts body.folderId with only an exists-and-untrashed files.get (driveEnsureTextFolder knownId branch, 1148-1155), so the same member can re-point a text and have an 'originals' child created inside a foreign folder. A parents-vs-project.drive_folder_id check is the obvious fix but conflicts with the deliberately parent-agnostic, tag-addressed text folders (driveEnsureTextFolder's header at 1134-1136); the repo's own prescription (plans/project-split.md PART VII.1) is a D1 drive_object(file_or_folder_id -> doc_id/instance_id/project_id) map resolved by one indexed lookup."
}
```

## a843e420e880d8fce

```json
{
  "refuted": false,
  "reasoning": "Verified against the actual code. worker/src/v1.js:3900-3901 resolves the caller-supplied destination with `SELECT ... FROM instance WHERE instance_id=? AND researcher_id=?` bound to `r.researcher_id` (= ctx.owner, the project owner), and `ctx.project_id` is never referenced anywhere in the /move route \u2014 no second authMember({instance: toId}) and no project comparison. Multi-project-per-owner is a live state, not hypothetical: reconcileProjects (v1.js:1755-1815) creates one project row per Drive project folder for the same researcher_id and rewrites instance.project_id per folder, and validateCaps (465-499) makes separate projects the DESIGNATED mechanism for finer separation (\"Finer separation is a separate project \u2014 which is how Seth's own estate is already arranged (Fayu Text Corpus and Dani Dictionary)\"). So instance.researcher_id is identical across projects A and B while project_id differs, and the `to` lookup only checks the former. No guard prevents it: driveEnsureDeviceFolder (1014-1034) returns the existing device folder id without checking its parent, so the PATCH at 3914-3915 re-parents the text folder under project B's device folder; check-project-scoping.sh only verifies that authMember is called and that !ctx.ok is guarded, which it is. The migration gate on POST /members (drive_folder_id NOT NULL) means sharing exists only on migrated, folder-structured estates \u2014 the multi-project case \u2014 so it widens rather than closes the path. Concrete sequence: member M of project A with {\"assignTexts\":true} POSTs /v1/instances/<A-instance>/texts/<A-docId>/move {\"to\":\"<B-instance>\"}; authMember resolves project A and passes, `from` and `to` both resolve on the shared owner researcher_id, and A's text folder is re-parented into a device folder of project B, which M has no grant on. Knowing a B instance id is a knowledge precondition, not a guard \u2014 a researcher removed from B legitimately held those ids, and the same file (the I2 comment at 1866-1882) treats surviving a removal as the failure to prevent. This falsifies the stated rule \"A member reaches everything in their project, nothing outside it\".",
  "correction": "One element of the stated impact does not hold and should not drive the fix: the minted delivery tokens are NOT usable by the attacker. /v1/textfile re-resolves member-minted tokens at redemption (v1.js:1886-1893) with `SELECT 1 FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?`, so a token M mints scoped to a project-B instance is 410 for everyone. The real, evidenced damage is the Drive re-parent alone: project A's text folder leaves A's tree and lands under a device folder in project B. (Separately, and not part of this finding, the docId in the same route is also resolved by a global appProperties search across the owner's whole Drive rather than scoped to ctx.project_id \u2014 the fix should scope both the destination instance and the folder search to ctx.project_id, or the same boundary is crossed in the other direction.)"
}
```

## ac30b658a9bc87265

```json
{
  "reasoning": "CONFIRMED \u2014 I could not refute it, and I traced the sequence myself.\n\n1) The code says what the finding claims. worker/src/v1.js:3889-3902:\n\n    if (m === 'POST' && sub === 'texts' && seg.length === 6 && seg[5] === 'move') {\n      const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');\n      ...\n      const r = ctx.owner;   // the PROJECT OWNER's row: Drive acts in their account (R2-5)\n      const toId = String(body.to || '');\n      ...\n      const to = await env.DB.prepare('SELECT instance_id, nickname, oauth_folder_id FROM instance WHERE instance_id=? AND researcher_id=? AND revoked=0')\n        .bind(toId, r.researcher_id).first();\n      if (!from || !to) return j({ error: 'not_found' }, 404, origin, env);\n\nThe destination is bound to `r.researcher_id`, the OWNER's id, not to `ctx.project_id`. There is no second authMember call and no project comparison in the route. `grep -n \"ctx\\.project_id\" worker/src/v1.js` returns NOTHING \u2014 the field authMember returns at line 638 (`{ ok: true, caller, owner, project_id, caps, isOwner }`) is read by zero routes, so no converted call site anywhere performs a destination-project check. This is the sibling of the mistake the /adopt route avoids by taking its destination from the authMember-checked path segment.\n\n2) The scoping is genuinely wider than the project. authMember's own comment (line 583) states instance.researcher_id is \"a maintained denormalization: ALWAYS equal to the project's owner_id\" \u2014 so binding on it spans EVERY project that owner owns. Multiple projects per owner is a first-class supported state: `POST /v1/researcher/projects/create` at line 3156, commented \"a SECOND (third, fourth\u2026) project\", inserts another `project` row with the same `owner_id`, and `driveProjectsMigrate` (line 1794) mints one project row per Drive project folder for the same researcher.\n\n3) It pierces the ONLY isolation the design offers. validateCaps' comment (lines 470-483) removes per-device visibility and records Seth's decision: \"if an owner researcher needs it to be more specific than that, all he or she has to do is just create a separate project for that scope\u2026 which is how Seth's own estate is already arranged (Fayu Text Corpus and Dani Dictionary).\" Separate projects ARE the boundary, and this route crosses it.\n\n4) Concrete, performable sequence:\n   - Owner O owns projects A and B. M is a project_member of A with {\"assignTexts\":true} (POST /v1/projects/A/members) and has NO assignTexts in B (member with only drive:read, or a former member of B whose row was deleted at line 2500).\n   - M learns a B instance id legitimately: GET /v1/researcher/keys (line 2307) returns `SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=?` bound to the CALLER \u2014 every device grant M has ever held in B, instance ids included. The route's own comment concedes the client keeps these (\"It cannot un-know a key the member already fetched and cached\"), so removal from B does not take the ids back.\n   - M sends POST /v1/instances/<A-instance>/texts/<A-docId>/move {\"to\":\"<B-instance>\"}. authMember resolves project A off the path instance and passes on assignTexts. The `to` query matches because researcher_id is O for B's instances too.\n   - Line 3905 `driveEnsureDeviceFolder(env, access, toId, \u2026)` returns/creates B's device folder (I read it at 1014 \u2014 it performs no authorization), and lines 3909-3915 PATCH the text folder with `addParents=<B device folder>&removeParents=<all old parents>`, executed with the OWNER's Drive token. Project A's text is now under project B's subtree and out of A's. Drive scoping is by folder parentage (R2-1), so B's other members gain it and A's lose it.\n\n5) Refutation attempts that failed:\n   - The token half IS partly contained: /v1/textfile at lines 1882-1897 re-checks `project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?`, so the minted URLs 410 for a non-member of B. That narrows the impact but does not prevent it \u2014 the Drive re-parent has already committed before any token is minted, and it is irreversible from M's side.\n   - Unguessable ids are not a defense here by this codebase's own explicit standard: the /revoke comment at line 3947 rejects exactly that reasoning (\"Today the only barrier is that instance ids are unguessable\u2026 it failed OPEN\") and the route was fixed rather than left resting on it. And the dual-membership variant needs no guessing at all.\n   - check-project-scoping.sh only verifies that authMember is called and both outcomes are guarded; it has no rule about caller-supplied secondary instance ids, so it passes on this route.\n\nCaveat for whoever fixes it: the owner must still be allowed to move between their own projects (and the legacy dual-read branch returns project_id:'' with isOwner:true), so the guard belongs on `!ctx.isOwner` \u2014 compare the destination instance's project_id against ctx.project_id, deny as not_found.",
  "refuted": false
}
```

## a01a3383f015c41a7

```json
{
  "refuted": false,
  "reasoning": "Confirmed by direct trace. worker/src/v1.js:3889-3903 resolves the destination with `SELECT ... FROM instance WHERE instance_id=? AND researcher_id=?` bound to `ctx.owner.researcher_id`, with no second authMember call and no comparison against ctx.project_id. That filter is not a project boundary: migrate-projects.sql redefines instance.researcher_id as \"a maintained denormalisation: always equal to the project's owner_id\", so it is identical across every project of that owner; per-project separation lives only in instance.project_id, which /v1/researcher/projects/assign rewrites per container (v1.js:3236-3239). Multiple projects per owner are first-class (POST /v1/researcher/projects/create, v1.js:3149 \"a SECOND (third, fourth...) project\"), and validateCaps' own comment states the project IS the only enforceable boundary (\"Finer separation is a separate project - which is how Seth's own estate is already arranged\"). Reproducible sequence: owner O has projects A and B; M is a project_member of A with {\"assignTexts\":true} and not a member of B; POST /v1/instances/<A-instance>/texts/<docId>/move {\"to\":\"<B-instance>\"} passes authMember on project A, the `to` row resolves because researcher_id=O matches B's instances, and the Drive PATCH runs addParents=<B device folder>&removeParents=<old parents>, re-homing project A's text folder under a project-B device. That is a write into another project performed with only project-A authority, violating the stated project-scoping rule, and it needs only assignTexts (not even drive:read). No guard elsewhere prevents it: driveEnsureDeviceFolder does no authorization, and check-project-scoping.sh only checks that authMember is called and guarded, never that a route's destination id is in the resolved project. The sibling /adopt route (3854-3886) is not affected because its `to` lookup binds instanceId, which authMember already scoped.",
  "correction": "The core claim is correct; one detail of the stated attack overreaches. The minted delivery URLs do not usefully reach the destination: mintTextfileUrl stamps m = ctx.caller.researcher_id (v1.js:1225) and /v1/textfile redemption checks \"SELECT 1 FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?\" against the scoped destination instance (v1.js:1885-1893), so a token scoped to a B instance and minted by a non-member of B returns 410. The file-bytes half is therefore already closed. The persistent, unguarded effect is the Drive re-parent: the text folder leaves project A's device folder and lands under project B's device folder, becoming visible to project-B members who hold no grant on A and disappearing from A."
}
```

## a1d0bef94a5806a17

```json
{
  "input": "{}",
  "refuted": true,
  "reasoning": "The code says what the finding quotes \u2014 `worker/src/v1.js:3554` gates POST /v1/instances/<id>/command on `authMember(..., 'manageDevices')` and the accepted type list at 3564 includes `assign`, `delete` and `uploadDelete` \u2014 but this is the spec'd mapping, not a bypass, and the attack does not achieve what it claims.\n\n1) It is deliberate and matches the design. `plans/project-split.md:40` defines the capability itself as:\n   `| manageDevices | change device settings, push commands, rename, revoke installs | per-device or all |`\n\"push commands\" IS manageDevices in the capability table the code implements. The command lane is one lane carrying changeSettings/triggerUpload/setDone as well; those are unambiguously manageDevices. The neighbouring `command/cancel` route even documents the same reasoning explicitly (3620-3628: \"manageDevices is the strictly-safe interim: never wider than today (this was owner-only)\"). This is a spec-ambiguity/taste question between two rows of a table, not a code defect \u2014 and \"fixing\" it by moving the lane to assignTexts would mis-gate changeSettings and triggerUpload.\n\n2) The assign half of the attack cannot hand out a project text. A device assignment is only usable with a minted private streaming token, and EVERY `mintTextfileUrl` call site on the instance lane is assignTexts-gated: `texts/<doc>/assignment/finish` (3806\u21923826-3828), `texts/<doc>/adopt` (3855\u21923881), `texts/<doc>/move` (3890\u21923921). The assignment upload chain (`assignment/begin`, `upload/start`, `upload/chunk`, 3724/3748/3791) is assignTexts too. A manageDevices-only member can therefore queue an assign command carrying only a URL they already control \u2014 they cannot mint or reach any URL for the project's Drive content, so the withheld capability's actual effect (delivering project texts) is intact.\n\n3) None of the stated defect-defining invariants is violated: authorization is decided in exactly one place (I1); the path fails closed (no manageDevices \u21d2 `{ok:false}` \u21d2 not_found, never a researcher_id fallback \u2014 I4); denial stays not_found; scoping is project-scoped via authMember; and no command type reaches wipe or force-remove, which remain owner-only on their own routes (4054-4055, 4093).\n\nThe only residual is wording: `uploadDelete` arguably reads onto assignTexts' \"remove texts from devices\". But it is the upload-FIRST delete (the device uploads a fresh Drive copy and only then removes the local text \u2014 `docs/js/researcher.js:944`, `docs/js/app.js:3857`), so it destroys nothing in the archive, and it rides the device-command lane the spec assigns to manageDevices. That is a granularity refinement to raise with the owner, not an evidenced correctness defect.",
  "correction": "The gate is per the spec, not a bypass: plans/project-split.md:40 defines manageDevices as including \"push commands\", and the command lane also carries changeSettings/triggerUpload/setDone. A manageDevices-only member still cannot assign any project text, because every route that mints a /v1/textfile streaming token (assignment/finish 3806, adopt 3855, move 3890) and the whole assignment-upload chain (3724/3748/3791) is assignTexts-gated \u2014 the queued assign can only point at a URL the member already controls. The one arguable overlap is `uploadDelete` (spec's assignTexts row says \"remove texts from devices\"), but it is upload-first and non-destructive to the Drive archive; that is a granularity question for the owner, not a defect against any of the stated invariants."
}
```

## ab37fd53496f80b1b

```json
{
  "refuted": true,
  "reasoning": "The code reads as described (v1.js:3554 gates on 'manageDevices'; 3564 accepts assign/delete/uploadDelete) and the request sequence is reachable \u2014 authMember passes a member with {manageDevices:true}, and the inner instance SELECT binds the owner's researcher_id, which does not exclude a member. But it is not a defect. (1) It matches the design's own capability table: plans/project-split.md:40 defines `manageDevices` as \"change device settings, **push commands**, rename, revoke installs\" \u2014 the command endpoint is the push-commands endpoint, so the gate is the spec, not a bypass of it. (2) No stated invariant is violated: the action stays inside the caller's own project on a device the owner granted them manageDevices over; no project boundary, master-folder, denial-semantics, ownership, key-wrap or owner-only (wipe/force-remove) rule is touched. validateCaps' own comment (v1.js:478-484) pins the intended granularity: \"membership in a project grants access to everything IN that project, and the capabilities below say what may be DONE. Finer separation is a separate project.\" (3) The claimed effect does not follow: withholding assignTexts withholds every URL-minting route (assignment/begin, upload/start, chunk, finish, adopt, move \u2014 3724-3890 \u2014 the only callers of mintTextfileUrl), so the member cannot deliver a project text; they can only queue an assign pointing at an arbitrary URL of their own, which is no escalation. (4) The removal half destroys nothing: docs/js/app.js:3614-3620 refuses any remote delete unless the doc is confirmed on Drive (\"refusing remote delete \u2014 not safely on Drive\"), and uploadDelete is upload-first (app.js:3857-3874). Meanwhile manageDevices already permits changeSettings and install revocation on the same device, comparable or greater disruption. (5) The authors annotate deliberate interim capability choices when they mean something narrower (see v1.js:3620-3629 for the cancel route), consistent with the plain table mapping here. The residue is a spec-granularity overlap between two capability rows, resolvable either way \u2014 a design decision for the owner to make, not an evidenced authorization hole.",
  "correction": "The command endpoint's manageDevices gate matches the design's capability table, which explicitly assigns \"push commands\" to manageDevices. A manageDevices-only member can queue assign/uploadDelete, but cannot mint any URL into the owner's Drive (all minting routes require assignTexts) and cannot destroy data (both delete paths are proof-of-backup gated and upload-first). If Seth wants assignTexts to hard-gate text handout/removal, that is a deliberate spec clarification plus a per-cmd.type check \u2014 not a fix to apply off an audit finding."
}
```

## ada096dfeaa3a4f71

```json
{
  "refuted": true,
  "reasoning": "The code says what the finding quotes \u2014 worker/src/v1.js:3554 gates POST /v1/instances/<id>/command on `authMember(..., 'manageDevices')` and the accepted type list at :3564 includes 'assign', 'delete' and 'uploadDelete', with no consultation of `assignTexts`. So the mechanical half is accurate. But it is not a defect, for three reasons.\n\n(1) It matches the written capability definition rather than contradicting it. plans/project-split.md:40 defines the capability table the implementation follows:\n  | `manageDevices` | change device settings, push commands, rename, revoke installs | per-device or all |\n\"Push commands\" IS this endpoint. The neighbouring cancel route carries an explicit, dated rationale for its capability choice on the same lane (v1.js:3620-3628: \"\u26a0 GATED ON manageDevices FOR NOW, NOT ON `cancelOthers`, and the difference is honest rather than an oversight\"), which shows the command lane's gating was reasoned about, not defaulted. Nothing in the code or the plan states that assign/delete commands require `assignTexts`.\n\n(2) None of the invariants that define a defect for this audit is violated. There is still exactly one authority (authMember, I1); it fails closed (I4); denial is `not_found`; scoping is project-level; the owner row is used only for Drive and `ctx.caller` for attribution (v1.js:3593 `by: ctx.caller.researcher_id`); no wipe/force-remove is delegated \u2014 a single-text uploadDelete is not the wipe path. What is left is a granularity question about two coarse capabilities whose plain-English descriptions overlap, i.e. a spec question for the owner, not an implementation bug. That is precisely the class of finding whose \"fix\" (adding `assignTexts` to the command route) would be a behaviour change made on an auditor's reading of a doc table rather than on a stated rule.\n\n(3) The attack as written does not do what it claims for the headline case. The `assign` half cannot hand out a text: the device only fetches content through `/v1/textfile` tokens, and every `mintTextfileUrl` call site in the file (v1.js:3826, 3827, 3828, 3881, 3921) sits inside a route already gated on `assignTexts` (assignment/finish, adopt, move). A manageDevices-only member can queue an assign command carrying an arbitrary URL, but cannot mint a URL to any project file \u2014 so \"M hands out texts the owner withheld\" does not reproduce; only \"M makes a device attempt a download from a URL M chose\" does, which is a different and much weaker claim than the one filed.",
  "correction": "What is true and survives: the `delete` and `uploadDelete` command types do reproduce a real effect under manageDevices alone \u2014 the device dispatcher accepts a command with no `enc` field (docs/js/sync.js:440 `let cmd = c; if (c.enc) {\u2026}`), so a plaintext `{type:'uploadDelete', id:'<docId>'}` reaches `syncDispatch`'s `case 'uploadDelete'` (docs/js/app.js:3857) via `cmd.docId || cmd.id`. Since plans/project-split.md:40 also describes `assignTexts` as \"assign texts to / remove texts from devices\", the removal action is reachable under either reading of the table. That is an ambiguity in the capability table (both rows describe the same action from different angles), and the right resolution is Seth deciding whether \"remove a text from a device\" belongs to manageDevices-as-push-commands or to assignTexts \u2014 not a unilateral gate change. The assign half of the finding should be dropped outright: URL minting is assignTexts-gated at every call site, so no usable assignment can be created without the capability."
}
```

## a65c91f0a0d341a6a

```json
{
  "refuted": false,
  "reasoning": "Confirmed by tracing the code; every quoted line matches.\n\n1) v1.js:2277-2300 \u2014 the grant write resolves `proj` via the dual-read (`inst.project_id ? <project row> : { project_id: null, owner_id: inst.researcher_id }`) and binds `String(proj.project_id || '')`. An instance whose project_id is NULL therefore produces a member_key row with project_id=''.\n\n2) v1.js:3471 \u2014 `INSERT INTO instance (instance_id, researcher_id, type, nickname, desired_blob, desired_rev, revoked, created_at, estate)` writes no project_id, and the comment above it states \"no `project_id` written anywhere, nothing to drift.\" The lazy stampers do not close the window promptly: `backfillProjectsFor` (1713) only adopts rows `WHERE project_id IS NULL` and is called at 2217 and at 2630 guarded by `if (!mine)` (only when the researcher owns no project); `reconcileProjects` (1813) only scans rows `WHERE oauth_folder_id IS NOT NULL`, which a brand-new instance without a Drive folder is not. So POST /v1/instances immediately followed by POST /v1/researcher/keys writes project_id=''.\n\n3) v1.js:2500-2501 \u2014 the removal batch is `DELETE FROM project_member WHERE project_id=? AND researcher_id=?` plus `DELETE FROM member_key WHERE project_id=? AND researcher_id=?`, under a comment asserting \"\u26a0 ONE BATCH, so a member can never be left listed-but-keyless or keyless-but-listed.\" A row holding '' (or any stale project id) does not match, so the second statement removes nothing and logApproval reports \"0 grant(s)\".\n\n4) v1.js:2312-2314 \u2014 GET /v1/researcher/keys selects `FROM member_key WHERE researcher_id=?` with no project or membership predicate, so the removed member keeps fetching the wrapped Ki indefinitely.\n\nNo compensating guard exists. `grep -n \"member_key\" worker/src/v1.js` returns only: the INSERT OR REPLACE (2290), the two SELECTs (2312/2314), the owner-only instance-scoped DELETE (2377/2379), and the sweep (2501). There is no `UPDATE member_key` anywhere in the file or the SQL, so member_key.project_id is a permanent write-time snapshot.\n\nThe grant write imposes no membership requirement on grantees \u2014 only `if (r.researcher_id !== proj.owner_id) 403` and the owner-copy check at 2283 \u2014 so the owner can grant to any researcher_id, as step 4 of the attack requires.\n\nThe staleness is also produced by two automatic paths beyond the '' case, which makes the defect reachable on fully migrated estates: POST /v1/researcher/projects/assign (3238) rewrites `instance.project_id` under a comment claiming \"\u26a0 SO BOTH ARE WRITTEN IN ONE ACT (invariant I3). They cannot drift because nothing updates one without the other\" \u2014 member_key is the third copy that claim omits; and reconcileProjects (1817-1819) rewrites instance.project_id from Drive parentage on every panel load, so an owner moving a device folder between project folders in Drive silently orphans that instance's member_key rows. In that second case the member genuinely held authorized access via the project, so removal is supposed to revoke and does not.\n\nConcrete sequence, all server-side and performable with direct API calls: owner O with migrated project P; POST /v1/instances -> instance X with project_id NULL; POST /v1/projects/P/members {researcher_id:M} -> 200; POST /v1/researcher/keys {instance_id:X, grants:[O,M]} -> both rows written with project_id=''; DELETE /v1/projects/P/members {researcher_id:M} -> {removed:1, grants_removed:0}; GET /v1/researcher/keys as M -> still returns {instance_id:X, wrapped_ki}.",
  "correction": "The mechanism is right; one sub-scenario in the attack text is framed loosely. \"Grant M a key while X is in P, then move X to Q ... removing M from Q deletes nothing\" only bites if M is a member of Q \u2014 which is precisely the sharper case: the row was stamped project_id=P at grant time, X is later reassigned to Q (v1.js:3238, or rewritten by reconcileProjects at v1.js:1817-1819 from Drive parentage), M reaches X legitimately as a member of Q, and DELETE /v1/projects/Q/members sweeps only project_id=Q and misses the P-stamped row. That variant, not the '' one, is where a member who genuinely had authorized access keeps a retrievable key after removal. In the '' variant the member never had authMember access to the unassigned instance (v1.js:588-591 denies non-owners when project_id is empty), so what breaks there is the owner's revoke action and the \"listed-but-keyless\" invariant rather than a project boundary. Both stem from the same root: member_key.project_id is a write-time snapshot that nothing ever updates, so the sweep should key on the membership relation (e.g. delete by researcher_id joined to the instances currently in the project, plus the '' rows granted by this owner) rather than on the stored snapshot."
}
```

## a19216f60f363c0f7

```json
{
  "refuted": false,
  "reasoning": "Traced every link in the chain against the code. (1) v1.js:2300 binds String(proj.project_id || '') and proj.project_id is null whenever instance.project_id is NULL (2276-2278), so the grant row is written with project_id=''. (2) POST /v1/instances (3470-3472) writes no project_id; the lazy mint at 2628-2630 fires only `if (!mine)` \u2014 when the researcher has NO project row at all \u2014 and reconcileProjects (1806-1820) only stamps containers that already carry an oauth_folder_id found in the Drive tree, capped at 64 writes. So a fresh device on a fully migrated account sits at project_id NULL, not transiently but until it acquires a folder and a reconcile pass reaches it. (3) POST /v1/researcher/keys requires only caller == proj.owner_id and that the grants array contains the owner's own copy; it applies no membership or project check to the other grantees, so the owner can legitimately write a row for member M against that NULL-project instance. (4) The removal sweep at 2501 is `DELETE FROM member_key WHERE project_id=? AND researcher_id=?` bound to the URL projectId, which is never '' \u2014 the row cannot match, and the route returns grants_removed:0 while logApproval records '0 grant(s)', directly contradicting its own comment that \"a member can never be left listed-but-keyless or keyless-but-listed\". (5) GET /v1/researcher/keys (2308-2315) authenticates with authResearcher, not authMember, and the unfiltered query has no project or membership predicate, so an ex-member \u2014 still an approved researcher \u2014 keeps receiving the wrapped Ki. Schema confirms project_id is not part of the primary key (instance_id, researcher_id, key_version) and is a pure snapshot; grep over worker/ finds NO `UPDATE member_key` statement anywhere, so /v1/researcher/projects/assign (3238) rewriting instance.project_id leaves the grant row stale as claimed. All four requests are ordinary authenticated calls; no unusual precondition beyond a NULL-project instance, which is the default state at creation. Refutation attempts that failed: the migration gate (2472-2478) constrains POST members only, not key grants; the DELETE /v1/researcher/keys route (2364-2382) keys on instance_id and works correctly, but it is a separate manual action, not part of member removal; authMember's dual-read does deny the ex-member data access on that instance, which narrows impact but does not stop the key handover.",
  "correction": "The mechanism is exactly as claimed, but the consequence should be stated as incomplete key-grant revocation rather than continued data access. authMember (v1.js:593-594) denies a non-owner against a NULL-project instance, so the removed member cannot read that device's blobs through the worker; what survives is (a) GET /v1/researcher/keys continuing to serve the wrapped Ki to a non-member indefinitely \u2014 which defeats revocation precisely in the case it is meant to cover, a grant written and the member removed before they ever fetched it \u2014 and (b) a false audit trail, since the route reports grants_removed:0 and logs '0 grant(s)' while asserting atomicity. Note also that the owner does have a working manual revoke at DELETE /v1/researcher/keys (2364-2382), which is scoped by instance_id and is unaffected; the defect is confined to the member-removal path. The stale-snapshot variant is real but narrower than presented: it requires M to be removed from Q after device X moved P->Q while M remains a member of P."
}
```

## a9c313fd238b2ab98

```json
{
  "refuted": false,
  "reasoning": "The finding reproduces exactly as described, and I could not refute any step.\n\nCode verified:\n- v1.js:2277-2279 `const proj = inst.project_id ? await \u2026SELECT project\u2026 : { project_id: null, owner_id: inst.researcher_id };` and v1.js:2300 `.bind(String(proj.project_id || ''), instanceId, String(g.researcher_id), version, \u2026)` \u2014 a grant written while instance.project_id is NULL is stored with member_key.project_id = ''. The in-code comment at 2292-2299 says so itself and warns \"\u26a0 Phase C must treat '' as unassigned rather than as a project id.\"\n- v1.js:3471 `'INSERT INTO instance (instance_id, researcher_id, type, nickname, desired_blob, desired_rev, revoked, created_at, estate) VALUES (?,?,?,?,?,0,0,?,?)'` \u2014 no project_id column; the comment at 3482 states \"no `project_id` written anywhere, nothing to drift.\" And reconcileProjects only stamps rows that already have a Drive folder: v1.js:1812-1814 `SELECT oauth_folder_id, project_id FROM ${table} WHERE researcher_id=? AND oauth_folder_id IS NOT NULL`. So NULL project_id on a fresh device is a persistent state, not a narrow race.\n- v1.js:2500-2501 the removal batch: `DELETE FROM project_member WHERE project_id=? AND researcher_id=?` and `DELETE FROM member_key WHERE project_id=? AND researcher_id=?`, both bound with the URL projectId. schema-current.sql:107-116 shows `PRIMARY KEY (instance_id, researcher_id, key_version)` \u2014 project_id is a denormalized snapshot, not part of the identity. `grep -n \"UPDATE member_key\"` over worker/ returns nothing, so the snapshot is never refreshed after instance reassignment (v1.js:3238 rewrites instance.project_id alone).\n- v1.js:2310-2315 `SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=? \u2026` \u2014 authResearcher only, no membership or project predicate, so a removed member's fetch still succeeds.\n- The sibling per-grant revoke at v1.js:2377-2379 uses `WHERE instance_id=? AND researcher_id=?` \u2014 the PK columns \u2014 proving the correct sweep key was available and the member-removal path picked a different one.\n\nAttack steps all hold: POST /v1/researcher/keys applies no membership test to grantees (only caller-is-owner at 2280 and the owner's-copy check at 2282), so granting M a key on a NULL-project instance is accepted and both rows land with project_id=''. The DELETE sweep then matches nothing and the route returns grants_removed:0 and logs `keys + ' grant(s)'` = \"0 grant(s)\".\n\nThis violates the route's own declared invariant, stated in its header comment at v1.js:2424-2427: \"\u26a0 REMOVING A MEMBER ALSO DELETES THEIR KEY GRANTS, in the same batch. Without that, 'removed' would mean 'no longer listed' while they still hold every wrapped Ki the ledger handed them \u2014 revocation as a UI state rather than an act (invariant I5).\" The batch comment at 2496-2498 (\"ONE BATCH, so a member can never be left listed-but-keyless or keyless-but-listed\") is likewise false in this case.\n\nRefutation attempts that failed: (a) no other code path deletes or rewrites member_key on removal; (b) no client-side compensating call exists (docs/js/researcher.js only ever self-grants, and there is no member-management client at all, so nothing cleans up); (c) the migration gate at 2473-2478 is on project.drive_folder_id, not on the instance, so it does not prevent the NULL-project instance case; (d) the assignment/reassignment variant is also real \u2014 no UPDATE member_key exists anywhere.\n\nThe only inaccuracy is impact scope, not mechanism: after removal M cannot obtain fresh ciphertext, because every /v1/instances/<id>/\u2026 route runs through authMember, which denies a non-member, and in the '' dual-read branch (v1.js:588-593) authorizes only the instance's own researcher_id. So \"M keeps decrypting device X's E2EE material indefinitely\" overstates the consequence. What actually persists and is evidenced: the worker keeps handing wrapped Ki secret material to a person who is no longer a member, the removal is not atomic as claimed, and the approval log records a false \"0 grant(s)\". That is a genuine defect in the removal path, and the fix is to sweep on the authoritative key (researcher_id plus the instances currently in the project, or researcher_id + instance_id like the 2377 revoke does).",
  "correction": "The mechanism is confirmed; only the stated impact needs narrowing. The removed member cannot reach instance data afterwards \u2014 authMember (v1.js:588-647) denies a non-member on every /v1/instances route, and in the project_id='' dual-read branch it authorizes only the instance's own researcher_id, consulting project_member not at all. So the accurate finding is: DELETE /v1/projects/<id>/members fails to delete the member's member_key rows whenever member_key.project_id (a write-time snapshot, '' for any grant made while instance.project_id was NULL, stale after any project reassignment) differs from the project id in the URL. Consequences: GET /v1/researcher/keys (v1.js:2314, which has no membership predicate) keeps serving the wrapped Ki to the ex-member indefinitely; the route's declared atomic invariant I5 at v1.js:2424-2427 and 2496-2498 is not met; and logApproval records a false \"0 grant(s)\". The correct sweep is the one the per-grant revoke already uses \u2014 the table's real key, instance_id + researcher_id \u2014 scoped to the instances currently in that project (SELECT instance_id FROM instance WHERE project_id=?), not to member_key.project_id."
}
```

## a46f59bcfffea7410

```json
{
  "refuted": false,
  "reasoning": "Verified in worker/src/v1.js. All three cited routes build the identical unparented query \u2014 line 3678 (GET .../texts/<docId>/files), 3868 (POST .../adopt), 3909 (POST .../move): `appProperties has { key='flextextDoc' and value='${docId}' } and mimeType='application/vnd.google-apps.folder' and trashed=false` issued with `spaces=drive` against `driveAccessToken(env, r)` where `r = ctx.owner` \u2014 i.e. the OWNER'S ENTIRE Drive estate, with no parent constraint. The only binding beforehand is `authMember(request, env, { instance: instanceId }, 'drive:read'|'assignTexts')` plus an owner-scoped `SELECT ... FROM instance WHERE instance_id=? AND researcher_id=?`. `authMember` returns `project_id` (line 647) but grep confirms NO route ever reads `ctx.project_id` \u2014 the docId is never constrained to the caller's project. adopt then does `if (!(f.parents || []).includes(toFolder)) await driveReparent(access, f.id, toFolder, f.parents)` (3874) on whatever that search returned, and has no guard that the folder was actually unassigned; move does the equivalent addParents/removeParents PATCH (3911-3914).\n\nMulti-project-per-owner is real: POST /v1/researcher/projects/create (3178) and reconcileProjects (1794) both INSERT additional `project` rows under the same owner_id, and the members DELETE comment states \"a person may be a member of two projects\".\n\nThe design intent this violates is stated in the file itself, which is what makes it a defect rather than an accepted trade-off. validateCaps' comment (474-482) records that the per-device `see` list was DELETED precisely because \"Google Drive is addressed by FILE ID \u2014 a member restricted to one device could still open another's files through any docId-routed Drive route\u2026 The honest boundary is the one Drive can actually enforce, and that is the project. \u2026Finer separation is a separate project \u2014 which is how Seth's own estate is already arranged (Fayu Text Corpus and Dani Dictionary)\" \u2014 two projects of ONE owner. And the members POST comment (2461-2466) requires that a member \"must not reach the root folder outside of projects shared with them\", so \"a member's file listing has to be ROOTED at that folder rather than walked from the account master and filtered afterwards\". These queries walk from the account master. The recommended mitigation (separate projects) therefore does not hold against these three routes.\n\nConcrete sequence: owner O has migrated projects P and Q. M is a member of Q with {drive:'read'} (so M legitimately learns Q docIds \u2014 member_key grants at 2292-2300 let M decrypt Q device status) and of P with {assignTexts:true}. M calls POST /v1/instances/<D_P>/texts/<docId_from_Q>/adopt. authMember resolves D_P \u2192 project P \u2192 assignTexts present \u2192 ok:true. The estate-wide tag search finds Q's text folder and re-parents it under D_P's device folder in P, clearing flextextUnassigned \u2014 a write inside Q by a caller with no capability in Q, moving another project's text into P. Read-only variant needs only drive:read in P: GET /v1/instances/<any P instance>/texts/<docId_from_Q>/files returns Q's file ids, names and sizes.\n\nNo guard elsewhere prevents this: check-project-scoping.sh only verifies that authMember is called and that both outcomes are guarded (and that wipe/force-remove stay owner-only); it never checks that a caller-supplied Drive identifier is confined to ctx.project_id. Pre-Phase-C the unparented search was safe because only the owner could reach these routes; Phase C introduced non-owner callers without adding the parent scoping, so this is a genuine Phase-C regression, evidenced in code."
}
```

## ad67591da2dcf5a27

```json
{
  "refuted": true,
  "reasoning": "The mechanical observations are accurate, but the escalation the finding claims does not reproduce.\n\nWHAT IS TRUE IN THE CODE\n- worker/src/v1.js:3868 (adopt), :3909 (move) and :3678 (files) all run the same unparented tag search:\n  `appProperties has { key='flextextDoc' and value='${docId}' } and mimeType='application/vnd.google-apps.folder' and trashed=false` with `spaces=drive` \u2014 the owner's whole Drive, no parent constraint (the helper comment at :1153/1134 says so).\n- `ctx.project_id` is indeed never read: `grep -n \"ctx\\.project_id\" worker/src/v1.js` returns nothing. The only extra binding is `SELECT ... FROM instance WHERE instance_id=? AND researcher_id=?` (:3667, :3860, :3899), which ties the INSTANCE to the owner, not the docId to a project.\n\nWHY THE ATTACK DOES NOT REPRODUCE AS CLAIMED\n1. docId is an unguessable random UUID. `docs/js/app.js:3153` \u2192 `task.docId || newGuid()`, and `docs/js/flextext.js:14` is `crypto.randomUUID()`. The route is a lookup by secret id, not an enumeration.\n2. No member-reachable endpoint discloses docIds outside the caller's own projects. The two routes that return docIds from a Drive scan \u2014 `/v1/researcher/drive-snapshot` (:2840) and `/v1/researcher/drive-estate` (:2869, `buildDriveEstate` \u2192 `docId: (f.appProperties||{}).flextextDoc`, :857) \u2014 both resolve `const r = await authResearcher(request, env)` and mint the access token from the CALLER's own row, so a member scanning there sees their own Drive, never the owner's. `/v1/researcher/approvals` (:3410), which logs a full docId on adopt (:3881), is gated `if (!isOperator(r.drive_email, env)) return 403`. `/v1/projects` (:2396) returns project ids and names only. Per-instance state/reports are resolved through `authMember({instance})`, i.e. already project-bound.\n3. Consequently the finding's own attack has to hand M membership in Q (\"M holds drive:read in project Q \u2026 so M legitimately learns Q's docIds\"). With drive:read in Q, M can already call `GET /v1/instances/<Q instance>/texts/<docId_Q>/files` and stream those files \u2014 so the stated \"read-only variant\" gains nothing; it reaches data M is authorized for by a different route. The route also requires drive:read on the instance's own project (:3662 `authMember(..., 'drive:read')`), so \"any instance M can reach\" is wrong.\n4. The title's strong claim \u2014 \"acts on every project\" / \"texts belonging to a different project of the same owner\" \u2014 is therefore unreachable for any project M is not already a member of, which is the case that would matter.\n\nRESIDUAL (not the finding as written)\nWhere M is a member of BOTH P and Q with asymmetric caps (assignTexts in P, only drive:read/see in Q), `POST /v1/instances/<D_P>/texts/<docId_Q>/adopt` will re-parent Q's text folder under D_P's device folder (:3874 `if (!(f.parents || []).includes(toFolder)) await driveReparent(...)`), i.e. a capability granted in P takes effect on a folder living in Q. That is a per-project capability-granularity gap, not the claimed cross-project reach, it requires the owner to have deliberately granted the same person two projects with different caps, and every byte involved is already readable by that member. Worth noting as a hardening item (constrain the search to the project subtree, which the members-POST comment at :2466 already states as the intended model), but it is not the finding under review and does not support its title, attack, or impact.",
  "correction": "The three routes really do search the owner's whole Drive by docId with no project constraint, but that is not exploitable as described: docIds are random UUIDs (`crypto.randomUUID()`), and every endpoint that would disclose another project's docIds is either operator-only (`/v1/researcher/approvals`) or runs on the CALLER's own Drive via `authResearcher` (`/v1/researcher/drive-estate`, `/v1/researcher/drive-snapshot`). A member cannot name a docId from a project they cannot already reach, so no cross-project listing, streaming or re-parenting is achievable. The only real residual is narrower: a member of two projects of the same owner can apply an assignTexts grant held in project P to re-parent a text they already legitimately see in project Q \u2014 a capability-granularity gap over data the attacker can already read, not the estate-wide breach claimed."
}
```

## a3ebacc3d6bace130

```json
{
  "refuted": false,
  "reasoning": "Code confirms the claim. worker/src/v1.js:3678 (files), :3868 (adopt) and :3909 (move) each run `appProperties has { key='flextextDoc' and value='${docId}' }` against `files?spaces=drive` \u2014 the OWNER's whole estate, no parent constraint \u2014 and adopt then does `if (!(f.parents || []).includes(toFolder)) await driveReparent(access, f.id, toFolder, f.parents)` at :3874. The only binding is `authMember(request, env, { instance: instanceId }, 'assignTexts'|'drive:read')` plus `SELECT ... FROM instance WHERE instance_id=? AND researcher_id=?`, which binds the INSTANCE to the owner, not the docId to a project. `grep -n \"ctx.project_id\"` returns zero hits in the entire file: no route ever consults the project authMember resolved, even though `driveProjectFolderFor` (:987) and `project.drive_folder_id` exist. The code's own justification convicts it: validateCaps' comment at :474-478 removes the per-device `see` list precisely because \"Google Drive is addressed by FILE ID \u2014 a member restricted to one device could still open another's files through any docId-routed Drive route\u2026 The honest boundary is the one Drive can actually enforce, and that is the project\" \u2014 and the project boundary is then not enforced on those routes. plans/project-split.md:1294 states the requirement verbatim (\"Any docId-routed fetch is parentage-verified \u2026 filtering the listing alone is cosmetic if a direct id still works\"), and R2-1 (:739) is the same unfixed class. Reachability, judged specifically: the routes are reachable by any member holding the cap in ANY project of that owner; multi-project-per-owner is a designed, live state (reconcileProjects mints one project row per Drive project folder, :1760-1796; Seth's own estate has two). The one part that does NOT hold up is the finding's docId-discovery mechanism \u2014 device inventories (`reported_blob`) are returned only by GET /v1/researcher at :2668 whose instance list is `WHERE researcher_id=?` (the caller's own), and /v1/researcher/drive-estate (:2871) is authResearcher over the CALLER's Drive, so no route enumerates another project's docIds, and docIds are crypto.randomUUID (researcher-panel.js:3220). A performable sequence still exists without any enumeration: M creates text T in project Q via POST /v1/instances/<D_Q>/texts/<d>/assignment/begin (M's own panel mints d, so M knows it forever); M is later removed from Q, or holds only drive:read there; M is a member of P (same owner) with drive:read+assignTexts; M calls GET /v1/instances/<D_P>/texts/<d>/files (returns Q's file ids/names/sizes) then POST /v1/instances/<D_P>/texts/<d>/adopt, which re-parents Q's folder into P and mints /v1/textfile tokens for the bytes. That also defeats the I2 revocation guard at :1878, whose membership join is satisfied by M's surviving membership in P (tk.i = D_P), so a grant removed in Q leaves Q's data readable.",
  "correction": "The defect is real as stated in the CLAIM and EVIDENCE, but the ATTACK's docId-discovery step is wrong and should be replaced. No member-facing route hands a member docIds from a project they are not in: device inventories come only from GET /v1/researcher (:2668, `WHERE researcher_id=?` \u2014 caller's own instances) and drive-estate (:2871) is authResearcher over the caller's own Drive; docIds are unguessable randomUUIDs. The realistic precondition is instead that the member legitimately learned the foreign docId \u2014 they created the text while a member of project Q (assign-by-upload mints the docId client-side) and were later removed, or they are a current member of Q with drive:read and no write capability there. With that precondition the sequence runs exactly as described. Also note the fix is not simply parent-scoping the tag search: v1.js:1134 documents that the search is deliberately unparented so a researcher-moved folder is still found. The correct remedy is to verify the found folder's ancestry against the drive_folder_id of ctx.project_id (failing closed on a flat/unmigrated estate) rather than constraining the query."
}
```

## ae6481cd8aa38cbc9

```json
{
  "refuted": false,
  "reasoning": "Could not refute; every quoted line is accurate and the sequence traces end to end. MINT (v1.js:3805-3831): `const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');` \u2026 `const r = ctx.owner;` \u2026 `const audioUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.audioFileId, '', ttlMs, scope, ctx.caller.researcher_id);` (same for flextextFileId, and promptFileId with scope null). The only prior validation is the instance lookup \u2014 bound to `r.researcher_id`, the OWNER's account, never to `ctx.project_id` \u2014 and `if (!body.audioFileId && !body.flextextFileId && !body.promptFileId) return j({ error: 'nothing_to_mint' }, 400, \u2026)` at 3815. HELPER (1209-1215): `if (!fileId) return null; const tk = { r: researcherId, f: fileId, x: extract || '', e: \u2026 }` \u2014 no Drive lookup, no parentage check; `clampTtlDays` (1186-1190) permits 7..400 days with no member-specific clamp. REDEMPTION (1850-1905): owner resolved from tk.r; `if (tk.i)` only checks the instance is live and belongs to tk.r; the member-grant gate is `SELECT 1 AS ok FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?` (or, unscoped, `JOIN project p \u2026 WHERE pm.researcher_id=? AND p.owner_id=?`). Neither query mentions tk.f or tk.d; the file then streams via `fetch('\u2026/files/' + encodeURIComponent(tk.f) + '?alt=media', { Authorization: 'Bearer ' + access })` with `access = await driveAccessToken(env, owner)` to a header-less GET. NO GUARD ELSEWHERE: authMember (543-648) resolves project_id only from instance.project_id / crowd_recorder.project_id / target.project and never sees a file id; check-project-scoping.sh enforces only call-site shape (!ctx.ok), no-403 denial, no authResearcher under /v1/instances, and owner-only wipe/force-remove. REACHABILITY: multi-project-per-owner is real (reconcileProjects inserts a project per Drive project folder, 1794) and drive.file spans the owner's entire app-created estate, so a member of P minting a Q file id is a genuine cross-project read; the concrete sequence is a researcher who was a member of Q (legitimately learning file ids from GET .../texts/<docId>/files, 3662-3690), is removed from Q, and then POSTs {flextextFileId:'<Q id>', ttlDays:400} to a project-P instance's .../assignment/finish and fetches the returned URL \u2014 precisely the post-removal tail the tk.m mechanism at 1866-1897 was written to close, defeated because the gate asks about the SCOPED INSTANCE's project, not the file's. NOT DELIBERATE: plans/project-split.md:1707-1714 states the intended rule the shipped code only half-implements \u2014 \"Put the scope INTO the token (owner, project, instance, doc, granting member, jti), re-check against live D1 at serve, and clamp member-minted TTLs well below the 400 days clampTtlDays allows.\" PHASE C REGRESSION, not pre-existing: GET /v1/researcher/drive-file/<fileId> (3387) still uses authResearcher, so an arbitrary id there reaches only the caller's own Drive; substituting `const r = ctx.owner` under a member-reachable guard is what converts an unvalidated body id into a read of the owner's Drive, violating the stated rule that a member reaches nothing outside their project.",
  "correction": "The mechanism holds, but two claims in the write-up are overstated and should not be carried into a fix. (1) This path supplies no enumeration: Drive file ids are unguessable, so the attacker must already KNOW a foreign id \u2014 the realistic source is ids retained from a project they were removed from (or the separate cross-project /texts/<docId>/files listing issue, which itself needs a foreign docId). (2) \"the account master Drive folder\" is not reachable this way \u2014 a folder id cannot be streamed with ?alt=media, so the exposure is individual files, not folder traversal. Also note the attack only works when the two projects share the same OWNER, since the mint uses the owner's Drive token and a different owner's file id 404s at Google. Correct scope of the defect: mintTextfileUrl's fileId is never related to a project at mint or at serve, so assignTexts in one project yields read access to any app-created file id the member can name anywhere in that owner's estate, for up to 400 days."
}
```

## a72926f0da0c94e11

```json
{
  "reasoning": "Tried to refute and could not; every quoted line is accurate and the sequence traces end to end.\n\nMINT \u2014 worker/src/v1.js:3805-3831. `const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts'); if (!ctx) 401; if (!ctx.ok) not_found; const r = ctx.owner;` then, after only `if (!body.audioFileId && !body.flextextFileId && !body.promptFileId) return j({ error: 'nothing_to_mint' }, 400, ...)` (3815) and an instance lookup bound to `r.researcher_id` (the OWNER's account, never `ctx.project_id`), `const audioUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.audioFileId, '', ttlMs, scope, ctx.caller.researcher_id);` (+ flextextFileId with scope, promptFileId with scope null). `mintTextfileUrl` (1209-1215) validates nothing: `if (!fileId) return null; const tk = { r: researcherId, f: fileId, x: extract || '', e: Date.now() + (ttlMs || ...) }`. `clampTtlDays` (1186-1190) permits 7..400 days with no member-specific clamp. The minted URLs are returned to the caller at 3831.\n\nREDEMPTION \u2014 1850-1905. Owner is resolved from `tk.r`; `if (tk.i)` only checks that instance is live and belongs to tk.r; the member gate is `SELECT 1 AS ok FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?` (or, for an unscoped token, `JOIN project p ... WHERE pm.researcher_id=? AND p.owner_id=?`). Neither query mentions `tk.f` or `tk.d`. Then `fetch('.../files/' + encodeURIComponent(tk.f) + '?alt=media', { Authorization: 'Bearer ' + access })` with `access = await driveAccessToken(env, owner)` streams to a header-less GET. So the file is never related to a project at mint or at serve \u2014 the tk.m mechanism is a revocation gate, not a scoping gate.\n\nNO GUARD ELSEWHERE \u2014 authMember (543-648) derives project_id only from instance.project_id / crowd_recorder.project_id / target.project and never sees a file id; there is no outer guard on the `/v1/instances` block (3499-3502); check-project-scoping.sh enforces only call-site shape (`!ctx.ok`), no-403 denial, no authResearcher regression under /v1/instances, and owner-only wipe/force-remove \u2014 nothing about file ids.\n\nPHASE C REGRESSION, NOT PRE-EXISTING \u2014 the contrast is still in the file: `GET /v1/researcher/drive-file/<fileId>` (3387) is still `const r = await authResearcher(request, env);`, so an arbitrary id there reaches only the CALLER's own Drive and is harmless. Substituting `const r = ctx.owner` under a member-reachable guard is exactly what turns an unvalidated body id into a read of the owner's Drive.\n\nNOT DELIBERATE \u2014 plans/project-split.md:1707-1714 states the intended rule the shipped code only half-implements: \"The file id comes straight from the request body at all three mint sites ... Put the scope INTO the token (owner, project, instance, doc, granting member, jti), re-check against live D1 at serve, and clamp member-minted TTLs well below the 400 days clampTtlDays allows.\" Only the minter stamp and instance scope shipped.\n\nREACHABILITY (the lens asked for) \u2014 the path is reachable. Multi-project-per-owner is real (reconcileProjects inserts a project per Drive project folder) and `drive.file` (1998) spans the owner's whole app-created estate, so a member of P minting a Q file id is a genuine cross-project read. Concrete sequence: M is a member of the same owner's projects P and Q, legitimately learns a Q file id from `GET .../texts/<docId>/files` (3662-3690) while in Q, is removed from Q but keeps assignTexts in P, then POSTs `{flextextFileId:'<Q id>', ttlDays:400}` to a project-P instance's `.../assignment/finish` and fetches the returned URL with no headers. That is precisely the post-removal tail the tk.m comment (1866-1897) claims to close, defeated because the gate asks about the SCOPED INSTANCE's project rather than the file's. It violates the stated rule that a member reaches \"nothing outside\" their project.",
  "refuted": false,
  "correction": "The mechanism holds, but two claims in the write-up are overstated and should not be carried into a fix. (1) This path supplies NO enumeration primitive: Drive file ids are unguessable and `/v1/researcher/drive-estate` (2869-2871) uses `authResearcher`, so it lists the CALLER's own Drive, not the owner's. The attacker must already know a foreign file id \u2014 realistically ids retained from a project they were removed from, or via the separate cross-project `/texts/<docId>/files` listing, which itself needs a foreign docId (docIds are `crypto.randomUUID()`). (2) The \"account master Drive folder\" is not reachable this way \u2014 a folder id cannot be streamed with `?alt=media`, so the exposure is individual files, not folder traversal. Also, the two projects must share the same OWNER: the mint uses the owner's Drive token, and another owner's file id 404s at Google under drive.file. Correct scope of the defect: `mintTextfileUrl`'s fileId is never related to a project at mint or at serve (same at 3881 /adopt and 3921 /move), so assignTexts in one project yields read access to any app-created file id the member can name anywhere in that owner's estate, for up to 400 days."
}
```

## a56de799829fc6807

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED \u2014 the code says what the finding says, no guard exists anywhere on the path, and the sequence traces end to end.\n\n1) The route (worker/src/v1.js:3806-3828). `authMember(request, env, { instance: instanceId }, 'assignTexts')` resolves the project from the instance row, then `const r = ctx.owner;` is the PROJECT OWNER. The only validation before minting is `if (!body.audioFileId && !body.flextextFileId && !body.promptFileId) return j({ error: 'nothing_to_mint' }, 400, ...)` (3815) and `clampTtlDays` (7..400 days, v1.js:1204-1208). The three ids go straight through:\n  3827: `const flextextUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.flextextFileId, '', ttlMs, scope, ctx.caller.researcher_id);`\nThe `inst` lookup at 3810 validates only the INSTANCE (`instance_id=? AND researcher_id=?`); nothing relates `body.*FileId` to that instance's Drive folder, to the docId's text folder, or to the project subtree.\n\n2) The mint (v1.js:1209-1210). `if (!fileId) return null;` is the whole of the file validation; `f: fileId` is copied verbatim into the token and the token is encrypted, not authenticated against any Drive parentage.\n\n3) The redemption (v1.js:1850-1918). Checks are: token decrypts, `tk.e > now`, owner row still has `drive_refresh_enc`, and \u2014 for v2 \u2014 `SELECT instance_id FROM instance WHERE instance_id=? AND researcher_id=? AND revoked=0` on `tk.i`. Then `driveAccessToken(env, owner)` and `fetch('\u2026/files/' + encodeURIComponent(tk.f) + '?alt=media')` (1899-1902). The scope check relates the token to the INSTANCE only; `tk.f` is never related to anything. The request needs no headers at all.\n\n4) The minter check does NOT save it, and this is the strongest evidence the gap is real rather than theoretical. The I2 grant re-check at 1887-1893 is `SELECT 1 FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?` bound to `(tk.m, tk.i)` \u2014 i.e. \"is the minter still a member of the project containing the SCOPED INSTANCE\", never \"is the FILE in that project\". For a token minted through device D_P in project P by member M, that join passes as long as M remains a member of P, regardless of which project the file lives in.\n\nConcrete sequence (no dependence on any other finding): owner O runs projects P and Q. M is a member of Q with drive:read and legitimately calls `GET /v1/instances/<D_Q>/texts/<docId_Q>/files`, which returns `files: [{ id, name, size, \u2026 }]` (v1.js:3706-3712) \u2014 M now holds raw Drive file ids for Q's texts. O later removes M from Q. That removal correctly kills M's outstanding Q tokens: their `tk.i` is a Q instance and the 1887 join now fails \u2192 410, which is exactly the \"revocation that leaves a 90-day tail is not revocation\" property the comment at 1874-1886 was written to guarantee. M, still a member of P with assignTexts, then POSTs `/v1/instances/<D_P>/texts/<any docId>/assignment/finish {flextextFileId:'<Q file id>', ttlDays:400}`. authMember passes (D_P is in P, caps.assignTexts true). A fresh v2 token is minted with `r = O.researcher_id`, `i = D_P`, `m = M`. Redeeming it: D_P is live and belongs to O (passes), M is still a member of P and D_P is in P (passes), and the worker streams Q's file bytes under O's refresh token to a header-less GET. The I2 revocation mechanism is defeated by re-minting through a project the ex-member still belongs to.\n\nThis violates the stated rule \"A member reaches everything in their project, nothing outside it\" and I4 (an unresolvable relationship \u2014 file\u2192project \u2014 is not checked at all rather than denied). The same uncontained pattern appears at 3881 (/adopt) and 3921 (/move), so a fix belongs in mintTextfileUrl's callers or in mintTextfileUrl itself, not at one site.\n\nRefutation attempts that failed: (a) `GET /v1/researcher/drive-file/<fileId>` (3387-3392) is NOT an equivalent pre-existing primitive \u2014 it calls `authResearcher` and `driveAccessToken(env, r)` with the CALLER's own row, so a member reading it gets 404 from Google for the owner's files; the finish/move/adopt mint is the only member-reachable route that streams the OWNER's Drive bytes by a caller-chosen id. (b) The `scope` at 3825 does not contain the file \u2014 it only ties the token to an instance. (c) `check-project-scoping.sh` enforces only that authMember is called and guarded on `!ctx.ok`; it says nothing about what the route then does with caller-supplied ids. (d) drive.file scope bounds the blast radius to the owner's app-created estate, which is precisely \"every project of that owner\" \u2014 not a mitigation for the project boundary.\n\nCaveat worth stating in the fix ticket (it lowers severity but does not refute): the attacker must already hold a file id from outside their project. Drive file ids are unguessable, so this is an access-retention/lateral-read escalation by someone who has or once had sight of those ids \u2014 which is exactly the threat model the code's own I2 comment adopts."
}
```

## a84e80bdd26f9a5e0

```json
{
  "refuted": true,
  "reasoning": "The MECHANISM described is accurately quoted \u2014 v1.js:3765-3776 does take `let parent = String(body.originalsFolderId || '');`, checks only non-emptiness (`if (!parent) return j({ error: 'bad_folder' }, 400, \u2026)`), and passes it straight into `parents: [parent]` on a resumable create signed with `Authorization: 'Bearer ' + access` where `access = await driveAccessToken(env, r)` and `const r = ctx.owner`. There is no comparison to the device folder, the text folder, or `project.drive_folder_id`.\n\nBut the finding is not a demonstrable escalation, because the step that makes it one \u2014 \"M learns a folder id outside P\" \u2014 has no path in the API, and the two vectors the finding names for it do not do what it says:\n\n1. `GET .../texts/<docId>/files` (v1.js:3662-3709) returns the folder for the docId THE CALLER SUPPLIES. `authMember(request, env, { instance: instanceId }, 'drive:read')` resolves the project off the instance row, so the caller can only address instances in their own project; the docIds a member legitimately holds are their own project's texts, and the `folderId`/`originalsFolderId` returned for those are inside P. To get a foreign folder id out of that route you must already possess a foreign docId \u2014 and a docId is `crypto.randomUUID()` (docs/js/researcher-panel.js:3220), not guessable or enumerable. Same for `adopt`'s `folderId` (v1.js:3884): it is the folder of the docId you passed in.\n\n2. Nothing member-reachable discloses the owner's master folder or another project's folder id, and the omissions look deliberate. `GET /v1/projects` (v1.js:2395-2415) returns `drive_folder_id` ONLY in the `owned` list; the `joined` list is mapped down to `{ project_id, name, owner_id, caps }` \u2014 a member is not even told their own project's Drive folder id. `GET /v1/researcher/drive-estate` and `/drive-snapshot` (v1.js:2839, 2870) \u2014 the only routes that emit `masterFolderId` and a full folder listing \u2014 are `authResearcher` and act on `driveAccessToken(env, r)` for the CALLER's own account, so a member sees their own Drive, never the owner's. `/v1/projects/<id>/members` is owner-only (`if (!ctx.ok || !ctx.isOwner) return not_found`).\n\n3. The cross-owner variant fails at Drive anyway: the create is signed with project P's owner's token, so a folder id belonging to a different Google account is a 404 from Drive and the route answers 502 `drive_error`. The only viable target is a folder in the SAME owner's Drive but outside P, which reduces to case 1/2 above \u2014 out-of-band knowledge (e.g. a researcher previously removed from another of that owner's projects), not a request sequence.\n\n4. Trusting a client-echoed folder id with an existence-only check is the pre-existing, documented v167 dedupe contract, not something Phase C introduced: `driveEnsureTextFolder(access, deviceFolder, docId, title, knownId)` (v1.js:1138-1152) does exactly the same with `body.folderId` \u2014 `files.get` for `id,trashed`, no parent check, \"A REMEMBERED id beats searching\". Phase C changed who may call these routes, not how `parent` is handled.\n\nSo this is a hardening gap (a confused-deputy write that would be closed by verifying the parent is under `project.drive_folder_id`), not a defect I can turn into a concrete request sequence from a member's legitimate position \u2014 which is the bar the audit sets. Per the instruction to prefer fewer real findings and default to refuted when the sequence cannot be traced, I refute it.",
  "correction": "The unchecked `parents: [body.originalsFolderId]` at v1.js:3765-3776 is real and worth hardening (compare the parent chain against `project.drive_folder_id`, the way `driveEnsureChildFolder` is parent-scoped). What is not supported is the claimed escalation: a member cannot obtain a folder id outside their project through any route \u2014 `joined` projects are returned WITHOUT `drive_folder_id`, `drive-estate`/`drive-snapshot` run against the caller's own Drive, and `/files`/`adopt` only echo back the folder for a caller-supplied `crypto.randomUUID()` docId. A folder in a different owner's Drive is rejected by Drive itself, since the upload is created with project P's owner's token. The residual risk is limited to an attacker who already knows a same-owner, out-of-project folder id from out-of-band history (e.g. a prior membership), and the impact is planting a file, not reading one."
}
```

## a7bcd9c7241ba8201

```json
{
  "refuted": false,
  "reasoning": "Verified verbatim at worker/src/v1.js:3748-3771. `const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');` ... `const r = ctx.owner;` ... `const access = await driveAccessToken(env, r);` ... `let parent = String(body.originalsFolderId || '');` ... `if (!parent) return j({ error: 'bad_folder' }, 400, ...)` ... `body: JSON.stringify({ name, mimeType: mime, parents: [parent], appProperties: { flextextRole: role } })`. The caller-supplied parent is never sanitised or compared to `inst.oauth_folder_id` (the device folder), to the text folder returned by `assignment/begin`, or to `project.drive_folder_id`. Reachability traced end to end and no guard found anywhere in the chain: (1) route entry at v1.js:3500-3502 (`if (seg.length >= 3 && seg[1] === 'instances')`) has no earlier device-token gate \u2014 this is a researcher-session route; (2) authMember (v1.js:543+) returns `{ok:true, owner}` for a NON-OWNER member whose project_member caps include assignTexts, and `owner` is the project owner's full researcher row used for `driveAccessToken`; (3) the instance-ownership SELECT binds `researcher_id=?` to `r.researcher_id` \u2014 the OWNER, not the caller \u2014 so a member of the project passes it (instance.researcher_id is the maintained denormalisation of project.owner_id); (4) the chunk relay at v1.js:3791-3804 only checks `sess.rr !== r.researcher_id`, again the owner, so the member passes; (5) neither `/assignment/finish` nor `relayDriveChunk` nor check-project-scoping.sh re-checks placement. Concrete sequence: member M with assignTexts on device D_P in project P sends POST /v1/instances/D_P/texts/<docId>/assignment/upload/start {kind:'flextext', name, size, originalsFolderId:'<foreign id>'} -> 200 uploadId, then PUT .../assignment/upload/chunk with the x-fx-upload header -> the bytes land in that folder tagged flextextRole:'source-flextext', written with the project owner's Drive token. This violates the stated rule that a member reaches nothing outside their project and never the account master folder. Not deliberate: the `kind === 'consent-prompt'` branch is the only one that computes its parent server-side, showing the others simply trust the panel. Git check (`git show 4fa6ac1^:worker/src/v1.js` line 3457) confirms the line predates Phase C, but pre-Phase C the caller was necessarily the owner so an arbitrary parent was self-inflicted; Phase C is what makes it reachable by a different principal acting with the owner's credentials.",
  "correction": "The claim about the code and its reachability holds; one detail of the attack narrative needs correcting, and it makes the surrounding picture slightly different rather than weaker. GET .../texts/<docId>/files (v1.js:3662-3681) does leak folder ids outside the project, but not because the caller's own project row exposes them: that route resolves the folder with `spaces=drive` and a bare `appProperties has { key='flextextDoc' and value='<docId>' }` query with NO parent or project constraint, using instanceId only to confirm the device belongs to the owner. So it returns a foreign `folderId`/`originalsFolderId` only if the member already knows a docId from another project \u2014 an unguessable id, same as the folder id itself. The variant needing no prior knowledge is passing Drive's `root` alias (or any id learned out of band) as originalsFolderId, which plants the file in the owner's My Drive outside every project folder. So: the defect is the total absence of a subtree containment check on a caller-supplied `parents` used with the owner's token; the \"plant into another project's folder so its device treats it as source material\" impact additionally requires a foreign docId or folder id, which the system does not hand members but also does nothing to withhold structurally."
}
```

## a56b385c52c2b36c7

```json
{
  "refuted": false,
  "reasoning": "The code says exactly what the finding claims. worker/src/v1.js:3748 gates on authMember({instance},'assignTexts') and sets `const r = ctx.owner`; the only other binding is `SELECT ... FROM instance WHERE instance_id=? AND researcher_id=?` bound to the OWNER. Then at 3765: `let parent = String(body.originalsFolderId || '');`, overridden only for kind==='consent-prompt', and at 3771-3777 it is passed straight through as `parents: [parent]` on a resumable create with `Authorization: 'Bearer ' + access` where `access = await driveAccessToken(env, r)`. No files.get, no parentage check, no comparison with inst.oauth_folder_id, the text folder, or ctx.project_id. Nothing downstream re-checks: the chunk relay (3791) and finish (3806) validate only `sess.rr === r.researcher_id`.\n\nThe id-acquisition step is also real and in-code: GET .../texts/<docId>/files (3663) resolves the folder with an ACCOUNT-WIDE tag search \u2014 `?spaces=drive&orderBy=createdTime&fields=files(id)&q=appProperties has {key='flextextDoc' and value='${docId}'}` with no parent or project filter \u2014 and returns `{ folderId, originalsFolderId }`. driveEnsureTextFolder's own comment (1134-1136) states the property: \"The tag search is scoped to trashed=false but NOT to the parent\".\n\nConcrete reproducing sequence: owner O owns projects P and Q; researcher M is a member of P with assignTexts and of Q with drive:read only. (1) GET /v1/instances/<Q-instance>/texts/<docQ>/files -> Q's originalsFolderId F. (2) POST /v1/instances/<P-device>/texts/<anyDoc>/assignment/upload/start {kind:'flextext', name:'x.flextext', size:N, originalsFolderId:'F'} -> 200 + uploadId. (3) PUT .../assignment/upload/chunk. The file lands in project Q's text folder tagged flextextRole:'source-flextext' under the owner's token, though M holds no write capability in Q. The same works for a researcher removed from Q who kept the remembered id \u2014 removing a member deletes project_member and member_key rows but nothing invalidates a folder id they already saw. Authorization was decided for P while the effect landed in Q, which is the \"member reaches nothing outside their project\" rule broken in effect.\n\nThis is a Phase C regression, not pre-existing behaviour: before the conversion at 3748 the caller was always the Drive owner, so an unvalidated parent was self-directed. The sibling assignment/begin route has the same hole one level up (3737 -> driveEnsureTextFolder's knownId path, 1147-1153, which trusts any caller-named folder that merely exists and is untrashed).\n\nI could not confirm one element of the stated attack and it should be corrected rather than treated as refuting the defect (see correction).",
  "correction": "Two narrowings to the finding as written. (1) The claimed id-acquisition path for the ACCOUNT MASTER folder is wrong: GET .../texts/<docId>/files and adopt return the TEXT's folder and its originals child only, never the uploads-master or the project folder. The only routes that surface masterFolderId / project drive_folder_id are GET /v1/researcher/drive-snapshot (v1.js:2839, `masterFolderId: (master && master.id) || ''`), GET /v1/researcher/drive-estate, and GET /v1/projects (2396) \u2014 the first two use authResearcher + driveAccessToken(env, r) on the CALLER's own Drive, and the third returns drive_folder_id only for projects the caller owns. So a member has no API path to the owner's master or project folder id; the \"write outside every project\" variant is unsupported today, though it would work if such an id ever leaked. (2) The demonstrable blast radius is therefore: any folder in the owner's app-created (drive.file) estate whose id the member can learn \u2014 in practice another project's text folder or originals folder, reached via the account-wide docId tag search at 3663 or remembered from a prior/parallel membership. That is still a cross-project write with no grant on the destination, so the defect stands; only the master-folder claim should be dropped."
}
```

## a66fdf0cdee63eef2

```json
{
  "refuted": false,
  "reasoning": "Verified in the code. worker/src/v1.js:2264-2281 is the only handler for POST /v1/researcher/keys; it calls authResearcher (not authMember), looks the instance up itself, and returns 404 not_found when the row is absent (2274) but 403 forbidden when the row exists and the caller is not the project owner (2281). j() passes the status through unmodified, and authResearcher admits any researcher holding a valid credential, so a caller who is a member of nothing reaches line 2281. That is a second authorization authority (against I1) answering with a status the design forbids, and the converted sibling one route below proves the intended shape: DELETE at 2371 does `if (!ctx.ok || !ctx.isOwner) return j({ error: 'not_found' }, 404, ...)`. check-project-scoping.sh genuinely cannot catch it: its 403 check greps only lines containing `if (!ctx.ok`, and its authResearcher check is bounded by `awk -v s=\"$start\" 'NR>s && NR<s+700'` anchored at the /v1/instances/<id> block, which does not cover line 2265. git blame shows the route is Phase B code (2933d18, 2026-08-18) that the Phase C sweep did not convert, so it is an omission rather than a documented exemption \u2014 and the authMember docblock at 526-529 states the exact opposite rule. The instance SELECT at 2272 also omits the `AND revoked=0` that authMember applies at line 555. No upstream guard prevents any of this and the path is reachable, so none of the refutation conditions hold.",
  "correction": "Two parts of the finding are overstated and should not be carried into a fix. (1) \"Enumerable\" is too strong: instance_id is crypto.randomUUID() (v1.js:3463), so blind guessing is infeasible \u2014 the oracle only confirms ids the caller already possesses. The realistic sequence is a removed member: after removal every converted route answers not_found for their old project's instances, but POST /v1/researcher/keys still answers 403, confirming those devices exist and remain owned. That is a genuine but low-severity disclosure, not id enumeration. (2) The sub-claim that the route wrongly \"skips the not_migrated gate\" is incorrect \u2014 docs/js/researcher.js:393 calls this route during the pre-migration key backfill, so requiring project.drive_folder_id here would break the migration the route exists to serve. There is also no privilege escalation: `r.researcher_id !== proj.owner_id` is strictly narrower than authMember, since it admits only the owner. (Separately worth noting, though outside this finding: that owner-only check contradicts the route's own docblock at 2253-2257, which describes \"a member with createInvites\" minting device keys \u2014 under the current code no member can ever write a grant.) The correct fix is the sibling's shape: `const ctx = await authMember(request, env, { instance: instanceId }, null); if (!ctx) 401; if (!ctx.ok || !ctx.isOwner) not_found;` \u2014 noting it also newly denies grant writes for revoked instances via authMember's `AND revoked=0`."
}
```

## ad303dac2cd5252eb

```json
{
  "refuted": false,
  "reasoning": "Confirmed by direct code trace. worker/src/v1.js:2265 dispatches POST /v1/researcher/keys and resolves auth with `const r = await authResearcher(request, env);` \u2014 no authMember, so this route is a second authorization authority (violates I1). Line 2271-2273 looks the instance up (`SELECT instance_id, project_id, researcher_id FROM instance WHERE instance_id=?` \u2014 note: no `AND revoked=0`, unlike authMember:555) and answers `if (!inst) return j({ error: 'not_found' }, 404, ...)`; line 2281 answers `if (r.researcher_id !== proj.owner_id) return j({ error: 'forbidden' }, 403, origin, env);`. The two answers are distinguishable, which authMember's own header (v1.js:526-529) declares forbidden: \"AND DENIAL IS INDISTINGUISHABLE FROM ABSENCE... A distinct 'forbidden' would turn every endpoint into an oracle for which project and instance ids exist.\" The Phase C commit 7fd1132 added the sibling DELETE immediately below with the correct shape (v1.js:2371 `if (!ctx.ok || !ctx.isOwner) return j({ error: 'not_found' }, 404, ...)`) and left the POST untouched.\n\nReachability holds. handleV1 (v1.js:1836) has no global gate; originAllows is used only to populate the CORS header (v1Cors:287) and never to reject, so a curl request with no Origin works. There is no isApproved() check on this route (the isApproved call sites are 2626, 2651, 3431, 3457, 4464, 4481). authResearcher only needs x-fx-researcher + x-fx-secret matching any researcher row. The 403 at 2281 is evaluated BEFORE the owner_grant_required check at 2283, so a probe with grants:[{researcher_id:'x',wrapped_ki:'y'}] returns the oracle answer with zero write side effects. Concrete sequence: authenticated researcher A (member of nothing) POSTs {instance_id:'<guess>', grants:[{researcher_id:'x',wrapped_ki:'y'}]} \u2014 404 for a nonexistent id, 403 for one belonging to another project.\n\nThe containment claim is also verifiably false. check-project-scoping.sh's oracle check is `if grep 'if (!ctx.ok' \"$W\" | grep -q '}, 403,'` (authMember guard lines only) and its no-revert check is `awk -v s=$start 'NR>s && NR<s+700 && /const r = await authResearcher/'` with start=3499 (the /v1/instances/<id> block). Line 2265 is outside both, so the script prints \"PASS \u2014 project data is reachable only through a resolved grant\" with this route present \u2014 precisely the \"route number seventeen reached for authResearcher\" failure its own header says it exists to catch.\n\nNone of the refutation grounds apply: the code says what is claimed, no upstream guard prevents it, the sequence is performable, the path is reachable, and the behaviour contradicts rather than implements the stated design rules.",
  "correction": "The mechanism is confirmed, but the impact should be stated narrowly. (1) instance_id is crypto.randomUUID() (v1.js:3463), so this is an existence-CONFIRMATION oracle for an id the caller already holds or observed, not practical brute-force enumeration of the id space. (2) The route remains owner-only, so nothing is widened \u2014 the defect is the second authority (I1) plus the distinguishable denial, not privilege escalation. (3) The oracle is not unique to this route: v1.js:4318 `if (asResearcher && inst.researcher_id !== asResearcher.researcher_id) return j({ error: 'forbidden' }, 403, ...)` has the same 403-vs-404 shape inside the instances block and also escapes the script (it binds `asResearcher`, not `const r = await authResearcher`, and sits past the 700-line scan window), so fixing 2281 alone does not close the oracle. (4) Any conversion to authMember must account for one real behaviour change: authMember's instance lookup filters `AND revoked=0` (v1.js:555) while this route does not, so writing grants for a revoked instance (e.g. a client-side key-migration sweep) would begin returning not_found."
}
```

## ac0d480ee9392c8d3

```json
{
  "refuted": false,
  "reasoning": "Confirmed by direct reading; I could not refute it on any of the offered grounds.\n\nThe code says what the finding claims. v1.js:2264-2281: the route resolves identity with `const r = await authResearcher(request, env)`, then does its own project resolution and its own ownership decision \u2014 `if (r.researcher_id !== proj.owner_id) return j({ error: 'forbidden' }, 403, origin, env);` \u2014 while a missing instance answers `j({ error: 'not_found' }, 404, ...)` at :2273. Two distinguishable responses, both reached before any DB write, so the probe is side-effect-free and repeatable.\n\nThe request sequence reproduces. authResearcher (v1.js:398) needs only x-fx-researcher + x-fx-secret and does not require approved=1. POST /v1/researcher/keys with {instance_id:'<guess>', grants:[{researcher_id:'x', wrapped_ki:'y'}]} passes the bad_body check, hits the instance lookup, and returns 404 for a nonexistent id or 403 for one owned by another researcher. Nothing earlier in handleV1 (entry :1836) gates the route; there is no global approved or membership check, and no earlier route pattern matches this path.\n\nThe guard script really is blind to it, as claimed. check-project-scoping.sh check #3 is `grep 'if (!ctx.ok' \"$W\" | grep -q '}, 403,'` \u2014 it inspects only authMember guard lines, so a route with no authMember call cannot trip it. Check #4 is bounded by `awk -v s=\"$start\" 'NR>s && NR<s+700 && /const r = await authResearcher/'` over the /v1/instances/<id> block, and this route lives at /v1/researcher/keys, outside that window.\n\nThe AND revoked=0 sub-claim also holds: authMember at v1.js:555 reads `SELECT project_id, researcher_id FROM instance WHERE instance_id=? AND revoked=0`, while this route's lookup at :2272 has no such clause.\n\nIt is not deliberate. The sibling DELETE /v1/researcher/keys, added by Phase C, is written the correct way at v1.js:2369-2371: `const ctx = await authMember(request, env, { instance: instanceId }, null); if (!ctx) 401; if (!ctx.ok || !ctx.isOwner) return j({ error: 'not_found' }, 404, ...)`. And the POST route's own doc comment at :2256 says \"a member with createInvites cannot mint a device key the owner cannot read, because the worker will not store the set at all\" \u2014 which presumes a member with createInvites CAN post grants; the code is owner-only, so that sentence describes behaviour the route does not implement. createInvites is a real capability (validateCaps, :484) already used as a needCap at :3530, so as written a member with createInvites can mint an invite but cannot write its key grant. That inconsistency corroborates that the route was missed by the 19-site conversion (git log -S shows it originates in Phase B commit 2933d18) rather than deliberately excluded.\n\nOne calibration caveat, which does not refute the finding but should temper its title: instance ids are random GUIDs, so this is an existence oracle for ids the caller already holds, not a way to walk the id space. \"Enumerable\" overstates the reachable impact. The repo itself uses that framing though (check-project-scoping.sh: \"would turn the endpoint into an oracle for which instance and project ids exist\"), and the I1 half of the claim \u2014 a second authorization authority outside authMember, invisible to the containment script \u2014 is unqualified and correct.\n\nAlso worth noting for triage, not as part of this finding: the same 403-vs-404 shape exists pre-Phase-C at v1.js:4316-4318 in the poll route (`if (!inst) ... 404` then `if (asResearcher && inst.researcher_id !== asResearcher.researcher_id) ... 403`), which evades check #4 too because it binds `asResearcher` rather than `const r = await authResearcher`. That means this route is not the only such oracle, but it does not make the finding wrong at the location cited."
}
```

## ab98b1d53630728fc

```json
{
  "refuted": false,
  "reasoning": "Traced every line and could not refute it.\n\nWRITE PATH (v1.js:2272-2300). `const inst = SELECT instance_id, project_id, researcher_id FROM instance WHERE instance_id=?`; then `const proj = inst.project_id ? SELECT project_id, owner_id FROM project ... : { project_id: null, owner_id: inst.researcher_id };` and the INSERT binds `String(proj.project_id || '')`. The `''` is applied to EVERY row in the grant set, grantee rows included \u2014 the only authorization checks are `r.researcher_id !== proj.owner_id -> 403` and the owner-copy presence check; there is no membership or project check on the grantee. The comment at 2292-2299 itself flags the hazard: \"'' means \\\"no project yet\\\" ... \u26a0 Phase C must treat '' as unassigned rather than as a project id.\"\n\nDELETE PATH (v1.js:2497-2508). The batch is `DELETE FROM project_member WHERE project_id=? AND researcher_id=?` plus `DELETE FROM member_key WHERE project_id=? AND researcher_id=?`, both bound to the real `projectId`. A row carrying `''` cannot match. `grants_removed` is that statement's `meta.changes`, so the response is literally `{ok:true, removed:1, grants_removed:0}` \u2014 success reported, nothing revoked. The comment directly above states the intent this violates: \"Without that, \\\"removed\\\" would mean \\\"no longer listed\\\" while they still hold every wrapped Ki.\"\n\nREAD PATH (v1.js:2311-2315). `SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=?` (optionally `AND instance_id=?`). No membership join, no project scoping \u2014 deletion is the only control, and it missed.\n\nNO REPAIR ELSEWHERE. grep over worker/ and docs/js/ finds member_key touched only at 2290 (INSERT OR REPLACE), 2312/2314 (reads), 2377/2379 (owner's per-instance DELETE, scoped by instance_id+researcher_id \u2014 not invoked by member removal), and 2501. There is no `UPDATE member_key` anywhere and no trigger in migrate-projects.sql / schema-current.sql, so nothing ever rewrites the sentinel once the instance is later stamped. check-project-scoping.sh contains no reference to member_key or project_id.\n\nPRECONDITION IS REACHABLE, which is the part I tried hardest to break. v1.js:3471 `INSERT INTO instance (instance_id, researcher_id, type, nickname, desired_blob, desired_rev, revoked, created_at, estate)` omits project_id, and migrate-projects.sql:64 `ALTER TABLE instance ADD COLUMN project_id TEXT` gives it no default \u2014 so every new instance is NULL. Neither stamper closes it for a migrated owner: backfillProjectsFor's `UPDATE instance SET project_id=? WHERE researcher_id=? AND project_id IS NULL` (1713) is only reached when the caller has NO project row (the lazy-mint guard at 2628-2630 `if (!mine) await backfillProjectsFor(...)`), which is false for an owner who already has migrated project P; reconcileProjects (1806-1820) only updates rows `WHERE oauth_folder_id IS NOT NULL` whose folder sits under a project folder, and it runs only inside GET /v1/researcher/drive-estate (2880-2907), not on the 12s /v1/researcher poll. A device paired before it has ever created a Drive folder therefore stays NULL indefinitely. POST /v1/projects/P/members' not_migrated gate (2473-2479) constrains the PROJECT's drive_folder_id, not the instance's project_id, so it does not block the sequence.\n\nThe whole sequence is performable today as raw HTTP with the owner's own credentials \u2014 no Phase D UI required \u2014 so it is a concrete request sequence, not a hypothetical.",
  "correction": "The mechanism holds; two claims in the write-up are overstated and should be corrected in the report rather than treated as refutations.\n\n1. IMPACT. \"M ... can decrypt that device's E2EE material for as long as the row exists\" overreaches. Once the project_member row is deleted, authMember denies M every instance route (the dual-read branch at 588-590 grants owner-equivalence only to the instance's own researcher_id, and M is not it), so M has no worker-mediated route to the ciphertext and no direct Drive access. The demonstrable defect is narrower and still real: the worker keeps HANDING OVER the wrapped Ki to a removed non-member via GET /v1/researcher/keys, while DELETE /v1/projects/<id>/members returns ok with grants_removed:0 \u2014 i.e. the endpoint's own stated contract (\"removed\" must not mean \"no longer listed while they still hold every wrapped Ki\") is silently unmet, and any ciphertext M cached or later obtains by other means stays readable.\n\n2. FREQUENCY. \"the DEFAULT outcome for devices created after Phase C ships\" is too strong. reconcileProjects stamps any instance that already has an oauth_folder_id inside a project folder on the next GET /v1/researcher/drive-estate, which the panel makes routinely. The durable NULL cases are (a) a device paired before its Drive folder exists (oauth_folder_id NULL, so reconcile skips it), (b) a device whose folder sits directly under master rather than in a project folder, and (c) the window between creation and the next drive-estate load. That is a narrower but still ordinary set of devices.\n\nFix direction: either scope the removal delete as `WHERE researcher_id=? AND (project_id=? OR project_id='')` restricted to instances owned by that project's owner, or \u2014 better, and matching the 2292-2299 warning \u2014 refuse to write a grant to a non-owner grantee at all while proj.project_id is null, since such a grant is unauthorizable by authMember anyway (fail-closed, I4)."
}
```

## a87fe3762279b289d

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED, with one correction to its framing. Every quoted line says what the finding claims, and the sequence is performable by the legitimate owner through the deployed API.\n\nWrite path, worker/src/v1.js:2272-2300 (POST /v1/researcher/keys): `const proj = inst.project_id ? await \u2026project\u2026 : { project_id: null, owner_id: inst.researcher_id };` then `.bind(String(proj.project_id || ''), instanceId, String(g.researcher_id), version, \u2026)`. The only authorization is `if (r.researcher_id !== proj.owner_id) return 403` \u2014 there is NO check that a grantee is a project member, so an owner can mint a grant for member M on an instance whose project_id is still NULL, and the row is stamped with the '' sentinel.\n\nInstances really are born NULL: v1.js:3470-3472 `INSERT INTO instance (instance_id, researcher_id, type, nickname, desired_blob, desired_rev, revoked, created_at, estate)` \u2014 no project_id column, deliberately (\"no `project_id` written anywhere, nothing to drift\"). project_id is filled only later by reconcileProjects (v1.js:1813-1819, keyed on oauth_folder_id, and a device folder still directly under master has `d.projectId === ''` and is explicitly \"left alone\") or by backfillProjectsFor (v1.js:1713), which after the lazy mint at v1.js:2628-2630 runs only for a researcher with no project row at all. So the NULL window is real and can persist.\n\nRemoval path, v1.js:2498-2503: `DELETE FROM member_key WHERE project_id=? AND researcher_id=?` bound to the real project GUID \u2014 '' rows do not match, and the response is still `{ok:true, removed:1, grants_removed:0}`, i.e. it reports success with no signal that grants were missed. This is the exact trap the write path's own comment warned about (\"\u26a0 Phase C must treat '' as unassigned rather than as a project id\"), handled correctly in authMember (v1.js:588-593) and NOT handled here.\n\nRead path, v1.js:2311-2315: `SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=?` behind authResearcher only \u2014 no authMember, no membership check \u2014 so a removed non-member is still served the wrapped Ki from any browser, forever. That is more than the acknowledged \"cannot un-know a cached key\" limitation at v1.js:2359-2362; the worker keeps actively handing it over.\n\nRefutations I tested and rejected: (1) nothing repairs the sentinel \u2014 member_key's PK is (instance_id, researcher_id, key_version) (migrate-projects.sql:53), no code anywhere UPDATEs member_key.project_id (only the INSERT at 2290 and DELETEs at 2377/2379/2501), and reconcileProjects writes only project/instance/crowd_recorder. (2) authMember's dual-read does block the removed member from instance ROUTES (it consults project_member not at all and only authorizes legacyOwner), but /v1/researcher/keys never calls authMember, so the key material still leaves the server. (3) The unscoped per-grant DELETE /v1/researcher/keys (v1.js:2377) can remove such a row, but it is a separate manual act; the member-removal batch is the one the code declares must not leave \"removed\" meaning \"no longer listed while they still hold every wrapped Ki (invariant I5)\". (4) The not_migrated gate (v1.js:2473-2478) constrains the PROJECT, not whether a given instance has been stamped, so it does not block the sequence.\n\nCORRECTION to the finding's severity claim only: \"instances are usually never stamped\u2026 this is the DEFAULT outcome\" is overstated. reconcileProjects runs off the estate load the panel makes constantly and does stamp instances whose device folder sits inside a project folder. The defect is a real but bounded window \u2014 grants minted before the next reconcile, or for a device folder still sitting directly under the account master (where reconcile deliberately leaves project_id alone). Within that window revocation-by-member-removal silently misses the grant and reports grants_removed:0.",
  "correction": "The mechanism is exactly as described (sentinel '' written at v1.js:2300, project-scoped delete at v1.js:2501, unscoped read at v1.js:2313-2315, no repair anywhere), but it is not the default outcome. reconcileProjects (v1.js:1813-1819) stamps instance.project_id on the estate loads the panel makes constantly, so grants minted after that carry the real project id and ARE deleted on member removal. The leak is confined to grants minted while the instance is still unstamped \u2014 a freshly created device (v1.js:3470-3472 writes no project_id), or a device whose Drive folder still sits directly under master, which reconcile explicitly leaves alone. Also worth noting: the owner does retain a working manual revocation via DELETE /v1/researcher/keys (v1.js:2377), which is not project-scoped; what is broken is the member-removal batch, which reports grants_removed:0 and gives no signal that anything was missed."
}
```

## ae7c34be02109b5b5

```json
{
  "refuted": false,
  "reasoning": "Every step of the attack is verifiable in the code and nothing guards it.\n\n1. Grant write stamps the sentinel. v1.js:2277-2279 `const proj = inst.project_id ? await ...SELECT project_id, owner_id FROM project... : { project_id: null, owner_id: inst.researcher_id };` and v1.js:2300 `.bind(String(proj.project_id || ''), instanceId, String(g.researcher_id), version, ...)`. With inst.project_id NULL the row is written with project_id ''.\n\n2. Nothing stops the grantee being a non-project researcher. The only checks on that path are `if (r.researcher_id !== proj.owner_id) return forbidden` (2281) and `if (!grants.some((g) => g && g.researcher_id === proj.owner_id && g.wrapped_ki))` (2283). There is no project_member lookup, so the owner can mint a wrapped Ki to M for a NULL-project instance.\n\n3. The NULL-project state is the DEFAULT for a new device, not a leftover. v1.js:3471 `INSERT INTO instance (instance_id, researcher_id, type, nickname, desired_blob, desired_rev, revoked, created_at, estate)` \u2014 project_id is absent, and the eager device-folder branch below it says outright \"no `project_id` written anywhere, nothing to drift.\" The only writers of instance.project_id are backfillProjectsFor (1713) \u2014 which the lazy path at 2628-2630 invokes ONLY when `SELECT project_id FROM project WHERE owner_id=?` returns nothing, i.e. never for an owner who already has the migrated project P the finding requires \u2014 and reconcileProjects (3238 / 1818), which updates only rows `WHERE oauth_folder_id IS NOT NULL`. A device that has not yet made a Drive folder is unreachable by both.\n\n4. Removal misses the row. v1.js:2500-2501 `DELETE FROM project_member WHERE project_id=? AND researcher_id=?` + `DELETE FROM member_key WHERE project_id=? AND researcher_id=?` bound to (projectId, who). 'P' != '' so 0 grant rows are deleted, and 2506-2510 still returns `{ ok:true, removed: gone, grants_removed: keys }` i.e. ok:true with grants_removed 0.\n\n5. The read path still serves it. v1.js:2312-2314 selects member_key `WHERE researcher_id=?` (optionally AND instance_id) with no membership or project join, so the removed M keeps fetching wrapped_ki.\n\n6. Nothing repairs the sentinel later: grep shows no `UPDATE member_key` anywhere in the worker, and migrate-projects.sql:53 `PRIMARY KEY (instance_id, researcher_id, key_version)` excludes project_id, so a later stamping of instance.project_id leaves the grant row at ''. That makes the fully-realistic variant work too: instance created NULL -> owner grants M -> the device uploads and reconcileProjects stamps it with P -> M has genuine member access to it -> owner removes M -> the grant row (still '') survives.\n\nThe write path's own comment (2298-2299) says \"\u26a0 Phase C must treat '' as unassigned rather than as a project id\"; the removal path at 2501 treats it as neither and simply does not match it, against the invariant stated three lines above it (\"REMOVING A MEMBER ALSO DELETES THEIR KEY GRANTS ... Without that, 'removed' would mean 'no longer listed' while they still hold every wrapped Ki\"). So it is a gap, not a deliberate design.\n\nI could not refute it on reachability, on a guard elsewhere, or on intent.",
  "correction": "Two scope corrections to the finding's impact wording, neither of which rescues the code.\n\n(a) \"can decrypt that device's E2EE material\" overstates what a removed M can newly reach through the API. If the instance is still project_id NULL, authMember's dual-read branch (v1.js:588-591) authorizes only `legacyOwner === caller.researcher_id`, so M \u2014 member or not \u2014 is denied every /v1/instances route for it. The concrete, reproducible harm is (i) revocation silently not happening: the ledger still holds the grant and GET /v1/researcher/keys still hands it to a removed non-member, so a device of M's that had never fetched it can still fetch it after removal; and (ii) in the variant where the instance is stamped with P after the grant was written, M genuinely had access while a member, and the removal that is supposed to withdraw the key leaves it readable.\n\n(b) There IS an owner-side remedy the finding does not mention: DELETE /v1/researcher/keys (v1.js:2364-2381) deletes `WHERE instance_id=? AND researcher_id=?` with no project_id predicate, so it does clear ''-stamped rows. It is a per-instance manual call, not part of member removal, and the member-removal response reports success \u2014 so it does not make the batch at 2501 correct, it just means the fix has an existing precedent to copy (drop the project_id predicate, or scope the delete by instance ids belonging to the project, and reconcile existing '' rows)."
}
```

## a9e43b1501fd279d2

```json
{
  "refuted": true,
  "reasoning": "The mechanical facts are mostly right but the defect does not follow. Verified: POST /v1/instances (v1.js:3470-3472) writes no project_id; driveEnsureDeviceFolder (v1.js:1028-1029) parents a new folder under master when no projectFolderId is passed; reconcileProjects (v1.js:1803-1804) skips containers with projectId ''; and researcher-panel.js:3042-3044 does send an empty project from the stray tab or a flat estate. But (1) the normal path stamps reliably: with a project tab open the folder is created eagerly inside that project (v1.js:3486-3494) and newDeviceModal immediately calls renderDashboard(), a full render that GETs /v1/researcher/drive-estate (researcher-panel.js:1433), which runs reconcileProjects (v1.js:2907) and stamps instance.project_id from Drive parentage; POST /v1/researcher/projects/assign (v1.js:3234-3241) stamps it directly as well. (2) The residual case is a device that is genuinely in no project \u2014 under master in Drive, shown as 'not in a project yet' in the panel \u2014 and every consequence is a DENY: authMember's legacy branch admits only instance.researcher_id, so a member gets 404. That is precisely what I4 (unresolvable steps deny) and 'a member reaches everything in their project, nothing outside it' require; no member gains reach, nothing is enumerable, and the owner's access is exactly pre-Phase-C behaviour for their own row. (3) Member-facing surfaces agree with the denial rather than contradicting it: the minter-grant check joins project_member to instance on project_id (v1.js:1886-1888), so a NULL-project instance is invisible there too. (4) The member_key '' sentinel consequence is the companion finding's claim, not independent evidence here. What remains is that the comment at v1.js:585-587 ('It closes on its own') overstates the case \u2014 a stale comment, which the brief excludes from findings.",
  "correction": "Accurate: instance creation does not write project_id, the lazy device-folder path parents under the account master rather than a project folder (the route comment at v1.js:3480-3484 is stale), and reconcileProjects leaves such rows NULL. Inaccurate: this is not 'every device from now on' and it is not a defect. When the panel has a project tab open it sends projectFolderId, the folder is created inside the project, and the drive-estate full render fired right after creation runs reconcileProjects and stamps instance.project_id; /projects/assign stamps it too. In the only case that stays NULL, the device really is in no project (Drive parentage and the panel both say so), and every resulting behaviour is a fail-closed 404 for members, which is what the design rules mandate. The legacy branch persisting for genuinely unassigned devices makes the 'it closes on its own' comment wrong, not the authorization wrong. A separate, real issue found while tracing (not this finding): reconcileProjects only emits writes for devices whose folder sits in a project, so a device folder dragged OUT of a project back under master keeps a stale project_id and remains reachable by that project's members."
}
```

## aad8b9dfdd3cd86f6

```json
{
  "refuted": true,
  "reasoning": "I traced every link of the chain. The individual code facts quoted are accurate, but the finding's causal claim (\"nothing reliably stamps it\", \"the live authorization path for every device created from now on\") does not survive the mainline path, and the harm it alleges is the fail-closed behaviour the design mandates.\n\nWHAT IS TRUE\n- worker/src/v1.js:3470-3472 \u2014 `INSERT INTO instance (instance_id, researcher_id, type, nickname, desired_blob, desired_rev, revoked, created_at, estate) VALUES (?,?,?,?,?,0,0,?,?)` \u2014 no `project_id` column, so a fresh instance really is NULL.\n- v1.js:2628-2630 \u2014 the lazy mint is gated on `if (!mine)` (no project row at all), so it never re-runs for an already-backfilled owner.\n- v1.js:1028-1029 \u2014 `const parent = projectFolderId || (await drivePriorProjectParent(access, existingId)) || await driveMasterFolder(access);` and the device lazy call sites (3736, 4130, 4231) pass no `projectFolderId`, so a folder minted on first upload does land under master. `flextextProject` is indeed never written (only `flextextRole:'project'` at 1007/3167).\n- v1.js:1804 \u2014 `if (!d.projectId || !d.folderId) continue;` really does skip a container sitting directly under master.\n- researcher-panel.js:3043 \u2014 `const intoProject = (currentProject && currentProject !== STRAY_TAB) ? currentProject : '';` \u2014 the panel can send nothing.\n\nWHY IT IS NOT THE DEFECT CLAIMED\n1. The mainline DOES close the branch, so \"every device created from now on\" is false. `projectScope()` (researcher-panel.js:4819-4822) never leaves `sel` on STRAY_TAB by default: `if (sel !== STRAY_TAB && !ids.has(sel)) sel = null; if (sel === null) sel = projects[0].folderId;`. Whenever any project folder exists, the panel sends `projectFolderId`, POST /v1/instances creates the folder eagerly inside it (3492) and writes `oauth_folder_id`, and the very next `GET /v1/researcher/drive-estate` \u2014 which the dashboard fires on every render (researcher-panel.js:1433) \u2014 runs `reconcileProjects` (v1.js:2907), whose device loop stamps `UPDATE instance SET project_id=? WHERE oauth_folder_id=?` (1813-1820). So the normal post-Phase-C device is project-scoped within one panel load. Step 2 of the attack is not the default path; it requires the researcher to deliberately select the \"Outside projects\" tab (which is only offered when strays already exist, 4855), or a flat estate, or a Drive failure inside the eager `try`.\n2. In every residual case the device is genuinely outside every project \u2014 its Drive folder is parented at the account master. The stated design rule is that a member \"must never reach the account master Drive folder\", and I4 says every unresolvable step denies. A member with manageDevices getting `not_found` on such a device is therefore the mandated outcome, not a break of project scoping. The same reasoning is written into the POST members gate at 2455-2477, which 409s `not_migrated` precisely because \"on a FLAT, unmigrated estate the device folders sit directly under master and there is no such subtree to root at\" \u2014 i.e. an unfiled container is deliberately unreachable by members, and on a flat estate members cannot exist at all.\n3. No widening, which is the invariant that actually governs the legacy branch. At v1.js:588-591 the branch returns ok only when `legacyOwner === caller.researcher_id`, and `instance.researcher_id` is bound to the caller at creation (3472) \u2014 a member creating an instance creates it in their OWN account. It consults `project_member` nowhere, so no member can ever arrive through it and no route reachable through it acts on another project's rows. Nothing in the attack produces access anyone did not already have.\n4. The remaining substance is a stale comment (3480-3484 / 585-587 overstate self-closure) plus, at most, a fault-path drift: if the eager Drive call throws, a device intended for project P silently lands under master. That is a usability/consistency issue with an in-UI remedy (\"Move to project\u2026\", which drives /v1/researcher/projects/assign and writes `project_id` at 3238), not an authorization defect, and comment accuracy is explicitly out of scope for this audit.\n5. The crowd half of the evidence (4491-4493) argues the other way: `driveEnsureCrowdFolder` at 1285 does fall back to `driveProjectFolderFor(...) \u2192 driveDefaultProjectFolder(access)`, so a new crowd folder lands inside the DEFAULT project folder and reconcileProjects stamps it \u2014 that path closes.\n6. The '' member_key sentinel is the companion finding's territory and requires the owner to deliberately write a grant for an unfiled device (2264-2281 is owner-only), so it cannot be used to prop up this one.",
  "correction": "The legacy branch is not the standing authorization path for new devices. When any Drive project folder exists, the panel names the project at creation (researcher-panel.js:3043 with `sel` defaulted to `projects[0].folderId` at 4822), the worker creates the device folder eagerly inside it (v1.js:3492) and records `oauth_folder_id`, and `reconcileProjects` stamps `instance.project_id` on the next `GET /v1/researcher/drive-estate` (v1.js:2907 \u2192 1813-1820), which the dashboard calls on every render. Instances can still be born NULL \u2014 the \"Outside projects\" tab, a flat estate, or a Drive failure inside the eager `try` \u2014 and for those the device folder genuinely sits under the account master, where the design says members must not reach; denying them `not_found` is I4 fail-closed behaviour, and the legacy branch still authorizes only the instance's own `researcher_id` and never reads `project_member`, so nothing is widened. What is genuinely inaccurate is the prose (3480-3484 and 585-587 claim the window closes on its own) and, at most, the fault path where a failed eager creation silently files a device outside the project it was created into \u2014 a drift with an existing UI remedy (`/v1/researcher/projects/assign`, v1.js:3238), not an authorization hole."
}
```

## a844406f07a1dad95

```json
{
  "refuted": false,
  "reasoning": "Traced the whole chain independently and every load-bearing claim checks out.\n\n(1) worker/src/v1.js:3470-3472 \u2014 the INSERT omits project_id entirely: 'INSERT INTO instance (instance_id, researcher_id, type, nickname, desired_blob, desired_rev, revoked, created_at, estate) VALUES (?,?,?,?,?,0,0,?,?)'. The eager branch below only creates a Drive folder, and only when the client sends projectFolderId.\n\n(2) docs/js/researcher-panel.js:3042-3043 \u2014 the panel deliberately sends nothing for the strays tab or a flat estate: `const intoProject = (currentProject && currentProject !== STRAY_TAB) ? currentProject : '';` So a request with no projectFolderId is a normal client action, not a contrived one.\n\n(3) worker/src/v1.js:1014-1032 \u2014 the lazy device folder: with existingId '' , drivePriorProjectParent returns '' at 1246 (`if (!folderId) return '';`), so `const parent = projectFolderId || (await drivePriorProjectParent(...)) || await driveMasterFolder(access)` resolves to MASTER. The device helper never consults driveProjectFolderFor, unlike the crowd helper at 1284-1285. The route's own comment at 3476-3478 (\"its parent comes from `driveProjectFolderFor` which falls back to the DEFAULT project\") is FALSE for devices \u2014 the code contradicts its own documented intent. Corroborating: `flextextProject` is searched at 989 and written nowhere in worker/src or docs/js.\n\n(4) buildDriveEstate line 825 gives a master-parented container `projectId: ''`; reconcileProjects skips it at 1803-1804 (`if (!d.projectId || !d.folderId) continue;`). So the only automatic stamper never touches it, however often /v1/researcher/drive-estate is polled.\n\n(5) The lazy adoption at 2628-2630 is gated on `if (!mine)` \u2014 the researcher having NO project row \u2014 so backfillProjectsFor's `UPDATE instance SET project_id=? WHERE researcher_id=? AND project_id IS NULL` (1713) never re-runs for a backfilled owner. `grep -n \"UPDATE instance SET project_id\"` yields exactly three sites: 1713 (gated), 1818 (needs Drive parentage inside a project folder), 3238 (/projects/assign, an explicit owner action). Nothing else.\n\nTherefore instance.project_id stays NULL indefinitely for such a device and authMember takes the legacy branch (588-591) forever; the \"it closes on its own: every lazy mint removes rows from it\" rationale at 585-587 is not true of devices created after Phase C. Attack sequence is performable with plain API calls: POST /v1/instances {\"nickname\":\"phone\"} (no projectFolderId) -> first upload creates the folder under master -> repeated GET /v1/researcher/drive-estate never stamps it -> POST /v1/instances/<id>/rename as a member with manageDevices returns not_found; POST /v1/researcher/keys for it writes member_key.project_id='' (2300), which the removal batch at 2500-2501 (DELETE ... WHERE project_id=? AND researcher_id=?) cannot match.\n\nNot refuted, but two scope corrections belong on the finding rather than counting against it: (a) it is not \"every device created from now on\" \u2014 one created with a project tab selected is stamped one drive-estate poll later via Drive parentage; the affected population is devices created from the strays tab, on an estate with no project folders, or by a client predating the projectFolderId parameter; (b) it is not privilege escalation \u2014 the legacy branch remains fail-closed and consults project_member not at all, so I4 and the not_found rule are intact. The defect is that a device born under the project model can land permanently outside every project with no automatic path back: members can never reach it, and grants for it carry the '' sentinel that member removal cannot delete. A manual remedy exists (panel 'moveto' -> /v1/researcher/projects/assign at 3234-3243), but nothing closes the window on its own."
}
```

## a9f740dd5f6099b87

```json
{
  "refuted": false,
  "reasoning": "I traced the whole sequence in worker/src/v1.js and could not refute it; every link holds.\n\n1) Grant write stamps '' whenever the instance is unstamped. POST /v1/researcher/keys (v1.js:2264-2303):\n   `const proj = inst.project_id ? await ...SELECT project_id, owner_id FROM project... : { project_id: null, owner_id: inst.researcher_id };`\n   then `...bind(String(proj.project_id || ''), instanceId, String(g.researcher_id), version, ...)`.\n   The route's owner check is `if (r.researcher_id !== proj.owner_id) return 403`, i.e. in the dual-read window the instance's own researcher passes, and there is NO check that the grantee is a member \u2014 the grantee is arbitrary, which is exactly what the /v1/researcher/pubkey route exists to enable (\"cross-researcher wrapping is simply impossible\" without it). So an owner sharing a device key for an unstamped instance writes project_id=''.\n\n2) The unstamped window is real and its residue is permanent. `INSERT INTO instance (instance_id, researcher_id, type, nickname, desired_blob, desired_rev, revoked, created_at, estate)` (v1.js:3473) has no project_id column \u2192 NULL, and the eager-folder comment right below says so explicitly (\"no `project_id` written anywhere, nothing to drift\"). reconcileProjects only stamps rows it can match by Drive folder \u2014 `for (const d of (estate.devices || [])) { if (!d.projectId || !d.folderId) continue; ...}` and `SELECT oauth_folder_id, project_id FROM ${table} WHERE researcher_id=? AND oauth_folder_id IS NOT NULL` (v1.js:3238-3250 region) \u2014 so a device with no Drive folder stays NULL indefinitely, and the 64-write CAP delays others. Crucially, `grep -n \"UPDATE member_key\\|member_key SET\" -r worker/` returns NOTHING: no backfill ever rewrites member_key.project_id, and selfGrantMissing (docs/js/researcher.js:384 `if (have[instanceId]) continue;`) only writes MISSING rows. So a row stamped '' stays '' forever, even after the instance is later stamped into a project.\n\n3) The migration gate does not close it. POST members' 409 not_migrated checks `SELECT drive_folder_id FROM project WHERE project_id=?` \u2014 a property of the PROJECT, not of any instance. A migrated project and an unstamped instance coexist fine.\n\n4) The removal is scoped by the snapshot. v1.js:2499-2506:\n   `env.DB.prepare('DELETE FROM member_key WHERE project_id=? AND researcher_id=?').bind(projectId, who)` \u2026 `return j({ ok: true, removed: gone, grants_removed: keys }, ...)`.\n   projectId is a real GUID; rows carrying '' (or a stale project id after /v1/researcher/projects/assign, which updates `instance SET project_id=?` but not member_key) never match. This is the only cleanup: the batch has exactly two statements, and no client code calls the members routes at all (`grep -rn \"/v1/projects\" docs/js/` \u2192 no hits), so there is no compensating client-side DELETE /v1/researcher/keys.\n\n5) The removed member keeps being served the key. GET /v1/researcher/keys (v1.js:2310-2318) is `SELECT instance_id, key_version, wrapped_ki FROM member_key WHERE researcher_id=? ORDER BY instance_id, key_version DESC` behind authResearcher only \u2014 no project predicate, no membership predicate. And schema-current.sql:107-116 confirms `PRIMARY KEY (instance_id, researcher_id, key_version)`, so project_id is a label, not identity.\n\nThe route's own contract is the standard it fails: \"\u26a0 REMOVING A MEMBER ALSO DELETES THEIR KEY GRANTS, in the same batch. Without that, 'removed' would mean 'no longer listed' while they still hold every wrapped Ki the ledger handed them \u2014 revocation as a UI state rather than an act (invariant I5)\" (v1.js:2424-2427). The dedicated revoke path DELETE /v1/researcher/keys deliberately scopes by instance_id + researcher_id (v1.js:2377-2379) and would have matched; the members route chose project_id and misses.\n\nScope note that keeps this honest rather than inflated: this is a revocation-completeness bug, not an authorization bypass. member_key is not consulted by authMember, so after removal the ex-member is still denied at every project/instance route; the residual harm is that the worker keeps handing over key material it claims to have withdrawn, and reports grants_removed:0 while doing it. That is still a concrete, code-evidenced deviation from a stated invariant with an obvious fix (scope the delete by the member's grants on instances belonging to the project, or by researcher_id + instance set, rather than by the project_id snapshot)."
}
```

## aa9c17f39bb2642f0

```json
{
  "refuted": false,
  "reasoning": "Confirmed by direct trace. (1) v1.js:2300 binds `String(proj.project_id || '')` \u2014 the dual-read branch at 2277-2279 yields `{project_id:null}` whenever `instance.project_id` is NULL, so the member_key row is stamped ''. (2) v1.js:2500-2501 removes grants with `DELETE FROM member_key WHERE project_id=? AND researcher_id=?` bound to the URL's projectId, which cannot match '' (or a stale id). (3) No statement anywhere rewrites member_key.project_id \u2014 the only member_key SQL is at 2290, 2312/2314, 2377/2379, 2501 \u2014 and schema-current.sql:107-116 keys the table on (instance_id, researcher_id, key_version), so project_id is a label that can never be corrected. (4) GET /v1/researcher/keys (2312-2315) filters on researcher_id alone, with no project or membership predicate, so the surviving row is still served to the removed member (who remains an approved researcher and can unwrap it). Reachability of each step checked: the instance INSERT at 3471 has no project_id column; reconcileProjects only stamps rows `WHERE oauth_folder_id IS NOT NULL` and skips devices whose Drive parent is not a project folder (`if (!d.projectId || !d.folderId) continue`), and oauth_folder_id is only written on device-folder creation (1032), so a device created without projectFolderId and with no uploads stays NULL indefinitely \u2014 the write path's own comment says the NULL case is 'EVERY instance today'. POST /v1/researcher/keys gates only on owner identity plus the owner's own copy being present; it never requires the grantee to be a member, so an owner can write a grant to M for a NULL-project instance. POST members gates on project.drive_folder_id, which is independent of any instance's project_id, so a migrated project containing an unstamped device is an ordinary state. The stale variant is also real: POST /v1/researcher/projects/assign (3238) and reconcileProjects' correction (1810-1817) both update instance.project_id and neither touches member_key. The route's own comment at 2424-2427 asserts exactly the guarantee that fails. Considered refutations and rejected them: no other delete path runs (the by-instance DELETE at 2377 is a separate manual endpoint the owner must call per device); no backfill exists; the sequence needs only documented endpoints the owner is authorized to call.",
  "correction": "The mechanism and the request sequence are as claimed; one impact qualifier belongs in the write-up. After removal, project_member is gone, so authMember denies the removed member on every project-scoped route (the dual-read branch can only ever authorize instance.researcher_id, never a member), and a NULL-project instance is by construction not under a shared project folder. So this is not a data-read bypass \u2014 it is a revocation-completeness defect: the worker keeps serving a wrapped Ki it promised to withdraw, and reports grants_removed:0 (and logs '0 grant(s)') so the failure is silent to the owner. Correct fix: scope the grant delete to the researcher plus the project's instances (e.g. instance_id IN (SELECT instance_id FROM instance WHERE project_id=?) plus the '' rows of that owner), not to the stored member_key.project_id label."
}
```

## a64531ef7a85162e0

```json
{
  "refuted": false,
  "reasoning": "The finding reproduces exactly as written; every quoted line is accurate and no guard elsewhere closes it.\n\nTraced sequence:\n1. POST /v1/instances (v1.js:3471) inserts with NO project_id column -> instance.project_id is NULL. Even the eager project-folder branch only writes oauth_folder_id (driveEnsureDeviceFolder, v1.js:1032, \"UPDATE instance SET oauth_folder_id=? WHERE instance_id=?\"), and its own comment says \"no `project_id` written anywhere\". reconcileProjects (v1.js:1800-1820) stamps project_id only for rows whose oauth_folder_id sits under a project folder, so the NULL window is real for every fresh device and permanent for a device that never uploads.\n2. POST /v1/projects/P/members succeeds for a migrated project (drive_folder_id set) \u2014 unrelated to D's project_id.\n3. POST /v1/researcher/keys (v1.js:2270-2302): the dual-read branch yields `{ project_id: null, owner_id: inst.researcher_id }`, the caller check is only `r.researcher_id !== proj.owner_id`, and the grants list is filtered only on `g.researcher_id && g.wrapped_ki` \u2014 there is NO check that a co-grantee is a member of anything. The row is bound with `String(proj.project_id || '')`, i.e. project_id=''.\n4. DELETE /v1/projects/P/members (v1.js:2499-2506) runs `DELETE FROM member_key WHERE project_id=? AND researcher_id=?` bound to 'P' \u2014 no match against '' \u2014 and returns grants_removed: 0.\n5. GET /v1/researcher/keys (v1.js:2311-2316) is authResearcher-only and filters solely on researcher_id, with no project or membership predicate, so the removed member is still served the wrapped Ki.\n\nSchema confirms project_id is a label, not identity: PRIMARY KEY (instance_id, researcher_id, key_version) (schema-current.sql:107-116), and a grep shows member_key.project_id is read at exactly one place \u2014 the delete at 2501.\n\nRefutation attempts that failed: (a) nothing re-stamps member_key.project_id when instance.project_id is later filled by reconcileProjects or POST /v1/researcher/projects/assign (v1.js:3236-3242 updates instance/crowd_recorder only), so the '' label is permanent for that grant; (b) DELETE /v1/researcher/keys (v1.js:2364-2381) does delete by instance_id+researcher_id irrespective of project, but it is a separate per-grant route the removal batch does not call; (c) the route's own comment (v1.js:2424-2427) asserts the opposite behaviour, so this is a gap between the stated invariant and the code, not a deliberate design choice.\n\nOnly softening: no shipped client currently mints cross-researcher grants (researcher.js:393 self-grants only), so the sequence needs direct API calls by the owner \u2014 but cross-researcher wrapping is precisely what Phase C added (GET /v1/researcher/pubkey exists solely for it, and the write path reasons about \"a member with createInvites\"), so the path is intended and reachable with legitimate owner credentials. The most natural reproduction needs no adversary at all: a device created into project P is shared with member M during the NULL window, and M's removal then silently leaves the grant in place.",
  "correction": "The finding stands as stated. One refinement worth carrying into any fix: the root cause is broader than the delete \u2014 POST /v1/researcher/keys also accepts grants to researchers who are not members of the instance's project, and it snapshots project_id at write time into a column that is never re-stamped. Scoping the removal delete by the instance's CURRENT project (e.g. DELETE FROM member_key WHERE researcher_id=? AND instance_id IN (SELECT instance_id FROM instance WHERE project_id=?)) \u2014 or dropping the project_id predicate and joining through instance \u2014 closes both the '' case and the stale case, and grants_removed then reports the truth."
}
```

## aaffd9870948c3e5b

```json
{
  "reasoning": "The mechanical trace is accurate, but the consequence the finding asserts \u2014 an un-withdrawable, still-live grant \u2014 does not hold.\n\nWhat I confirmed in the code:\n- v1.js:555 `SELECT project_id, researcher_id FROM instance WHERE instance_id=? AND revoked=0` \u2014 a revoked instance never resolves; v1.js:594-598 then denies, so DELETE /v1/researcher/keys (v1.js:2369-2371, `if (!ctx.ok || !ctx.isOwner) return j({ error: 'not_found' }, 404 \u2026)`) does answer 404 for a revoked device.\n- v1.js:3966-3969 revoke writes only `UPDATE instance SET revoked=1 \u2026` / `UPDATE install SET revoked=1 \u2026`; no member_key cleanup.\n\nWhy it is not a defect:\n\n1. There IS a working withdrawal path, and it is not instance-scoped or blocked by revocation. DELETE /v1/projects/<pid>/members (v1.js:2500-2502) runs `DELETE FROM project_member WHERE project_id=? AND researcher_id=?` and `DELETE FROM member_key WHERE project_id=? AND researcher_id=?` \u2014 no instance predicate at all, so it removes the grantee's rows for revoked instances too. So \"the owner can no longer withdraw any grant on it\" is false; what is unavailable is the narrower \"withdraw one device's grant while keeping the member\". The finding waves this away as \"only removes rows whose stored project_id equals the project\", but under Phase C a member can only exist in a migrated project (POST members 409s `not_migrated` unless `project.drive_folder_id` is set, v1.js:2472-2477), and grants minted then carry `proj.project_id` (v1.js:2299). The `project_id=''` sentinel is the unassigned/legacy case, where there is no member to withdraw from.\n\n2. The stale row confers no reachable access, so it is not a live grant. `member_key` is never consulted for authorization anywhere in the worker \u2014 the only readers are GET keys (2312/2314), DELETE keys (2377/2379) and members DELETE (2501). The wrapped Ki is only usable against instance-scoped ciphertext, and every path to that ciphertext for a revoked instance already denies: all converted instance routes resolve through authMember's `revoked=0` lookup; the owner dashboard lists `\u2026 WHERE researcher_id=? AND revoked=0` (2662); scoped/member-minted /v1/textfile tokens 410 once the instance is revoked (1861-1864). And per the route's own documented E2EE honesty, deleting the row cannot un-know a key the grantee already fetched \u2014 real revocation is rotation (key_version, Phase E). So step 4 of the attack (\"M is still served the wrapped Ki\") yields M nothing they did not already have and nothing they can use.\n\n3. The behaviour is the deliberate, uniform fail-closed treatment of a revoked instance (I4 + \"denial indistinguishable from absence\"): a revoked device is unaddressable by every authMember route, not just this one. There is no un-revoke route, so the row is inert forever.\n\n4. The obvious \"fixes\" are the dangerous kind. Deleting member_key on revoke would also delete the OWNER's own wrapped copy \u2014 the worker cannot re-wrap it (it cannot read Ki), so it would break the wrap-to-owner invariant and permanently strand the owner's ability to decrypt that instance's stored blobs. Relaxing authMember's revoked filter for one route weakens the single-authority guarantee for no reachable gain.\n\nResidual (not a defect worth reporting): a grant written on an instance whose project_id was still NULL stores `project_id=''` and, once that instance is revoked, is deletable by neither route \u2014 an orphan escrow row for a dead device that no authorization path reads. That is housekeeping, not access.",
  "refuted": true,
  "correction": "A revoked instance is indeed unaddressable by DELETE /v1/researcher/keys (authMember's `revoked=0` lookup at v1.js:555), and revoke does not delete member_key rows. But the owner is not left without a withdrawal: DELETE /v1/projects/<pid>/members deletes `member_key WHERE project_id=? AND researcher_id=?` with no instance predicate, so it removes the grantee's rows on revoked instances as well. And the surviving row grants nothing reachable \u2014 member_key is never used for authorization, and every route serving a revoked instance's ciphertext already denies (authMember, the owner dashboard's `revoked=0` filter, and the 410 on scoped/member-minted /v1/textfile tokens). What is actually missing is the narrow ability to revoke one device's grant while keeping the member; that is a housekeeping gap, and the naive fix (purging member_key on revoke) would delete the owner's own wrapped copy and break the wrap-to-owner invariant."
}
```

## a0333adb0fa0f2787

```json
{
  "refuted": true,
  "reasoning": "The mechanics described are real: authMember's instance lookup (v1.js:555) filters `revoked=0`, so DELETE /v1/researcher/keys (v1.js:2369-2371) answers 404 for a revoked instance, and revoke (v1.js:3966-3968) deletes no member_key rows. But the claimed harm does not hold. (a) The grant IS withdrawable: DELETE /v1/projects/<id>/members (v1.js:2496-2506) batches `DELETE FROM project_member` + `DELETE FROM member_key WHERE project_id=? AND researcher_id=?`, which removes the grantee's rows for revoked instances too. The claim's dismissal of that path assumes member_key.project_id is '', but a member can only exist post-migration \u2014 POST members 409s `not_migrated` unless project.drive_folder_id is set (v1.js:2473-2478), and the same reconcile that stamps drive_folder_id backfills `UPDATE instance SET project_id=? WHERE researcher_id=? AND project_id IS NULL` (v1.js:1713) \u2014 so real member grants carry the true project_id and removal cleans them. This is also the remedy the design itself names in the POST-keys comment (\"remedied by revoking the member and re-keying\"). (b) The surviving wrapped Ki confers no worker-side access: every instance-scoped route filters revoked=0 (3534, 3728, 3752, 3810, 3862, 3900, 4277), so no ciphertext for that instance can be fetched afterwards; and the route's own comment concedes deletion cannot un-know a cached key \u2014 real revocation is rotation. (c) The 404 is the same fail-closed \"a revoked instance is absent\" treatment every other instance route applies, consistent with I4; no design rule listed is violated. (d) The obvious fix this framing invites \u2014 relaxing `revoked=0` in the single authorization helper \u2014 would widen the boundary for all 19 converted call sites, a worse bug than the wart. What actually remains is a narrow usability gap (withdrawing one device's grant while keeping the member requires remove-and-re-add), not an access-retention vulnerability.",
  "correction": "A revoked instance does make DELETE /v1/researcher/keys answer 404, and instance revocation leaves member_key rows in place \u2014 but the owner can still withdraw the grant via DELETE /v1/projects/<project_id>/members, which deletes the member's member_key rows scoped by project (v1.js:2501); those rows carry the real project_id for any member grant, since members can only be added after migration and the same reconcile backfills instance.project_id. The stale wrapped Ki also grants nothing through the API, because every instance-scoped route treats a revoked instance as absent. The residual issue is only that per-device grant withdrawal is unavailable on a revoked device (owner must remove and re-add the member); a safe cleanup would delete member_key rows inside the revoke batch, or resolve the target at the route rather than loosening `revoked=0` in authMember."
}
```

## a5ff573238afb0db2

```json
{
  "refuted": true,
  "reasoning": "The code quotes are accurate \u2014 authMember's instance lookup filters `revoked=0` (v1.js:555) and denies on empty project_id (594-598), so DELETE /v1/researcher/keys (2369-2371) 404s for a revoked instance, revoke deletes no member_key rows (3966-3968), and GET keys serves by researcher_id with no instance check (2311-2315). But it is not a defect. (a) It is fail-closed, which I4 mandates, and uniform with the whole file: every instance-scoped query carries `AND revoked=0` (2662, 3534, 3566, 3637, 3728, 3752, 3810, 3862, 3898-3900) and the device poll 410s (4317); the dashboard at 2662 does not even list a revoked instance, so there is no surface to initiate the sequence from. (b) No access boundary moves: with the instance revoked the worker serves none of its ciphertext to anyone, so a wrapped Ki for a dead instance unlocks nothing the worker will hand over; and per the route's own E2EE note plus getKi's fetch-once-per-unlock (docs/js/researcher.js:45,416), a member who was going to use the key already holds it, so deletion would change nothing anyway. (c) The owner has a working withdrawal path for the case that matters: DELETE /v1/projects/<pid>/members (2497-2502) deletes `member_key WHERE project_id=? AND researcher_id=?` in the same batch, is not instance-scoped, never consults `instance`, and the grant row's project_id was bound from that instance's project at write time (2300) \u2014 so removing M does remove the grant on the revoked device. The finding's dismissal of that remedy rests on a separate stale-project_id claim. (d) The route has no caller: grep over docs/js finds only POST (researcher.js:393) and GET (researcher.js:416), no DELETE. The only real oddity is asymmetry with POST keys, which looks the instance up without `revoked=0` (2270) \u2014 untidy, but the safe direction is the one taken, and the obvious fix (exempting this route from the revoked filter, or relaxing it inside authMember) would punch a hole in the single authorization helper every instance route depends on.",
  "correction": "Accurate as a description of the code, but it describes deliberate, uniform fail-closed behaviour rather than a defect: a revoked instance is unmanageable and invisible everywhere in this worker, holding a wrapped Ki for one grants no data the worker will serve, and the owner can still withdraw the grant via DELETE /v1/projects/<pid>/members, which deletes member_key by project_id without touching the instance row. The only real residue is an untidy asymmetry \u2014 POST /v1/researcher/keys resolves the instance without the revoked filter (v1.js:2270) while the DELETE resolves through authMember and cannot \u2014 worth noting as hygiene, not worth fixing by relaxing the revoked=0 filter in the shared auth helper."
}
```

## a0343388865d781e0

```json
{
  "refuted": false,
  "reasoning": "Confirmed by direct trace; every quoted anchor is accurate at the cited line numbers.\n\nCODE VERIFIED\n- v1.js:3921 (move) and v1.js:3881 (adopt) are the identical line: `const mint = (fileId, extract) => mintTextfileUrl(env, url.origin, r.researcher_id, fileId, extract, 0, { instanceId: to.instance_id, docId }, ctx.caller.researcher_id);` followed by `const flextextUrl = await mint(body.flextextFileId) || await mint(body.extractFromZipId, 'flextext'); const audioUrl = await mint(body.audioFileId);`. `r = ctx.owner`, so the token is minted with the OWNER's researcher_id; the file ids come straight off the request body.\n- mintTextfileUrl (v1.js:1209-1227) validates nothing about fileId beyond `if (!fileId) return null;`. No parent check, no docId cross-check, no project check.\n- v1.js:3899-3903: `from`/`to` are looked up with `WHERE instance_id=? AND researcher_id=? AND revoked=0` bound to `r.researcher_id` \u2014 the owner's ENTIRE estate, never ctx.project_id.\n- v1.js:1886-1888 redemption recheck is verbatim as quoted and binds `(tk.m, tk.i)`: it asks only whether the minter is still a member of the DESTINATION instance's project. `tk.f` is never re-examined; the stream at 1904-1907 runs on `driveAccessToken(env, owner)`.\n\nSEQUENCE EXECUTES\nM holds {\"assignTexts\":true} in project A with devices D1, D2. `POST /v1/instances/D1/texts/zzz/move {to:\"D2\", flextextFileId:\"F\"}`. authMember({instance:D1},'assignTexts') passes. The only body guard is `if (!docId || !toId || toId === instanceId) return bad_move`, so a throwaway docId is accepted; the Drive folder search simply returns no rows (`movedFolder=false`, no throw) and minting proceeds anyway. Redemption: D2 is live under owner O, M is a member of D2's project (A), so the grant recheck passes and F streams. F is never related to anything in project A.\n\nREFUTATIONS ATTEMPTED AND FAILED\n- No guard elsewhere. authMember (543-660) only resolves the TARGET INSTANCE's project; its own comment at 633-635 says \"resolving the target to this project (above) is the whole of the scoping.\" It never sees file ids.\n- Not deliberate. validateCaps' comment (468-482) concedes only that PER-DEVICE separation inside a project is unenforceable via Drive file ids, settling on the project as the honest boundary. The members migration-gate comment (~2440) states the opposite of the observed behaviour: \"a member's file listing has to be ROOTED at that folder rather than walked from the account master and filtered afterwards.\" Cross-project reach is exactly what the design forbids (\"must never reach the account master Drive folder or another project\").\n- Not pre-existing. Before Phase C these routes used authResearcher, so a caller-supplied file id reached only the caller's own Drive \u2014 no escalation. Phase C is what turns it into one.\n- Discovery is bounded but real: GET /v1/projects (2396) leaks no foreign file ids and drive-estate/drive-snapshot are authResearcher (owner-only), BUT GET .../texts/<docId>/files (3663) runs an account-wide `spaces=drive` appProperties search with only `instance_id AND researcher_id` in D1 and no project constraint, and a researcher removed from project B but retained in A keeps every id they saw. That last case defeats the stated purpose of the `m` recheck at 1218-1225 (\"removing that member leaves them holding 90 days of read access\") \u2014 they can mint FRESH tokens for B's files.\n- The fix is implementable, so this is not an accepted impossibility: drivePriorProjectParent (1245) already walks parents and driveProjectFolderFor exists, so F's parentage could be verified against ctx.project_id's folder.\n\nSoft point, stated honestly: the attack presumes M knows a foreign Drive file id. The finding states that as a precondition and two in-code paths supply it. The primitive itself \u2014 a caller-controlled, unvalidated file id sealed into an owner-authority bearer token from a project-scoped route \u2014 is fully evidenced in the code."
}
```

## a043055c9b4413a29

```json
{
  "refuted": false,
  "reasoning": "Confirmed by direct trace; every quoted line matches. (1) mintTextfileUrl (v1.js:1209-1227) validates nothing about fileId \u2014 `if (!fileId) return null;` and then seals the caller-supplied id into the token; there is no Drive parentage check, no project-folder check, no files.get on `f`, anywhere in the file (grep for any file-in-project helper returns nothing; only driveProjectFolderFor, which is used for creating container folders). (2) move (3921) and adopt (3881) mint from raw body fields with `r.researcher_id` = ctx.owner, and the only DB validation in the route is the `from`/`to` instance lookups at 3898-3901, scoped `AND researcher_id=?` to the OWNER, never to ctx.project_id \u2014 the file ids are never validated against the doc, the device, or the project. (3) The Phase C member-grant recheck at 1885-1888 is `SELECT 1 FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?` \u2014 it binds the token to the DESTINATION instance's project, not to the file's, so a token {r:owner, f:F(project B), m:M, i:D2(project A)} passes and 1904-1907 streams F with the owner's Drive access token. (4) No other guard: check-project-scoping.sh only enforces the !ctx.ok shape, no-403, no authResearcher regression under /v1/instances, and wipe/force-remove owner-gating \u2014 nothing about caller-supplied Drive ids. (5) The sequence is concrete and even simpler than claimed: a member of project A with assignTexts can POST /v1/instances/<D1>/texts/<docId>/assignment/finish {\"flextextFileId\":\"F\"} (v1.js:3806-3828, same unvalidated mint, scope {instanceId: D1, docId}) and GET the returned header-less URL \u2014 no second device, no move needed. (6) Counter-evidence I tested actually supports the finding: pointing `to` at a device in project B DOES fail (redemption checks the destination's project), which demonstrates the destination is scoped while the file is not. Bounding caveat: the attacker must know F, an unguessable Drive id, so this is not an enumeration primitive; the realistic holder is a researcher removed from project B who remains a member of project A (or one who saw ids before a re-org) \u2014 exactly the case the tk.m recheck at 1868-1880 was written for (\"remove that member and, without this, they keep reading those files for the rest of the 90 days\"). The recheck closes that tail for the project the token names and leaves it open for every other project of the same owner, violating both the project-scoping rule and invariant I2's own stated purpose.",
  "correction": "Two refinements to the finding as written. (a) The shortest reproduction is not move/adopt but POST /v1/instances/<D1>/texts/<docId>/assignment/finish (v1.js:3806-3828), which mints from body.audioFileId/flextextFileId/promptFileId with the same total absence of validation and scopes the token to D1 \u2014 a device already inside the attacker's own project \u2014 so no second device and no Drive re-parenting is involved. move/adopt are the same defect, one hop longer. (b) The claim that the file can be \"any app-created file in the owner's whole Drive\" is right, but the attack is not an enumeration: the file id must already be known, so the exposure is a failure of REVOCATION and cross-project containment (a member of project A reading a project-B id they retain), not a way to discover ids. The fix is to bind the minted file to the caller's project \u2014 verify F's ancestry resolves under ctx.project_id's drive_folder_id before minting, and/or stamp the project into the token and recheck it at 1885 alongside the instance check."
}
```

## a7a1b849309abdd97

```json
{
  "refuted": false,
  "reasoning": "The code says what the finding claims and I traced the sequence end to end.\n\n1. Route gate (v1.js:3890-3893): `const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');` then `const r = ctx.owner;` \u2014 a member with assignTexts passes, and no `ctx.isOwner` check exists on move or adopt.\n\n2. Instance resolution is ACCOUNT-scoped, not project-scoped (v1.js:3899-3903): both `from` and `to` are `SELECT instance_id, nickname, oauth_folder_id FROM instance WHERE instance_id=? AND researcher_id=?` bound to `r.researcher_id` (the owner). `ctx.project_id` is never consulted.\n\n3. The mint takes caller-supplied ids with zero validation (v1.js:3921-3923, and identically at 3881-3883 for /adopt):\n   `const mint = (fileId, extract) => mintTextfileUrl(env, url.origin, r.researcher_id, fileId, extract, 0, { instanceId: to.instance_id, docId }, ctx.caller.researcher_id);`\n   `const flextextUrl = await mint(body.flextextFileId) || await mint(body.extractFromZipId, 'flextext');`\n   `mintTextfileUrl` (v1.js:1209) only does `if (!fileId) return null;` and packs the id into the token \u2014 no parent/project/doc/device check. The token is sealed with the OWNER's researcher_id (`tk.r = r.researcher_id`) and `tk.m = ctx.caller.researcher_id`.\n\n4. The request is performable: with a junk docId the Drive tag search finds nothing, so `movedFolder=false` and no side effect occurs, yet the mints still run and the URLs are returned in the 200 body.\n\n5. Redemption does not close it (v1.js:1886-1889): `SELECT 1 AS ok FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?` \u2014 it validates that the minter is still a member of the project the DESTINATION INSTANCE belongs to. It asks nothing about `tk.f`. Since the attacker names D2 (a device in their own project A), the check passes, and v1.js:1904-1907 streams the file using `driveAccessToken(env, owner)`.\n\nNo guard elsewhere prevents it: check-project-scoping.sh only enforces that routes call authMember, guard on `!ctx.ok`, avoid 403, and keep wipe/force-remove owner-only \u2014 it says nothing about file ids.\n\nThis also defeats the invariant the `tk.m` recheck was written for (its own comment at v1.js:1874-1882: \"remove that member and, without this, they keep reading those files for the rest of the 90 days with no grant behind it\"). A researcher removed from project B but still a member of project A can re-mint fresh 90-day URLs for project-B file ids they saw legitimately while in B, and the recheck passes because it interrogates the destination device's project. That makes the attack a concrete, non-hypothetical sequence rather than one that needs id guessing.\n\nIt violates the stated design rule \"A member reaches everything in their project, nothing outside it, and must never reach the account master Drive folder or another project.\"",
  "correction": "Two refinements, neither of which refutes it:\n\n(a) Exploitability framing. Drive file ids are unguessable random strings, so this is a confused-deputy read for an id the member already knows, not an enumeration primitive. Realistic sources: a prior membership in another project of the same owner (the case that defeats the tk.m revocation recheck), a forwarded link, or the account-wide `flextextDoc` folder search at v1.js:3677 given a docId. \"Any app-created file in the owner's whole Drive\" is accurate as a capability but requires knowing the id; and \"the account master Drive folder\" means files under it whose ids are known \u2014 `alt=media` against a folder id itself fails.\n\n(b) Scope is wider than the finding states. The identical unvalidated primitive exists at v1.js:3826-3828 in `POST .../texts/<docId>/assignment/finish`:\n  `const audioUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.audioFileId, '', ttlMs, scope, ctx.caller.researcher_id);`\n  `const flextextUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.flextextFileId, '', ttlMs, scope, ctx.caller.researcher_id);`\n  `const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);`\nThat route is also `authMember(..., 'assignTexts')`, and `promptUrl` is minted deliberately UNSCOPED (`scope = null`), so a member-minted prompt token carries `m` but no `i` and falls into the coarser redemption branch at v1.js:1893-1895 (\"member of ANY project this owner owns\") \u2014 same cross-project read, and it survives revocation of any particular device.\n\nA minimal fix has to validate the supplied file ids against the resolved project (e.g. confirm the file's parent chain lands under `project.drive_folder_id` for `ctx.project_id`) before minting, for all three routes; the redemption recheck cannot do it because the token carries no project."
}
```

## a973ee1144653ca4e

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED \u2014 traced end to end in the actual code.\n\nMINT (worker/src/v1.js:3806-3828). `const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');` resolves the grant against the INSTANCE's project. The only body validation is 3815 `if (!body.audioFileId && !body.flextextFileId && !body.promptFileId) return j({ error: 'nothing_to_mint' }, 400, ...)`. Then 3828: `const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);` with `r = ctx.owner`. The instance re-check at 3810 (`SELECT ... FROM instance WHERE instance_id=? AND researcher_id=?`) constrains the instance, never the file. `clampTtlDays` (1186-1190) permits 400 days.\n\nTOKEN (1209-1215). `if (!fileId) return null;` then `const tk = { r: researcherId, f: fileId, ... }` \u2014 the caller-supplied id is copied in with no lookup. scope=null \u21d2 no `tk.i`/`v:2`; minterId !== researcherId \u21d2 `tk.m` set.\n\nREDEEM (1850-1905). Checks: decrypt+expiry; owner row has `drive_refresh_enc`; `if (tk.i)` instance liveness (SKIPPED, no `i`); `if (tk.m && tk.m !== tk.r)` \u2192 with no `tk.i` it takes the documented coarse fallback `SELECT 1 FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?`, i.e. \"still a member of ANY project this owner owns\" \u2014 satisfied by project A. Then 1904 `fetch('https://www.googleapis.com/drive/v3/files/' + encodeURIComponent(tk.f) + '?alt=media', { headers: { Authorization: 'Bearer ' + access ...` using `driveAccessToken(env, owner)`. Neither path ever asks where `tk.f` lives.\n\nNo compensating guard. authMember (543-648) is never handed a file id and states \"NO PER-DEVICE CHECK HERE \u2014 the project IS the boundary\", so it structurally cannot catch this; check-project-scoping.sh only enforces call-site shape, not argument provenance. The other member-reachable mint sites (3881 /move, 3921 /adopt) have the same shape; /v1/crowd/<id>/prompt/finish (4809) is owner-authed. The owner's token is `drive.file`, and the repo's own R2-1 note (plans/project-split.md:725-745) states that scope covers the whole FlexText estate, so the reachable set really is every app-created file in the owner's account, other projects and the master folder included.\n\nNot deliberate. The comment at 3820-3825 deliberately leaves the PROMPT token unscoped with respect to instance revocation; nothing claims the file id is trusted. plans/project-split.md:1708-1714 lists this exact gap as Phase C work \u2014 \"The file id comes straight from the request body at all three mint sites... Put the scope INTO the token (owner, project, instance, doc, granting member, jti), re-check against live D1 at serve, and clamp member-minted TTLs well below the 400 days clampTtlDays allows.\" Phase C shipped the instance and minter halves and omitted the file and TTL halves. It violates the stated rule \"a member reaches everything in their project, nothing outside it, and must never reach the account master Drive folder or another project\", and it walks around the very invariant the tk.m check at 1878-1897 exists to enforce.\n\nOne honest narrowing, which does not refute: Drive file ids and docIds are unguessable, so the discovery step is not free. The /files route at 3663-3679 is genuinely unscoped (`spaces=drive`, `appProperties has { key='flextextDoc' and value='${docId}' }`, no parent/project constraint) and will return a foreign project's file ids, but only to a caller who already knows that project's docId. The concrete, fully reachable sequence is the finding's second variant: a researcher removed from project B who retains file ids from that period, but who remains a member of project A, mints a 400-day unscoped prompt token from an A instance and keeps reading project B's audio/flextext \u2014 removal that leaves a 400-day tail, which is exactly what the minter check was written to prevent.",
  "correction": "The finding stands as written. Two precisions worth carrying into any fix: (1) exploitation requires the attacker to already hold the foreign Drive file id (or the foreign docId for the /files enumeration step) \u2014 ids are not enumerable, so the realistic actor is a former member of the other project, not an arbitrary member guessing; (2) the same missing check exists on the /move (worker/src/v1.js:3881) and /adopt (3921) mint sites, where the scope names a project-A instance and the redemption membership check passes for the same reason \u2014 a fix that only touches the finish route at 3826-3828 would be incomplete."
}
```

## a8aacd912813889f4

```json
{
  "refuted": false,
  "reasoning": "Verified in the code. worker/src/v1.js:3815 is the only validation applied to the three caller-supplied Drive file ids in POST /v1/instances/<id>/texts/<docId>/assignment/finish (`if (!body.audioFileId && !body.flextextFileId && !body.promptFileId) return j({ error: 'nothing_to_mint' }, 400, ...)`), and 3826-3828 pass them verbatim into mintTextfileUrl. mintTextfileUrl (1209-1215) does `if (!fileId) return null; const tk = { r: researcherId, f: fileId, ... }` with no Drive lookup. There is no file-containment helper anywhere in the worker (grep for parent/project checks on a file id finds none; driveProjectFolderFor is used only when creating container folders, 1285/3477), and the finish route does not tie the id to an upload session either (the chunk relay validates sess.rr at 3796, finish validates nothing). Redemption (1852-1907) checks the owner row, the scoped instance when tk.i is present, and \u2014 for member-minted tokens \u2014 only the coarse membership query at 1893-1895 (`SELECT 1 FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?`), i.e. membership of ANY project that owner owns. Line 1904 then streams `files/<tk.f>?alt=media` with the owner's Drive token. The file is never compared to project.drive_folder_id or to the caller's project at any point in mint or redeem. Multiple projects per owner are a real state (1760, 2400, reconcileProjects at 1754-1794), so cross-project is reachable, and clampTtlDays (1186-1190) really allows 400 days; the promptFileId token additionally passes scope=null (3828) so it carries no `i` and no device revocation kills it. This is authorization over the Drive object decided outside authMember \u2014 in fact decided nowhere \u2014 and it defeats the invariant the code's own comment at 1868-1885 states as required (\"remove that member and, without this, they keep reading those files ... with no grant behind it\"): a researcher removed from project B but still a member of project A mints a fresh long-lived token for B's files through A, and the tk.m check passes. That violates \"a member reaches everything in their project, nothing outside it\".",
  "correction": "The mint-and-redeem leg reproduces exactly as claimed, but the id-DISCOVERY leg is overstated. GET .../texts/<docId>/files (3663-3679) is indeed an unscoped whole-Drive tag search (`spaces=drive`, `appProperties has { key='flextextDoc' and value='<docId>' }`, no parent constraint) reachable by any member with drive:read, but it requires the attacker to already know a docId belonging to project B; I found no member-reachable route that enumerates the owner's texts or instances across projects (the instance listing at 2662 is scoped to the caller's own researcher_id). So the realistic entry is the finding's own alternative: a docId or file id retained from a project the member was removed from, or otherwise leaked \u2014 which is exactly the removed-member threat the tk.m check was written for and does not close. Severity is unchanged for that case; it is not a zero-knowledge cross-project browse."
}
```

## aa3ab8c450ee26bc2

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED \u2014 I traced the full sequence in the shipped code.\n\n1) Mint: worker/src/v1.js:3805-3830. After `authMember(request, env, { instance: instanceId }, 'assignTexts')` and `const r = ctx.owner;`, the only body validation is line 3815 `if (!body.audioFileId && !body.flextextFileId && !body.promptFileId) return j({ error: 'nothing_to_mint' }, 400, origin, env);`. Line 3828: `const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);` \u2014 scope is literally `null`, so no `i`/`d` is stamped. `ttlMs` comes from `clampTtlDays(body.ttlDays)`, which caps at 400 days (1186-1190).\n\n2) mintTextfileUrl (1209-1215): `if (!fileId) return null;` then `const tk = { r: researcherId, f: fileId, x: extract || '', e: \u2026, n: \u2026, iat: \u2026 };` plus `if (minterId && minterId !== researcherId) tk.m = minterId;`. No Drive metadata lookup, no parent/project check \u2014 the caller's id is sealed verbatim under the OWNER's researcher_id.\n\n3) Redemption (1850-1907): validates decrypt/expiry, loads the owner row by `tk.r`, checks `tk.i` only when present (absent here), then the Phase-C minter recheck. With `tk.m` set and `tk.i` absent the query is `SELECT 1 AS ok FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?` \u2014 \"is the minter still a member of ANY project this owner owns\" \u2014 which the attacker's project-A membership satisfies. It then streams: line 1904 `fetch('https://www.googleapis.com/drive/v3/files/' + encodeURIComponent(tk.f) + '?alt=media', { headers: { Authorization: 'Bearer ' + access \u2026 } })` with `access = await driveAccessToken(env, owner)`. Neither query nor fetch ever relates `tk.f` to a project, a doc, or an instance. Under drive.file scope that reaches every app-created file in the owner's account \u2014 all their projects and the master folder.\n\n4) Reachability of the id: GET .../texts/<docId>/files (3663-3690) authorizes with `authMember(\u2026, 'drive:read')` against a project-A instance, re-checks only `instance \u2026 AND researcher_id=?` against the owner, then runs an account-wide search \u2014 line 3678-3679 `appProperties has { key='flextextDoc' and value='${docId}' } and mimeType='application/vnd.google-apps.folder' and trashed=false` over `spaces=drive`, no parent/project constraint \u2014 returning folderId, originalsFolderId and every child file id. So a docId retained from a project the member was removed from yields CURRENT file ids (including files uploaded after removal), and those ids feed step 2.\n\nRefutation attempts, all failed:\n- No guard elsewhere. All six mintTextfileUrl call sites (3826, 3827, 3828, 3881 adopt, 3921 move, 4809 owner-only) pass caller-supplied ids; the finish route's only other checks are the instance-ownership SELECT and the docId sanitiser used for logging.\n- Path is member-reachable: `validateCaps` (465-500) accepts `assignTexts` and `drive:'read'` as real member capabilities, and authMember (602-647) returns `{ ok:true, owner, caller, isOwner:false }` for a member of the resolved project.\n- Not deliberate/accepted: plans/project-split.md:1708 states the same gap as required work \u2014 \"Minted /v1/textfile URLs are unscoped at mint and unrevocable at serve \u2026 The file id comes straight from the request body at all three mint sites \u2026 Put the scope INTO the token (owner, project, instance, doc, granting member, jti), re-check against live D1 at serve, and clamp member-minted TTLs well below the 400 days clampTtlDays allows.\" Only the minter recheck (I2) was implemented; the file/project binding and the member TTL clamp were not. validateCaps' own comment (473-482) asserts \"The honest boundary is the one Drive can actually enforce, and that is the project\" \u2014 which is exactly the boundary this path does not enforce.\n\nMinor accuracy notes on the write-up, none of which change the verdict: the attack's step-1 discovery needs a docId the member knows (docIds are not enumerable from any member-reachable route I found \u2014 /researcher/drive-estate at 2870 uses authResearcher and acts in the CALLER's own Drive, so it leaks nothing of the owner's), so the practical entry is a docId/file id retained from prior membership or handed over; and the `tk.i`-scoped audio/flextext variant is exploitable too, since the scoped query at 1886-1889 validates the DESTINATION instance's project, never the file."
}
```

## ab589c1597a388357

```json
{
  "refuted": false,
  "reasoning": "Verified in worker/src/v1.js. Line 1882-1897: for a member-minted token without a scoped instance, redemption runs `SELECT 1 AS ok FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?` bound to (tk.m, tk.r) \u2014 minter and OWNER, never the project the token was minted in; the token carries no project id to bind against (mintTextfileUrl, line 1215-1225, records only r/f/x/e/n/iat, plus v/i/d when scope.instanceId is set, plus m).\n\nThe vulnerable token shape is really minted: POST .../texts/<docId>/assignment/finish is gated by authMember(..., 'assignTexts') (line 3810-3813), and line 3828 mints promptUrl with scope=null and minterId=ctx.caller.researcher_id; line 1225 (`if (minterId && minterId !== researcherId) tk.m = minterId`) stamps m while i stays absent, so it lands exactly on the fallback branch. The unscoped-prompt decision is deliberate per the comment at 3819-3825, but that only concerns instance scoping, not the minter/grant check.\n\nMulti-project membership under one owner is reachable: POST /v1/projects/<id>/members (line 2448) refuses only adding the owner themself; nothing prevents the same researcher_id in two projects of the same owner, and GET /v1/projects returns a `joined` list, so it is an expected state. DELETE .../members (line 2493-2500) deletes `WHERE project_id=? AND researcher_id=?` with an explicit comment that removal from one project must not touch the other, so the row in project B survives and satisfies the fallback query. No other guard closes it: the remaining checks are tk.e > now, owner.drive_refresh_enc, and the tk.i instance-liveness check that this token shape skips. TTL is up to 400 days (clampTtlDays, line 1186-1190).\n\nNet: a member removed from project A but still a member of project B (same owner) keeps a live streaming URL into project A's Drive file, contradicting the in-code claim at line 1889-1892 that \"the coarse one still closes on removal\" and violating the member-minted-token-is-a-pointer invariant (I2) and project scoping.\n\nOne correction to the attack as written: v1.js has no project-deletion endpoint (only POST /v1/researcher/delete at line 4859), so the \"owner deletes project A outright\" variant is not reachable; the member-removal variant is, and that is sufficient.",
  "correction": "The primary sequence is correct as stated. Secondary claim is not reachable: there is no endpoint that deletes a project, so only the \"removed from project A while remaining a member of project B\" path can be performed."
}
```

## a405c7f8cde2ecb42

```json
{
  "refuted": false,
  "reasoning": "The code says what the finding claims and the sequence is traceable. worker/src/v1.js:1889-1896 \u2014 for a member-minted token with no scoped instance the fallback is `SELECT 1 AS ok FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?` bound to (tk.m, tk.r), i.e. \"is the minter a member of ANY project owned by this owner\", never \"the project the token was minted in\". The token has no project field to bind against: mintTextfileUrl (1209-1226) emits only r/f/x/e/n/iat, plus i/d when scoped and m when minterId !== researcherId. The unscoped member-minted token is really produced at 3828 (`mintTextfileUrl(..., body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id)`), reached after `authMember(request, env, { instance: instanceId }, 'assignTexts')`, so ctx.caller can be a member \u2260 owner and tk.m is set with tk.i absent. clampTtlDays (1186-1190) caps TTL at 400 days, so the long-lived case in the attack is real. Removal is per-project (2497-2502: `DELETE FROM project_member WHERE project_id=? AND researcher_id=?`, whose own comment notes a person may be a member of two projects), so the row in project B survives and satisfies the fallback. Refutations tried and rejected: (a) \"membership implies they could just re-mint the URL\" \u2014 the fallback tests membership only, not caps, so a member left in project B with caps {} or drive:read cannot mint anything via authMember(...,'assignTexts') yet keeps the URL minted in A; that is access beyond their remaining grant. It only collapses when they retain assignTexts under the same owner (and even then only because minting itself does not check that promptFileId lies in the caller's project). (b) \"deliberate per the comment\" \u2014 the coarseness is deliberate, but the comment's justification, \"the coarse one still closes on removal\", is exactly what fails, and invariant I2 (\"a member-minted token is a pointer, good only while the grant is\") is a stated design rule, not a preference. Sharpest concrete sequence: O owns A and B; M is a member of both, with assignTexts in A and non-assignTexts caps in B; M mints a promptUrl via POST /v1/instances/<inst in A>/texts/<docId>/assignment/finish; O removes M from A; M re-fetches the URL, the fallback finds the project-B row, and the file keeps streaming until the token expires. Same holds if O deletes project A while M remains in B.",
  "correction": "The finding stands as written; the only refinement is scope of harm. When the removed member still holds assignTexts in another project of the same owner, the surviving token grants nothing they could not re-mint, because the mint path at 3828 never checks that body.promptFileId lies inside the caller's project. The un-refutable case is a member left in another of the owner's projects WITHOUT assignTexts (or with caps {}): the fallback query checks membership only, never caps, so they retain a live streaming URL their remaining grant could never produce."
}
```

## ae95cdd4241eed90e

```json
{
  "refuted": true,
  "reasoning": "The mechanical claims are accurate \u2014 worker/src/v1.js:1885-1888 selects `1 AS ok` and never reads pm.caps; validateCaps({}) returns a truthy {} so an INSERT OR REPLACE at :2486 leaves a caps-empty membership row; the mint at :3806 does require 'assignTexts'. But the behaviour is deliberate and correct per the design, and the implied fix would cause a real regression.\n\n(a) The invariant is stated in terms of REMOVAL, not capability. The block comment at :1855-1882 reads \"remove that member and, without this, they keep reading those files for the rest of the 90 days with no grant behind it\", and the fallback branch's comment says the coarser any-project check is used \"deliberately so \u2026 the coarse one still closes on removal, which is what the invariant is about.\" A check that intentionally accepts membership in a DIFFERENT project as sufficient is not one that was attempting capability precision and missed it.\n\n(b) No listed design rule requires caps re-check at redemption. validateCaps' own comment defines the model: \"membership in a project grants access to everything IN that project, and the capabilities below say what may be DONE.\" The token serves a file inside a project the downgraded member is still a member of, so project scoping is not breached; I1/I4/denial-opacity/owner-not-a-member/wrap-to-owner/identity/wipe-owner-only are all untouched.\n\n(c) The fix would break working assignments. The token is redeemed by the FIELD DEVICE with no headers, not by the member. Reading pm.caps at redemption means any routine capability edit (e.g. clearing assignTexts once a member's assigning phase is done) 410s in-flight assignment media on innocent devices. The code explicitly weighed that cost and confined it to removal (\"\u26a0 AND YES, THIS CAN CUT OFF A FIELD DEVICE mid-assignment when its minter is removed. That is the intended trade\").\n\n(d) The sequence is API-only and the residual is bounded: there is no member-management UI yet (grep for 'projects/.*members' and 'assignTexts' in docs/js/researcher-panel.js returns nothing), the residual is read access to files the member already listed and selected while legitimately holding drive:read, and the owner's actual revocation tool (DELETE /v1/projects/<id>/members, :2497-2513) closes membership and key grants in one batch and does close the token. This is the same acknowledged class of limit as the adjacent \"deleting the row \u2026 cannot un-know a key the member already fetched\" comment.",
  "correction": "Redemption checks membership, not capability, by design: the documented invariant (worker/src/v1.js:1855-1893) is that a member-minted token dies when the MEMBERSHIP dies, and the fallback branch is deliberately coarser still (any project of that owner). A capability downgrade that keeps the membership row therefore leaves outstanding tokens serving, which is the intended granularity \u2014 the owner's revocation tool is DELETE /v1/projects/<id>/members (:2497-2513), which does close them. Re-checking caps at redemption would 410 innocent field devices mid-assignment on any routine capability edit, a blast radius the code deliberately confined to member removal."
}
```

## aa46cbdf3bf8f2f37

```json
{
  "refuted": false,
  "reasoning": "I traced the sequence in the actual code and it reproduces.\n\nMINT (worker/src/v1.js:3828, inside POST /v1/instances/<id>/texts/<docId>/assignment/finish, which is gated by `authMember(request, env, { instance: instanceId }, 'assignTexts')`):\n`const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);`\n`scope` is `null` \u2014 deliberately, per the comment at 3819-3824 (\"Deliberately unscoped; do not 'fix' the inconsistency by scoping it\"). In mintTextfileUrl (1215-1225), `tk.i`/`tk.v` are set only `if (scope && scope.instanceId)`, and `if (minterId && minterId !== researcherId) tk.m = minterId;`. So a member-minted prompt token carries `m` and no `i` \u2014 exactly the shape that lands in the fallback branch. TTL is `clampTtlDays` (1186-1190), max 400 days.\n\nREDEEM (1882-1897): `tk.i` is absent so the instance-liveness check at 1859-1863 is skipped, and the grant check runs the else branch:\n`await env.DB.prepare('SELECT 1 AS ok FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?').bind(tk.m, tk.r).first()`\nThe bind pair is (minter, owner). The token carries no project id, so nothing binds the check to the project the token was minted in.\n\nREVOKE (2497-2503): the DELETE members batch is scoped to one project, and says so \u2014 \"a person may be a member of two projects, and removing them from one must not touch the other\" \u2014 deleting only `project_member` and `member_key` for that project. There is no token deny-list or epoch anywhere (the only mention, at 1213, calls it \"a future deny-list\"), so nothing else closes the token.\n\nSo: owner O with projects A and B, member M in both (multi-project ownership is real \u2014 reconcileProjects inserts one project row per Drive project folder at 1794, and POST members is keyed (project_id, researcher_id)); M mints a prompt URL against an instance in A; O removes M from A; M re-fetches the URL; the fallback finds M's surviving row for B (p.owner_id is still O) and the file streams for the rest of the TTL. The file is genuinely inside A's subtree \u2014 a 'consent-prompt' upload is parented to `driveEnsureDeviceFolder(...)` (3766-3768, 1014-1034), and device folders sit under the project folder on a migrated estate, which membership requires (the 409 not_migrated gate at 2473-2481). That is a member reading a file in a project they are no longer in, which is what \"access is PROJECT-scoped\u2026 nothing outside it\" and I2 (\"a pointer that is only good while the grant is\") forbid.\n\nTwo things I checked as possible refutations and neither holds. (a) \"Deliberate and correct\": it is deliberate, but the justification in the comment \u2014 \"the precise question is unanswerable without the instance, and the coarse one still closes on removal\" \u2014 is wrong on both halves. It does not close on removal for a multi-project member, and the project id IS available at mint time (authMember returns `project_id`, and the route already holds `ctx`), so a `tk.p` stamped at mint plus a bind on (m, p) would answer the precise question without any instance. (b) \"Already guarded elsewhere\": scoped tokens (audioUrl/flextextUrl, 3826-3827) do close correctly via the `i.project_id=pm.project_id` join, but that is the other branch; nothing guards the unscoped one.\n\nScope of the real finding, stated honestly: the leak needs the multi-project precondition (a member of exactly one of the owner's projects is correctly cut off), and it covers unscoped member-minted tokens only \u2014 in practice `promptUrl` from 3828. The crowd prompt mint at 4809 passes no minterId at all, so it never carries `m`; that is a separate question, not this one.",
  "correction": "The claim holds as written, with two qualifications worth carrying into any fix. (1) It bites only when the removed member remains in another project of the SAME owner \u2014 remove them from the owner's estate entirely and the coarse query does close the token, so the comment is right in the single-project case and wrong in the multi-project case it does not consider. (2) It affects unscoped member-minted tokens only; the scoped audio/flextext tokens minted at 3826-3827 bind through `instance i ON i.project_id=pm.project_id` and correctly close on removal from the minting project. The fix is at mint, not at redemption: stamp the resolved project on member-minted tokens (`tk.p = ctx.project_id`, available from authMember) and bind the fallback on (tk.m, tk.p), failing closed when a member-minted token carries no `p`."
}
```

## a1ccaffaaae0e889c

```json
{
  "refuted": true,
  "reasoning": "The MECHANICAL half of the claim is accurate \u2014 I verified every line. worker/src/v1.js:1884-1897 does select a constant, not caps:\n\n```js\nif (tk.m && tk.m !== tk.r) {\n  ok2 = tk.i\n    ? await env.DB.prepare('SELECT 1 AS ok FROM project_member pm JOIN instance i ON i.project_id=pm.project_id '\n        + 'WHERE pm.researcher_id=? AND i.instance_id=?').bind(tk.m, tk.i).first()\n    : \u2026\n  if (!ok2) return j({ error: 'gone' }, 410, origin, env);\n}\n```\nand the members POST (2486-2488) is `INSERT OR REPLACE`, with `validateCaps({})` returning a truthy `{}` (v1.js:465-499: `const out = {}` \u2026 `return out`), so a caps-emptied row does survive and the token does keep streaming. The sequence reproduces.\n\nBut it is not a defect, for three reasons that together outweigh the mismatch.\n\n1. It is the documented intent of this check, stated twice. The block's own header defines the invariant as closing on REMOVAL \u2014 \"remove that member and, without this, they keep reading those files for the rest of the 90 days with no grant behind it\" \u2014 and the unscoped fallback is annotated \"Coarser, and deliberately so \u2014 \u2026 the coarse one still closes on removal, which is what the invariant is about.\" Nothing in the design rules under audit (I1, I4, denial-as-absence, project scoping, owner-not-a-member, member_key wrap, identity, wipe/force owner-only) requires redemption to re-evaluate the minting capability. I4 is not implicated: the query resolves, and every unresolvable branch already denies (`catch { ok2 = null; }` \u2192 410).\n\n2. No privilege is gained. A `/v1/textfile` token names ONE Drive file id that the member themselves supplied while holding assignTexts, and they necessarily saw and could fetch it then (they also held drive:read, which is what `/texts/<docId>/files` at 3663 gates). After the downgrade they can mint nothing new (3806/3855/3890 all deny) and list nothing (3663 denies). The residual is a tail on bytes they already had \u2014 not access to anything they could not already have copied.\n\n3. The proposed fix would cause a real bug \u2014 the failure mode this audit is told to avoid. The token is the DEVICE's header-less download URL; the worker cannot tell the member's fetch from the field device's. The code accepts that cost explicitly and only for removal: \"\u26a0 AND YES, THIS CAN CUT OFF A FIELD DEVICE mid-assignment when its minter is removed. That is the intended trade.\" Re-checking `pm.caps` at redemption would 410 live, in-flight assignments every time an owner made ANY narrowing caps edit on a member they still trust and still keep in the project (e.g. dropping assignTexts while keeping drive:read) \u2014 breaking innocent devices' downloads for an action that was not a revocation.\n\nSecondary: there is no client path to the downgrade at all \u2014 `grep assignTexts|manageDevices docs/js/*.js` returns nothing, so a member-management UI does not exist yet; the \"attack\" is the owner hand-POSTing a partial revocation and then expecting full-revocation semantics, when DELETE /v1/projects/<id>/members (2497-2508) is the action that does close the tokens.",
  "correction": "Redemption really does check membership only, never caps \u2014 but that is the documented scope of the invariant (\"closes on removal\"), it grants the ex-capable member nothing beyond specific files they already held and minted, and tightening it would 410 legitimate field-device downloads on every narrowing caps edit. At most this is a docs/UI note: emptying a member's caps is not revocation; DELETE the membership row, which does invalidate their minted tokens."
}
```

## a1ae53599c96c3d96

```json
{
  "refuted": false,
  "reasoning": "Not refuted \u2014 the code says what the finding claims, no other guard closes it, and the sequence traces end to end.\n\n1) Redemption (worker/src/v1.js:1883-1896). For a member-minted token the grant re-check is `ok2 = tk.i ? \u2026 'SELECT 1 AS ok FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?').bind(tk.m, tk.i) : \u2026 'SELECT 1 AS ok FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?').bind(tk.m, tk.r)`. It selects the constant 1; `pm.caps` is not in either projection and no caller parses caps. The mint site does require one: v1.js:3806 `const ctx = await authMember(request, env, { instance: instanceId }, 'assignTexts');`.\n\n2) The downgrade is a real, accepted operation. POST /v1/projects/<id>/members is an upsert \u2014 v1.js:2486 `'INSERT OR REPLACE INTO project_member (project_id, researcher_id, caps, added_at, added_by) VALUES (?,?,?,?,?)'` \u2014 and `validateCaps({})` (v1.js:465-500) returns `{}`, which passes the handler's `if (!caps) return bad_caps` guard because `{}` is truthy. The `not_migrated` 409 gate is already satisfied (the member could only have been added on a project whose `drive_folder_id` was set). So the row survives with caps '{}'.\n\n3) After the downgrade the member is fully denied everywhere else, which is what makes the surviving token access beyond the remaining grant rather than a duplicate of it: authMember denies `drive:read` (v1.js:640 `if (caps.drive !== 'read' && caps.drive !== 'manage') return deny;`) and every assignTexts route; and every `needCap: null` call site (2369, 2430, 4058, 4096) additionally requires `!ctx.ok || !ctx.isOwner \u2192 not_found`, so bare membership reaches no route at all. Yet the token keeps streaming: /v1/textfile needs no headers, and its remaining checks are decrypt, `tk.e > now`, owner `drive_refresh_enc`, and (for tk.i) `SELECT instance_id FROM instance WHERE instance_id=? AND researcher_id=? AND revoked=0` \u2014 none of which the downgrade touches. TTL is up to 400 days (clampTtlDays).\n\nConcrete sequence: O POSTs /v1/projects/A/members {researcher_id:M, caps:{assignTexts:true, drive:'read'}}; M POSTs /v1/instances/<D>/texts/<docId>/assignment/finish and receives audioUrl/flextextUrl (v1.js:3826-3832), tokens carrying m=M, i=D; O re-POSTs the same member with caps:{} (row replaced, membership intact); M GETs the saved URL with no headers \u2014 the join at 1885-1888 matches on the surviving row and the owner's Drive bytes stream.\n\nRefutations attempted and rejected: (a) \"membership already grants read of everything in the project\" (validateCaps' comment) \u2014 contradicted by the existence of `caps.drive: 'read'|'manage'` and by `drive:read` gating GET .../texts/<docId>/files (v1.js:3663); a caps-{} member demonstrably has no read grant. (b) \"deliberate: the check is about removal, and cutting a device off mid-assignment is the cost of removal only\" \u2014 the design treats NARROWING as a revocation event that must take effect (plans/project-split.md, the member_key item: \"a member narrowed from see:all to [i1] can still fetch i2's key they never previously held. That is key DELIVERY after revocation, which is the one thing revocation actually promises\"), and I2 is stated as \"a pointer that is only good while the grant is\" (project-split.md:553-558); the plan's own remediation text at 1708-1714 says to put \"(owner, project, instance, doc, granting member, jti)\" into the token and \"re-check against live D1 at serve\". (c) \"unreachable because there is no members UI\" \u2014 grep for members/project_member in docs/js/researcher-panel.js returns nothing, so the entire feature is API-only in this build; add and downgrade are equally reachable, so this discounts the feature, not the finding. (d) \"the member already downloaded the files\" \u2014 the honest-limit section allows un-downloading to be impossible but requires \"no further access from the moment of revocation \u2014 no new reads\"; a live pointer is a new read.\n\nSeverity caveat that lowers but does not refute: the residual access is bounded to file ids that member themselves minted tokens for, and it only bites when the owner downgrades caps instead of DELETEing the membership row (DELETE at 2493-2502 does close it, along with the member_key grants)."
}
```

## a9b5a8c6cb6a2aa44

```json
{
  "input": "{}",
  "refuted": false,
  "reasoning": "I traced the code and could not refute it; the sequence is concrete.\n\nWHAT THE CODE ACTUALLY SAYS\n- worker/src/v1.js:3663-3680 (GET .../texts/<docId>/files): `const ctx = await authMember(request, env, { instance: instanceId }, 'drive:read');` \u2026 `const r = ctx.owner;` \u2026 `const owned = await env.DB.prepare('SELECT instance_id FROM instance WHERE instance_id=? AND researcher_id=?').bind(instanceId, r.researcher_id).first();` \u2026 `const docId = String(seg[4] || '')\u2026` \u2026 `const access = await driveAccessToken(env, r);` \u2026 `const fq = encodeURIComponent(\\`appProperties has { key='flextextDoc' and value='${docId}' } \u2026 \\`)` \u2026 `driveJson(access,'GET','\u2026/files?spaces=drive&orderBy=createdTime&fields=files(id)&q=' + fq)`. Authorization is resolved from the PATH instance's project; the object is resolved from the caller-supplied docId over the OWNER's entire Drive. The `owned` re-check only asserts the instance still belongs to the owner \u2014 it says nothing about the doc. Nothing compares the found folder's parents to `ctx.project_id`.\n- Identical unscoped tag queries with the owner's token in adopt (3868-3877, followed by `if (!(f.parents || []).includes(toFolder)) await driveReparent(access, f.id, toFolder, f.parents);` and the `flextextUnassigned: ''` PATCH) and move (3909-3915, the addParents/removeParents PATCH).\n- authMember (543-645) resolves `project_id` off the instance row and returns `owner`/`caps`; it never returns or checks a Drive folder. Multiple projects per owner are the design (`SELECT \u2026 FROM project WHERE owner_id=? ORDER BY created_at` at 1760/2400; POST /v1/researcher/projects/create at 3156 mints more; validateCaps' own comment recommends \"a separate project\" as the narrower boundary), and all project folders sit under the one master \"FlexText Uploads\" folder (buildDriveEstate, 773-890), so an account-wide `spaces=drive` tag search spans projects by construction.\n- The finding's grep claim holds: `drive_folder_id` is read only at 1679/1686/1695/1700/1706/1719/1754/1760-1794/2400/2465-2475/3178/3234/3271 \u2014 all owner-only routes (reconcileProjects, GET /v1/projects, the members-POST migration gate, projects/assign). It is read nowhere in the /v1/instances block a member can reach. check-project-scoping.sh only enforces that authMember is CALLED in the right shape (null vs ok:false vs no 403, wipe/force-remove owner-only); it has no notion of docId or folder scoping.\n\nWHY THIS IS A DEFECT AND NOT AN ACCEPTED LIMIT\nThe members-POST gate's own comment (2455-2470) states the intended rule outright: *\"The boundary a member is confined to IS a Drive project folder \u2014 Seth's rule is that they must not reach 'the root folder outside of projects shared with them', so a member's file listing has to be ROOTED at that folder rather than walked from the account master and filtered afterwards.\"* The 409 not_migrated gate exists to guarantee that root exists \u2014 and then no member-reachable route ever uses it. validateCaps' comment concedes only the sub-project (per-device) granularity as unenforceable; it explicitly names the project as the boundary that IS enforced. Here it is not.\n\nCONCRETE SEQUENCE (no attacker foresight needed)\nOwner O owns projects A and B. M is a project_member of A with {\"assignTexts\":true,\"drive\":\"read\"}, not of B. Device D is in project A; M legitimately learns D's texts' docIds while D is in A. O then moves D into B with POST /v1/researcher/projects/assign (3212), which sets `instance.project_id` = B and carries D's text folders into B's subtree in Drive. M now sends GET /v1/instances/<any-A-instance>/texts/<docId-of-a-text-now-in-B>/files: authMember resolves the A instance \u2192 ok (drive:read), `owned` passes, and the account-wide tag query returns B's text folderId, originalsFolderId and every child file's id/name/size/mime/role. POST .../texts/<same-docId>/adopt with `{}` on an A instance then reparents that folder out of B and under an A device folder and clears `flextextUnassigned`, physically relocating B's recordings/flextexts/consent files into A; .../move does the same to a second A instance. Same outcome for a member removed from B who retains a docId (removal deletes project_member + member_key rows only). The repo's own standard rejects \"the ids are unguessable\" as a defense \u2014 see the revoke comment at 3947-3960 (*\"Today the only barrier is that instance ids are unguessable. Under the projects/researchers split every member legitimately SEES those ids\"*).\n\nRefutations I tried and rejected: (a) a guard elsewhere \u2014 none; the only re-check is `researcher_id`-scoped, which is owner-wide, i.e. exactly the fallback I4 forbids; (b) unreachable path \u2014 all three routes are ordinary member-reachable routes with the caps a normal assistant researcher gets; (c) deliberate \u2014 the design comments state the opposite intent; (d) single-project accounts \u2014 multi-project per owner is the documented arrangement.\n\nNote on scope, not a weakening: the reparenting variants (adopt/move) also mean pre-existing owner-only behaviour is now delegated, so the fix has to scope the docId resolution (verify the found folder's ancestry against `project.drive_folder_id`, fail closed to not_found), not merely restrict the caps."
}
```

## a1b300bd3471f3f62

```json
{
  "refuted": false,
  "reasoning": "CONFIRMED \u2014 I traced the sequence in the code and every cited line is exact.\n\n1) The code says what the finding claims. v1.js:3663-3680 (files) authorizes on the PATH instanceId via authMember(...,'drive:read'), sets `const r = ctx.owner`, re-checks only `SELECT instance_id FROM instance WHERE instance_id=? AND researcher_id=?` (owner ownership of the *instance*, nothing about the doc), then takes docId straight from `seg[4]` and runs `appProperties has { key='flextextDoc' and value='${docId}' } ...` against `files?spaces=drive` with `driveAccessToken(env, r)` \u2014 the OWNER's token, whole account. Identical unscoped searches at 3868-3870 (adopt, followed by `driveReparent(access, f.id, toFolder, f.parents)` and a PATCH clearing `flextextUnassigned`) and 3909-3910 (move, addParents/removeParents PATCH). assignment/begin (3737-3738) reaches the same search through driveEnsureTextFolder, whose header at 1134-1136 states the property outright: \"The tag search is scoped to trashed=false but NOT to the parent\". Nothing between authMember and any of these Drive calls compares the found folder's parents to ctx.project_id.\n\n2) Cross-project reach is real, not hypothetical. One researcher = one Drive token; reconcileProjects (1723-1800) exists precisely because one owner had TWO project folders in one account (\"Fayu Text Corpus\" and \"Dani Dictionary\"), and buildDriveEstate (786-789) shows the tree master/<project>/<container>/<text>. Every project's text folders carry flextextDoc, so an account-wide tag query spans them.\n\n3) The intended guard is absent. grep shows `drive_folder_id` read only at 1679/1700/1760/1787/1794/2400/2473/3234 \u2014 all in /v1/researcher/* (authResearcher, caller's own Drive) or the backfill; no route under /v1/instances reads it. The members-POST gate's own comment (2456-2461) states the rule the converted routes do not implement: \"a member's file listing has to be ROOTED at that folder rather than walked from the account master and filtered afterwards.\" check-project-scoping.sh only checks call SHAPE (!ctx.ok guards, no 403, no authResearcher in the block, wipe/force-remove owner-only) \u2014 it has no Drive-parentage check.\n\n4) The request sequence is performable. M, member of project A with {drive:'read'|'manage', assignTexts:true}: authMember({instance:A-instance}) resolves A, finds the project_member row, passes the cap, returns owner=O; the `owned` re-check passes because the A instance does belong to O. GET /v1/instances/<A-instance>/texts/<B-docId>/files then returns B's folderId, originalsFolderId and every child file's id/name/size/mime/role. POST .../texts/<B-docId>/adopt physically re-parents B's text folder under a device folder inside project A.\n\nRefutation attempts that failed: (a) GET /v1/researcher/drive-estate uses authResearcher with the CALLER's own Drive token, so it is not a docId oracle for a member \u2014 that limits discovery, it does not scope the vulnerable routes. (b) docId is crypto.randomUUID() (docs/js/sync.js:107), so it is not enumerable \u2014 again a precondition on the attacker, not a guard in the code. (c) Both no-foresight paths hold: a member removed from B keeps docIds their panel already decrypted (removal deletes project_member + member_key but cannot un-know cached ids \u2014 the DELETE route's own comment at 2360-2363 says exactly that about keys), and POST /v1/researcher/projects/assign (3211-3247) rewrites instance.project_id when a device moves between projects specifically so \"a container that moved in Drive while D1 still said otherwise would be authorized against the project it just left\" \u2014 the docId routes silently undo that revocation, since \"A container keeps its folder ID across the move, so nothing that resolves by id notices\". (d) Not deliberate: validateCaps (474-482) concedes only per-DEVICE separation as unenforceable and names the project as \"the honest boundary\"; the audit's design rule says a member \"must never reach ... another project\".\n\nPre-Phase-C the unscoped search was harmless because only the owner could reach these routes and the search stayed inside their own Drive. Phase C is what converts it into a cross-project path, so this is a defect introduced by the change under audit.",
  "correction": "The mechanism and the code quotes are correct; two qualifications on scope. (i) The docId is a random UUID (crypto.randomUUID, docs/js/sync.js:107) and no member-reachable route lists the owner's whole estate \u2014 /v1/researcher/drive-estate runs under authResearcher with the CALLER's own Drive token \u2014 so this is not blind discovery: the member must already hold a docId from outside their current project. The two concrete ways they do are the ones named (prior membership in B, and a device reassigned A\u2192B via /v1/researcher/projects/assign, which rewrites instance.project_id precisely to revoke the old project's members while leaving every docId still resolving). (ii) The exposure is slightly wider than the five routes listed: assignment/begin and assignment/upload/start reach the same unscoped search through driveEnsureTextFolder (v1.js:1153), and driveEnsureTextFolder additionally trusts a caller-supplied `body.folderId` after only a files.get existence/trashed check (1146-1152), so a member can also target an arbitrary folder id directly, not just a docId."
}
```

## a146836deb569e0ff

```json
{
  "refuted": false,
  "reasoning": "I traced every step in worker/src/v1.js and each quoted line says what the finding claims.\n\n1. Mint accepts any caller-supplied id with zero validation. v1.js:1209-1216:\n   `async function mintTextfileUrl(env, urlOrigin, researcherId, fileId, extract, ttlMs, scope, minterId) { if (!fileId) return null; const tk = { r: researcherId, f: fileId, x: extract || '', e: Date.now() + (ttlMs || 90*86400000), n: \u2026, iat: \u2026 };`\n   No Drive `files.get`, no parents check, no comparison against the project's Drive folder, no comparison against the text folder the assignment flow just created.\n\n2. The call sites hand it the OWNER's identity plus a body-supplied file id. v1.js:3809 `const r = ctx.owner;` and 3826-3828:\n   `const audioUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.audioFileId, '', ttlMs, scope, ctx.caller.researcher_id);`\n   `const flextextUrl = \u2026 body.flextextFileId \u2026`\n   `const promptUrl = await mintTextfileUrl(env, url.origin, r.researcher_id, body.promptFileId, '', ttlMs, null, ctx.caller.researcher_id);`\n   and the response returns them to the caller: v1.js:3832 `return j({ ok: true, ttlDays, audioUrl, flextextUrl, promptUrl }, 200, origin, env);`. Same unvalidated `mint(fileId, extract)` closure at 3881 (adopt) and 3921 (move), both also with `r = ctx.owner` and `ctx.caller.researcher_id` as minter.\n\n3. authMember (v1.js:542-647) resolves the project only from the *target instance* (`SELECT project_id, researcher_id FROM instance WHERE instance_id=?`) and the caps row. It never sees a file id, so nothing upstream constrains `body.*FileId`.\n\n4. Redemption checks the minter's membership, never the file. v1.js:1885-1896:\n   `ok2 = tk.i ? 'SELECT 1 AS ok FROM project_member pm JOIN instance i ON i.project_id=pm.project_id WHERE pm.researcher_id=? AND i.instance_id=?' : 'SELECT 1 AS ok FROM project_member pm JOIN project p ON p.project_id=pm.project_id WHERE pm.researcher_id=? AND p.owner_id=?'`\n   Neither query mentions `tk.f`. The scoped branch asks \"is the minter a member of the project the *instance* belongs to\" (true for a project-A instance); the unscoped branch asks \"a member of ANY project this owner owns\" (true because of project A). Then v1.js:1899-1906 streams with the OWNER's credential: `const access = await driveAccessToken(env, owner); \u2026 fetch('https://www.googleapis.com/drive/v3/files/' + encodeURIComponent(tk.f) + '?alt=media', { headers: { Authorization: 'Bearer ' + access, \u2026 } })`.\n\nConcrete sequence I can perform: M is a project_member of project A with {\"assignTexts\":true} and formerly a member of project B under the same owner (so M holds B's file ids and docIds \u2014 the panel shows them; the `/texts/<docId>/files` route at 3661-3679 is itself a global `spaces=drive` appProperties search, not parent-scoped, so a known foreign docId also yields fresh ids through a project-A instance). M sends POST /v1/instances/<A-instance>/texts/x/assignment/finish {\"promptFileId\":\"<B-file-id>\",\"ttlDays\":400}. authMember passes (instance is in A, cap present); `inst` lookup passes (instance belongs to the owner); `clampTtlDays` caps at 400 days (v1.js:1186-1190); the prompt token is minted unscoped so it carries tk.m=M, tk.r=owner, no tk.i. The response hands M the URL. GET /v1/textfile/<token> takes the `tk.m && !tk.i` fallback, which is satisfied by M's project-A row, and streams B's file. Being unscoped, it also survives revoking every device in project A.\n\nRefutation attempts that failed: there is no parentage/containment guard anywhere (grep for project_id, mintTextfileUrl, driveProjectFolderFor turns up nothing on this path); check-project-scoping.sh only enforces the `!ctx / !ctx.ok` shape, 403-avoidance and owner-only wipe/force-remove \u2014 it says nothing about file ids; the `scope` comment at 3819-3824 explains why the prompt token is unscoped (a real, deliberate decision) but that is about instance revocation, not about which project the file is in; the assign-copy removal note at 3936 shows the codebase already treats \"a caller-chosen Drive id\" as the hazard class, so this is not a documented accepted risk.\n\nThis violates the stated rules directly: \"A member reaches everything in their project, nothing outside it, and must never reach the account master Drive folder or another project,\" and I4 fail-closed (the file's project is never asked, so it cannot deny). It also defeats the very invariant the tk.m check was added for \u2014 the coarse \"any project of this owner\" fallback lets an ex-member of B keep minting *fresh* 400-day reads of B's files for as long as they hold any other membership under that owner.\n\nConfirmed, not refuted."
}
```
